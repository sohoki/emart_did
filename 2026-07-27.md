# 2026-07-27 작업 로그

## 00:40 — 채번 정책 변경(MAX+1 → 실제 DB 시퀀스) + sts/brd/web 3개 컨트롤러 리팩토링
**파일:** `did_emart/db-encoding-fix/08_create_missing_sequences.sql`(신규),
`backend/.../sts/mhs/{mapper/MhsViewConnInfoManageMapper.xml,.java, service/MhsViewConnInfoManageService.java, web/CultureDisInfoManageController.java}`,
`backend/.../sts/cnt/{mapper/*, service/*, web/*}`(content_seq/contentfile_seq/condetail_seq 관련),
`backend/.../sts/snd/{mapper/SendMsgInfoManagerMapper.xml,.java, service/SendMsgInfoManageService.java}`,
`backend/.../sts/xml/{mapper/XmlInfoManagerMapper.xml,.java, service/XmlInfoManageService.java}`,
`backend/.../sts/brd/web/{BrodAnniversaryManagerController, BrodContentDetailManageController, BrodScheduleManagerController}.java`(신규 재작성),
`backend/.../sts/brd/mapper/{BrodAnniversaryManagerMapper.xml, BrodContentInfoManagerMapper.java+.xml, BrodScheduleInfoManagerMapper.xml}`,
`backend/.../sts/brd/service/BrodContentInfoManageService.java`,
`front/src/pages/backoffice/BrodManage/{BrodAnniversaryListPage, BrodScheduleStatusPage}.jsx`(신규),
`front/src/constants/URL.jsx`, `front/src/App.jsx`

### 변경 내용
- 사용자 지시로 정책 변경: 지금까지(어제 9~15절) "원본 DB 시퀀스가 없다"고 확인될 때마다
  MAX+1로 대체 채번해온 것을, "시퀀스는 나중에 자동증가로 바꿀 예정이니 MAX 말고 시퀀스 생성
  스크립트를 작성하라"는 지시에 따라 순수 숫자 시퀀스 9개(mhsconn_seq, content_seq,
  contentfile_seq, condetail_seq, conanniversary_seq, xml_seq, msg_seq, brodcondetail_seq,
  brodconimsi_seq)를 실제 PostgreSQL 시퀀스로 되돌림. 단건 INSERT는 MyBatis
  `<selectKey order="BEFORE">NEXTVAL(...)</selectKey>`로, 다건 복사(INSERT...SELECT)는
  원래 SQL의 NEXTVAL 리터럴을 그대로 유지(둘 다 애초에 정상 동작하는 패턴이라 손댈 필요 없었음)
- 단, `MCL_`/`EMART_`/`MSG_`/`BROD_` 같은 접두사 포맷 ID(EgovIdGnrService 빈이나 DB 함수 기반)는
  순수 시퀀스가 아니라서 이번 정책 변경 대상에서 제외하고 MAX+1 방식 유지
- 08_create_missing_sequences.sql은 DB 생성이 필요한 작업이라 CLAUDE.md 규칙에 따라 스크립트만
  준비(미실행, 사용자 검토 후 직접 실행 예정)
- sts/brd/web 6개 컨트롤러 중 "작은 3개 우선"으로 진행(큰 2개 BasicBrodManageController(1098줄)/
  BrodContentInfoManageController(1162줄)는 후속으로 미룸):
  - BrodAnniversaryManagerController(183줄) — 이미 REST 뼈대로 작성돼 있었으나 실제 서비스와
    메서드명·파라미터 타입이 전혀 안 맞는 상태였음(존재하지 않는 메서드 호출 다수). 실제 서비스
    계약에 맞춰 재작성. 방송-기념일 체크박스 연결/해제 기능은 테이블 구조상 개념 자체가
    매퍼에 없어 제외
  - BrodContentDetailManageController(480줄) — 서비스 계약이 정확히 일치하는 순수 레거시
    포트(schUpdate 스케줄 재계산 로직 포함 그대로 포팅)
  - BrodScheduleManagerController(330줄) — 매장별 배포 연결/해제 로직 포팅
  - **버그 발견/수정**: BrodScheduleInfoManagerMapper.xml의 selectBrodRigthLst가 특정
    방송코드를 하드코딩(`'BROD_0000000474'`)하고 있어 어떤 방송을 조회하든 항상 그 기준으로만
    배포여부가 표시되던 버그를 파라미터 바인딩으로 수정
  - BrodAnniversaryVO.java의 잘못된 자기참조 import(같은 패키지인데 존재하지 않는 legacy 경로
    import)도 함께 수정
- 프론트: 방송 기념일 CRUD 페이지, 배포 스케줄(좌측 방송목록+우측 매장 연결/해제) 페이지 신규
  구축. 콘텐츠 편성(파일 배치/시간대 자동계산) 화면은 복잡도가 높아 백엔드 API까지만 준비

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [ ] Performance concerns
- [ ] Test coverage (프로젝트 전체 컴파일 실패로 실제 기동 테스트 불가 + 신규 시퀀스 스크립트 미실행)

### 이슈 / 개선사항
- 전체 컴파일 에러 241개 → 185개로 감소
- **08_create_missing_sequences.sql을 실행하기 전까지는 이번에 손댄 채번 로직(9개 시퀀스
  관련 등록 기능)이 런타임에 실패함** — 우선순위 높음
- 여전히 프로젝트 전체 빌드는 안 되어 실기동 테스트는 못 함
- 다음 작업: 시퀀스 스크립트 실행 확인, sts/brd/web 남은 2개 대형 컨트롤러

## 03:20 — 콘텐츠 편성 프론트 구축 + sts/brd/web 남은 대형 컨트롤러 2개 완료
**파일:** `front/src/pages/backoffice/ConManage/{ContentDetailEditorPage(신규), ContentMutiListPage}.jsx`,
`backend/.../sts/brd/web/{BrodContentInfoManageController, BasicBrodManageController}.java`(전면 재작성),
`backend/.../sts/brd/mapper/{BrodContentInfoManagerMapper.xml, BasicBrodManagerMapper.xml}`,
`backend/.../sts/brd/service/BasicBrodInfoManageService.java`,
`did_emart/db-encoding-fix/08_create_missing_sequences.sql`(시퀀스 4개 추가, 총 13개),
`front/src/pages/backoffice/BrodManage/{BasicBrodListPage, BrodContentListPage}.jsx`(신규),
`front/src/constants/URL.jsx`, `front/src/App.jsx`

### 변경 내용
- 콘텐츠 편성 화면(어제 후속으로 미룬 부분) 신규 구축: `ContentDetailEditorPage.jsx`에서
  콘텐츠 상세페이지 탭별 배치 파일 조회/추가/순서변경/시간간격 수정/삭제 지원, 목록 페이지에
  "편성" 버튼으로 연결
- `BrodContentInfoManageController.java`(1162줄) 전면 재작성 — 방송(음원) 콘텐츠 CRUD +
  우측 편성후보 조회/삭제/복사 등 포팅. "편성표 생성/조회/엑셀" 클러스터(약 410줄)는 사용자
  확인 후 제외(존재하지 않는 `UniSelectInfoManageService`에 의존 — 동적 SQL 조립 패턴이라
  추측 구현 시 SQL 인젝션 위험, 그리고 이미 제외됐던 `FN_CENTERBRODINFO` 의존 기능도 포함)
  - 매퍼 버그 수정: `selectBrodContentLst`가 없는 DB 함수 `FN_DETAILCODENM(...)` 호출 →
    서브쿼리로 대체
- `BasicBrodManageController.java`(1098줄) 전면 재작성 — 앞부분(이미 REST 전환됨)은 누락된
  import(`DeleteMapping`/`GetMapping`/`RequestParam`) 추가 + 잘못된 import 제거로 컴파일
  가능하게 고치고, 뒷부분(약 610줄 레거시)을 REST로 전환. 엑셀 다운로드 2개는 존재하지 않는
  View Bean에 의존해 제외(후속 작업 필요)
  - **버그 발견/수정**: `insertBasicBrodCopy`가 컨트롤러가 이미 덮어쓴 새 BASIC_CODE로
    복사원본을 찾고 있어 항상 0건 복사되던 버그 — `basicCodePre`(원본 코드) 기준으로 수정
  - 매퍼에서 리터럴 NEXTVAL을 쓰는 시퀀스 4개(basicbrodfile_seq 등)가 DB에 없는 것을 추가
    발견, 08_create_missing_sequences.sql에 추가만 함(코드 변경 불필요, 이제 총 13개)
  - BASIC_CODE 채번은 없는 DB 함수 FN_BASICCODE() 대신 `generateBasicCode()`(MAX+1,
    접두사 포맷이라 정책상 대상 아님) 신규 추가
- 프론트: 기초 방송 템플릿 목록+매장배포 페이지, 방송(음원) 콘텐츠 목록+등록/수정 폼 페이지
  신규 구축. 시간대 그룹 상세 편집 UI는 콘텐츠 편성 화면과 동일하게 복잡도가 높아 이번
  범위에서는 프론트 제외(백엔드 API는 준비됨)

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [ ] Performance concerns
- [ ] Test coverage (시퀀스 스크립트 미실행 + 프로젝트 전체 컴파일 잔여 이슈로 실기동 테스트 불가)

### 이슈 / 개선사항
- `./gradlew compileJava` 기준 이번에 작성/수정한 파일은 전부 에러 없음. 남은 컴파일 에러
  52개는 이번 세션에서 건드리지 않은 다른 도메인(ScheduleInfoManageController,
  FileUpladController)의 기존 문제
- 프론트 `npm run build` 성공
- **`08_create_missing_sequences.sql`(총 13개) 아직 DB 미실행** — sts/brd/web 전체를
  포함해 지금까지 손댄 모든 시퀀스 기반 등록 기능이 실행 전까지 런타임에 실패함 (최우선 확인 필요)
- sts/brd/web 6개 컨트롤러 전체 완료 (BrodAnniversaryManagerController,
  BrodContentDetailManageController, BrodScheduleManagerController,
  BrodContentInfoManageController, BasicBrodManageController + BrodOrganizationManagerService
  경유 기존 완료분)
- 후속 필요: 엑셀 다운로드 2건(BrodPlayExcelView 등) 재구현, 편성표 생성/엑셀 클러스터
  (UniSelectInfoManageService 대체 설계), 시간대 그룹 상세 편집 프론트

## 06:10 — 사용자가 생성한 DB 함수 20개+프로시저 3개 확인 및 애플리케이션 레벨 우회 코드 일괄 반영
**파일:** `e:\dev\front\did_emart\DB_FUNCTIONS_VERIFICATION.md`(신규),
`backend/.../sts/brd/{service/BasicBrodInfoManageService.java, mapper/BasicBrodManagerMapper.xml, web/BasicBrodManageController.java, mapper/BrodContentInfoManagerMapper.xml}`,
`backend/.../sts/cnt/{mapper/ContentInfoManagerMapper.xml, mapper/ContentMutiInfoManagerMapper.xml, mapper/ContentMessageInfoManagerMapper.java+.xml, service/ContentMessageInfoManageService.java}`,
`backend/.../bas/cnt/{service/CenterInfoManageService.java, mapper/CenterInfoManagerMapper.java+.xml, web/CenterInfoManageController.java, service/CenterAnniManagerService.java, web/CenterAnniManagerController.java}`,
`backend/.../sts/mhs/{service/MhsMonitorInfoManageService.java, mapper/MhsMonitorManageMapper.java+.xml, web/CultureDisInfoManageController.java}`,
`backend/.../use/{service/GroupManagerService.java, mapper/GroupManagerMapper.java+.xml}`

### 변경 내용
- 사용자가 pgAdmin 등에서 스크린샷으로 DB 함수 20개(`fn_basiccode` 등) + 프로시저 3개
  (`sp_basicupdate`/`sp_didsttus`/`sp_tableinsert`)를 직접 생성했다고 알려옴. 라이브 DB에서
  `pg_get_functiondef()`로 전체 목록과 실제 본문(동작)까지 하나하나 확인 후 새 문서
  `DB_FUNCTIONS_VERIFICATION.md`에 정리
- 어제~오늘 여러 도메인에서 "DB 함수가 없어서 애플리케이션 레벨(MAX+1)로 대체"했던 부분들이
  이 함수들과 다수 겹쳐서, 사용자 확인("지금 바로 전체 일괄 반영") 후 실제 함수 호출로 되돌림:
  기초방송(BASIC_CODE→FN_BASICCODE), 매장ID(CENTER_ID→FN_CENTERID), MHS모니터코드
  (→FN_MHSMONITERID), 부서ID(GROUP_ID→FN_GROUPCODE, 대상 테이블 LETTNAUTHORGROUPINFO 일치
  확인), DID발송 SEND_DIDID(→FN_DIDMESSAGEINFO), 콘텐츠 코드명 서브쿼리 다수
  (→FN_DETAILCODENM/FN_DETAILCODEDOC)
- 매장 방송시간대 조회(`selectCenterTimeInfo`)는 서비스/매퍼엔 이미 FN_CENTERBRODINFO가
  연결돼 있었으나(9절에서 함수 없음 확인하고 엔드포인트만 안 만들었었음) 이번에
  `GET /{centerId}/timeInfo.do`로 신규 노출
- **의도적으로 반영하지 않은 것**: (1) `fn_centerannicode()`는 실제 정의를 열어보니
  매개변수 대신 특정 매장코드가 하드코딩된 버그가 있어 그대로 쓰면 다른 매장 기념일 코드가
  잘못 계산됨 → 기존 방식 유지, (2) `fn_brodcodesp()`는 일반 채번과 다른 별도 범위 규칙으로
  보여 용도 불명확이라 보류, (3) `fn_seqmax()`는 단일테이블 MAX만 지원(MAX+1 아님)이라
  두 테이블 겸용 미리보기 쿼리에는 부적합해서 기존 로직 유지
- **17절에서 제외했던 "편성표 생성/조회/엑셀" 클러스터는 복원하지 못함** — 해당 컨트롤러
  전면 재작성(덮어쓰기) 시 원본 ~410줄이 이미 사라졌고, git 저장소가 아니라 히스토리 복구도
  불가능함을 확인. 사용자에게 물어본 결과 "레거시 소스/화면을 나중에 제공하면 그거 보고
  포팅"하기로 결정 — 보류 상태로 남김
- 함수명이 겹치지 않는 나머지 도메인(`sym/sch`, `sym/did`, `bas/role`)은 이번 마이그레이션
  범위 밖(아직 리팩토링 전)이라 손대지 않음

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities (fn_uniresult/fn_seqmax의 동적 SQL 위험성을 문서화하고, 실제
  반영 시에는 사용 안 함 — 리터럴 인자만 넘기는 경우에 한해 안전하다는 점을 명시)
- [ ] Performance concerns
- [ ] Test coverage (시퀀스 스크립트 미실행 + 프로젝트 전체 컴파일 잔여 이슈로 실기동 테스트 불가)

### 이슈 / 개선사항
- `./gradlew compileJava` — 오늘 수정한 파일 전부 에러 없음(개별 grep으로 확인). 남은 52개
  에러는 이번 세션 미착수 파일(ScheduleInfoManageController, FileUpladController)의 기존 문제
- 프론트 `npm run build` 성공
- `fn_centerannicode()`의 하드코딩 버그는 DB측 수정이 필요함 — 사용자에게 별도 전달 필요
- 편성표 생성 클러스터는 사용자가 레거시 자료를 줄 때까지 보류

## 06:40 — 08_create_missing_sequences.sql 실행 확인
**파일:** `db-encoding-fix/08_create_missing_sequences.sql`(실행 확인 주석 추가)

### 변경 내용
- 사용자가 13개 시퀀스 스크립트 실행을 요청, 실행 전 안전 확인용으로 라이브 DB 재조회
  (`pg_sequences`)한 결과 **13개 시퀀스가 이미 전부 생성되어 있었음**(제가 실행한 적은
  없음 — 다른 경로로 이미 반영됨)을 확인
- START_VALUE가 스크립트에 적힌 값과 정확히 일치, 각 테이블 현재 MAX값+1과도 어긋남 없이
  일치(스크립트 작성 이후 해당 테이블에 새 데이터가 쌓이지 않음). 전부 `is_called=false`
  상태라 다음 INSERT부터 정상적으로 지정된 값부터 채번됨
- 스크립트 상단에 실행 확인 주석 추가, `LOGIN_MANAGER_FEATURE.md` 17절 검증 항목 갱신

### 리뷰 체크리스트 결과
- [x] Logic errors & edge cases (MAX값 어긋남 여부 재확인 후 안전 확인)
- [ ] Test coverage

### 이슈 / 개선사항
- 그동안 반복적으로 "최우선 확인 필요"로 보고했던 시퀀스 미실행 이슈가 해소됨 — 지금까지
  작업한 모든 시퀀스 기반 등록 기능(콘텐츠/방송기념일/문화센터/DID발송/방송편성/기초방송 등)이
  정상 동작 가능한 상태

## 07:30 — 프로젝트 전체 컴파일 에러 0개 달성
**파일:** `build.gradle`, `sym/dbd/web/DashBoardManagerController.java`(전면 재작성),
`util/web/FileUpladController.java`(삭제), `sts/brd/web/BasicBrodManageController.java`,
`use/mapper/GroupInfoManagerMapper.java+.xml`, `bas/code/service/EgovCcmCmmnDetailCodeManageService.java`,
`sym/did/web/DidInfoManageController.java`(전면 재작성), VO 클래스 29개(`@Builder` 제거)

### 변경 내용
- 사용자가 "남은 52개는 이 2개 파일만 고치면 되냐"고 물어서 재확인하니, 실제로는 4개 파일
  (`UtilInfoService`/`DashBoardManagerController`/`ScheduleInfoManageController`/
  `FileUpladController`)에서 에러가 나고 있었음 — 우선 이 4개부터 처리
  - `UtilInfoService`: Apache HttpClient5(`org.apache.hc.*`) 의존성이 `build.gradle`에 아예
    없었음. 문제 메서드 3개는 아무 데서도 안 쓰이지만, 파일 자체는 11곳에서 참조되는(NVL 등
    정적 유틸) 살아있는 파일이라 삭제 대신 `httpclient5:5.3.1` 의존성 추가
  - `DashBoardManagerController`: 존재하지 않는 `egovframework.let.sym.dbd.service.*` import.
    실제 서비스는 `com.common.backoffice.sym.dbd.servie`(오타 패키지명 그대로)에 정상
    포팅되어 있어서 REST로 재작성. 주석 안 `*/`가 Javadoc을 조기 종료시키는 버그도 발견/수정
  - `FileUpladController`: 코드베이스 어디서도 호출 안 되는 죽은 파일(mp3 태그 추출) —
    같은 기능은 이미 `fileMultiService`로 대체 완료된 걸 확인하고 사용자 승인 받아 삭제
- 이 작업 중 `build.gradle`에 의존성을 추가하면서 Gradle 증분 컴파일 캐시가 무효화되어
  **전체 클린 빌드**가 처음으로 돌았고, 그동안 "52개"라고 보고해온 것과 달리 **실제로는
  121개 에러**가 있었다는 게 드러남(증분 컴파일이 다른 파일들을 재검사하지 않아 숨겨져
  있었음) — 사용자에게 즉시 정정 보고
- 사용자 확인("builder() 구조적 문제부터 먼저 고치기") 후 순차 해결:
  - **29개 VO 클래스 공통 버그**: `XxxVO extends Xxx` 패턴에서 부모/자식 둘 다 `@Builder`를
    써서 정적 `builder()` 메서드 반환 타입이 안 맞아 컴파일 거부(JLS 8.4.8.3). 코드베이스
    전체에서 `.builder()` 호출이 단 한 곳도 없는 걸 먼저 확인한 뒤 자식 VO의 `@Builder`만
    일괄 제거
  - `BasicBrodManageController`(오늘 작성분) 자체 버그 2건: 존재하지 않는 `setUserId`/
    `formatFileAlbumRegDate`/`temporaryEncode` 호출 수정
  - `GroupInfoManageService.selectGroupInfoManageCombo` 타입 불일치(매퍼는 Map 기대, 서비스는
    GroupInfoVO 전달) + 매퍼의 `params.roleId`(채워주는 코드가 없어 항상 미평가되던 죽은
    키) 버그 수정
  - `EgovCcmCmmnDetailCodeManageService`에 JPA repository 필드 선언 자체가 빠져 있던 버그 수정
  - **`DidInfoManageController`(1193줄) 전면 재작성**: 클래스는 `@RestController`인데 본문은
    세션/ModelMap/JSP뷰 반환이 뒤섞인 반쯤 변환 상태(반환타입 ResultVO인데 JSP 경로 문자열을
    리턴하는 등 애초에 컴파일 불가). `LOGGER`(미선언, `@Slf4j`는 `log`를 생성) 및 4개 모델
    import 누락도 원인. 서비스 메서드 시그니처 전부 일치 확인 후 JWT/ResultVO로 재작성,
    구식 LoginVO 필드(authorCode/mberId/groupId) → 신규 필드(roleId/managerId/partId) 매핑.
    죽은 엔드포인트 2개(`test.do`, 로직이 전부 주석 처리된 `integrate.do`) 제외

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [ ] Performance concerns
- [ ] Test coverage (컴파일은 통과했으나 실서버 기동 테스트는 아직 안 함)

### 이슈 / 개선사항
- ✅ **`./gradlew clean compileJava` 기준 프로젝트 전체 에러 0개** — 이 마이그레이션 프로젝트
  시작 이래 최초로 전체가 정상 컴파일됨
- ✅ 프론트 `npm run build` 성공
- 다음 단계로 실서버 기동 + 로그인/CRUD 실제 동작 확인이 가능해짐(그동안 컴파일 에러 때문에
  전혀 시도할 수 없었던 것) — 사용자 확인 후 진행 권장
- `08_create_missing_sequences.sql`은 이미 실행 확인됨(위 06:40 항목)

## 08:56 — 실서버 최초 기동 성공 + 런타임 설정 버그 다수 수정
**파일:** `application.yml`, `build.gradle`, `bas/cnt/web/CenterInfoExcelMapping.java`,
`egovframework/com/config/EgovConfigAppDatasource.java`,
`mapper/config/postgresql-config.xml`, `egovframework/com/security/WebMvcConfig.java`,
`bas/code/web/{EgovCcmCmmnDetailCodeManageController, EgovCcmCmmnCodeManageController,
EgovCcmCmmnClCodeManageController}.java`, `sym/grp/web/GroupInfoManageController.java`,
`bas/role/web/RoleInfoManageController.java`, `bas/menu/web/MenuInfoManageController.java`,
`msg/rabbitmq/**`(삭제), `util/service/ImageVectorService.java`(삭제)

### 변경 내용
- "실서버 기동 + 로그인/CRUD 확인" 요청으로 `bootRun` 시도 → 매번 배너만 찍고 예외 없이
  종료됨. 원인 추적 결과 **로그 설정 자체가 깨져서 실제 예외가 콘솔에 전혀 안 보이고
  있었음**(`logging.config`가 Log4j2 형식 XML을 가리키는데 실제 프레임워크는 Logback이라
  `<Appenders>`/`<Loggers>` 태그를 못 알아듣고 무시 → 콘솔 appender 자체가 안 붙음). 이 설정을
  제거해서 Spring Boot 기본 Logback으로 되돌리자 그제서야 실제 예외가 보이기 시작함
- 이후 컨텍스트 기동 실패 원인을 하나씩 해결(자세한 내용은
  `LOGIN_MANAGER_FEATURE.md` 20절):
  1. `factoryBeanObjectType` IllegalArgumentException — 바이트코드 디컴파일로 확인한 결과
     `mybatis-spring:2.1.1`(egovframe.rte.psl.dataaccess가 전이 의존성으로 끌고 옴)이 Spring
     6.1.x에서 깨지는 구버전 동작을 하고 있었음. `org.egovframe.rte.*`를 Spring Boot 3.x
     정식 지원 버전인 4.3.0으로 올리고, `mybatis-spring:3.0.3`을 명시 추가해서 해결
     (egovframe 4.3.0 API 변경 1건 — `EgovExcelMapping.mappingColumn()` throws 시그니처 대응)
  2. `Globals.kakao.Url` 미해결 — 카카오 알림톡 전용 MySQL DataSource 빈인데 관련 매퍼도
     없는 죽은 기능임을 확인, 사용자 승인 후 제거
  3. MyBatis 전역 typeAlias 설정 오타 2건(`ContentMutilInfo`, `XmlInfo`/`XmlInfoVO`가 잘못된
     클래스를 가리킴) — 기동 시점에 전체를 즉시 로딩하는 구조라 하나만 틀려도 전체 기동 불가
  4. `spring.data.redis.host` 등 Redis 연결 정보가 아예 없었음 — 사용자 확인 후
     `localhost:6379`로 추가(공통코드 캐시/ShedLock용, 실제 서버 없으면 해당 기능만 런타임
     실패, 기동 자체는 Lettuce 지연연결이라 문제없음)
  5. `webinfPath.url` — 예전에 CenterInfoManageController에서 발견했던 것과 동일한
     패턴(`Common.filePath`가 맞음)
  6. `Globals.pageUnit`/`Globals.pageSize` — 실제 키는 `Globals.addedOptions.pageUnit/pageSize`
     (중첩)인데 4개 컨트롤러가 중첩 없이 참조 — 9절에서 EgovConfigAppProperties 하나만
     고치고 개별 컨트롤러 반영이 누락됐던 것
  7. RabbitMQ(공통코드/메뉴/권한 변경 캐시 무효화 방송용)와 potrace(메뉴 아이콘 PNG→SVG
     변환용) — 둘 다 실제 호출부가 전부 주석 처리됐거나 전용 엔드포인트뿐이라, 카카오와
     같은 패턴으로 사용자 확인 후 관련 클래스/의존성/엔드포인트 전부 제거

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [ ] Performance concerns
- [x] Test coverage (실서버 기동 + HTTP 요청/응답 확인 완료)

### 이슈 / 개선사항
- ✅ **`Started BackendApplication in 21.6초`로 이 프로젝트 최초 실서버 기동 성공**(포트 7004)
- ✅ Swagger UI/API docs 200 응답, PostgreSQL 연결(HikariPool)/JPA 정상 초기화 확인
- ✅ `/api/loginJwt.do` 정상 응답(로그인 자체는 실패 — 비밀번호가 아직 평문이라 해시 비교
  불일치는 **의도된 상태**. 사용자가 "패스워드 암호화는 맨 마지막에 적용 예정"이라고 지시)
- ✅ 인증 필요 엔드포인트가 미인증 요청에 정상 403 응답(크래시 아님) 확인 — JWT
  필터/AuthHelper/ResultVO 응답 체계가 실제로 작동함을 확인
- ⏸️ 실제 로그인 성공 + 데이터 CRUD 수동 확인은 `07_rehash_manager_password.sql` 실행
  (사용자가 마지막 단계로 미루기로 함) 이후 가능
- 로그 설정(log4j2 vs Logback) 근본 정리는 후속 과제로 남김(현재는 Logback 기본 설정으로
  동작은 하나, log4jdbc의 SQL 노이즈 필터링은 미적용 상태)
