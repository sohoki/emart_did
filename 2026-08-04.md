# 2026-08-04 작업 로그

## 다크모드에서 select/input이 OS 설정 따라 제멋대로 어둡게 보이던 문제 수정
**파일:** `front/src/index.css`, `front/src/style/DarkMode.css`

### 원인
`index.css`의 `:root { color-scheme: light dark; }`(Vite 기본 템플릿 잔재) 때문에, `className`
없는 순수 `<select>`/`<input>`(예: 방금 이식한 `ProgrameInfo.jsx`의 검색조건 select/input)이
앱 자체의 다크모드 토글(`[data-theme]`)과 무관하게 **브라우저/OS의 다크모드 설정을 보고**
네이티브 위젯을 어둡게 렌더링하고 있었음.

### 수정
- `index.css`: `color-scheme: light dark` → `color-scheme: light`(기본 라이트로 고정)
- `DarkMode.css`: `[data-theme="dark"] { color-scheme: dark; ... }` 추가 — 앱의 다크모드 토글이
  켜졌을 때만 네이티브 폼 컨트롤도 같이 어두워지도록 함

## 프로그램 등록 모달 제목이 회색으로 흐릿하게 보이던 문제 수정
**파일:** `pages/backoffice/Basic/components/{DetailCodeFormModal,MenuSelectForm,RoleFormModal,ProgramFormModal}.jsx`

### 원인
다크모드가 켜진 상태에서, 이 4개 모달이 `.modal-dialog`의 배경색을 `backgroundColor: '#fff'`로
하드코딩하고 있었음. `.modal-title__title`의 다크모드 글자색(`#e9ecef`, 어두운 배경 위에서
읽히도록 설계됨)은 정상 적용되는데 배경은 하드코딩된 흰색이라 "흰 배경 위 밝은 회색 글씨"로
흐릿하게 보였던 것. `CodeFormModal.jsx` 하나만 이미 `var(--bs-body-bg, #fff)`(테마에 따라
자동 전환)로 고쳐져 있었음 — 나머지 4개도 동일 패턴으로 통일.

## 프론트엔드 리스트 화면 기본 골격을 CLAUDE.md에 문서화 + ManagerListPage 적용
**파일:** `CLAUDE.md`(11절 신규), `pages/backoffice/HrInfo/ManagerListPage.jsx`,
`pages/backoffice/HrInfo/components/ManagerAdminFormModal.jsx`(신규)

### 내용
사용자가 `content-header`/`content-search`/`content-table` 3단 구성(이미 `RoleInfo.jsx`/
`ProgrameInfo.jsx`에 구현돼 있던 패턴)을 앞으로 만들 모든 리스트 화면의 기본 골격으로
삼아달라고 요청 → CLAUDE.md 11절에 문서화하고, `ManagerListPage.jsx`부터 이 골격 +
`useGridInfinite`(무한스크롤 그리드 훅)로 재작성함. 기존엔 관리자 등록/수정 기능이 아예
없었는데(리스트+로그아웃 버튼만 존재), 이번에 "관리자 등록" 버튼과 등록/수정 모달
(`ManagerAdminFormModal.jsx`, 아이디 중복체크 + 부서/권한 선택 + 상태/사용유무 포함)을
신규로 붙임. 이 모달은 헤더 프로필 드롭다운의 "내 정보 수정"(`ManagerFormModal.jsx`,
본인 수정 전용, mode 항상 'Edt')과는 별개 컴포넌트임.

### ⚠️ 확인 필요 — 부서 개념 두 갈래
관리자 등록 모달의 "부서" 드롭다운은 `GROUP_COMBO`(`LETTNAUTHORGROUPINFO`, 아래 부서관리
항목 참고)를 사용함. 그런데 `TB_MANAGERINFO.PART_ID`가 실제로 조인하는 테이블은
`TB_PARTINFO`이고, 이 테이블을 다루는 `hr/part` API는 did_emart에 아직 컨트롤러 자체가
없음(`PART_LIST`/`PART_COMBO` 등 URL 상수만 있고 구현 없음). 즉 "부서"라는 이름의 테이블이
`TB_PARTINFO`(관리자가 실제로 속한 신규 테이블)와 `LETTNAUTHORGROUPINFO`(구 레거시 테이블,
`EMART_`+13자리 ID 포맷이 `TB_PARTINFO`의 기존 데이터와 일치하는 것으로 보아 마이그레이션
출처로 추정)로 나뉘어 있어, 지금은 임시로 후자를 재사용했음. 두 테이블의 관계를 확인해서
`hr/part` API를 정식으로 만들지, `LETTNAUTHORGROUPINFO` 그대로 쓸지 결정 필요.

## 부서 관리(LETTNAUTHORGROUPINFO) 화면 신규 이식
**파일:** `use/web/GroupManagerController.java`, `pages/backoffice/HrInfo/GroupListPage.jsx`(신규),
`pages/backoffice/HrInfo/components/GroupFormModal.jsx`(신규), `routes/index.jsx`, `constants/URL.jsx`

### 배경
사용자가 레거시 URL `/backoffice/sub/basicManage/selectGroupLst.do`("부서관리")를 제시하며
인사관리 메뉴에 추가해달라고 요청. 레거시 JSP 소스(`emart_cms3.2.1`)에서 이 URL이 "부서관리"
탭임을 확인. 대응하는 `use/web/GroupManagerController.java`가 이미 존재했으나 `javax.*`
패키지(Jakarta EE 이관 전)를 그대로 쓰고 있어 컴파일이 안 되는 상태였고, `@ModelAttribute`
세션 바인딩 방식이라 JWT 인증 체계와도 안 맞았음(Service/Mapper는 이미 정상 포팅되어 있었음).

### 변경 내용
- `GroupManagerController.java` — `jakarta.*`로 전환 + REST/JWT 패턴으로 재작성
  (`/api/backoffice/sub/basicManage/group` 아래 `POST /list.do`, `GET /combo.do`,
  `GET /{groupId}.do`, `POST /update.do`, `DELETE /{groupId}.do`)
- `GroupListPage.jsx`(신규) — 부서 목록(계층형) + 등록/수정/삭제. 이 API는 `pageIndex`/
  `pageUnit`을 실제로 반영하지 않고 항상 전체 목록을 반환하므로, `useGridInfinite`(서버
  offset 페이징)가 아니라 클라이언트 사이드 그리드로 처리함(11절 골격의 예외 케이스)
- `URL.jsx`에 `GROUP_LIST`/`GROUP_COMBO`/`GROUP_INFO`/`GROUP_UPDATE` 추가
- `routes/index.jsx` — 인사 관리 그룹에 `/backoffice/sub/basicManage/group` 라우트 추가

### 검증
- `./gradlew compileJava`, `npm run build` 모두 정상 확인

### 이슈 / 개선사항
- 위 "부서 개념 두 갈래" 항목 참고 — `TB_PARTINFO` vs `LETTNAUTHORGROUPINFO` 관계 확인 필요
- `GroupManagerMapper.xml`의 목록 조회 쿼리가 페이징 파라미터를 안 쓰는 것도 후속 정리 대상
- 서버 재시작 필요(`GroupManagerController` 신규 라우트 반영)

## 부서 관리 화면 페이지 네비게이션 제거
**파일:** `pages/backoffice/HrInfo/GroupListPage.jsx`

### 내용
부서 목록이 계층형(트리) 구조라 페이지를 나눠 보여주면 상하위 관계가 끊겨 보기 불편함 →
사용자 요청으로 `AppAgGrid`의 `pagination`/`paginationPageSize`를 제거하고 전체 목록을
한 번에 표시하도록 변경.

## 매장(센터) 관리 화면 기본 골격 적용 + 등록/수정 모달 신규 작성
**파일:** `pages/backoffice/BasicManage/CenterListPage.jsx`,
`pages/backoffice/BasicManage/components/CenterFormModal.jsx`(신규), `style/Modal.css`

### 내용
- `CenterListPage.jsx`를 11절 골격(`content-header`/`content-search`/`content-table`) +
  `useGridInfinite`로 재작성
- 레거시 `centerDetail.jsp`를 참고해서 매장 등록/수정 모달(`CenterFormModal.jsx`) 신규 작성
  (지점명/관리부서/영업시작·종료/지점구분/우편번호/주소/기본음원(음원방송 매장만)/
  지점이미지/사용유무)
- 영업 시작·종료를 `<input type="time">` 데이트피커로 변경, 시작 시간이 종료 시간보다
  같거나 늦으면 등록/수정 전 경고창 표시하도록 검증 추가
- "중복확인" 버튼이 모달 푸터 버튼보다 과도하게 크게 보이던 문제 — `common.css`의
  `.modal-footer button` 축소 규칙이 `modal-body` 안의 버튼(`input-group` 안 중복확인 등)에는
  적용 안 되고 있었음. `common.css`는 수정 금지 대상이라 `Modal.css`에
  `.modal-body .input-group > button` 규칙을 동일 크기로 추가해서 해결

### 미해결 이슈
- `POST /api/backoffice/sub/basicManage/cnt/list.do`가 curl 테스트 시 500 에러 반환 —
  원인 미파악(매퍼 SQL 자체는 육안상 문제 없어 보임), 서버 콘솔 스택트레이스 확인 필요

## 매장 기념일 관리를 모달 방식으로 전환
**파일:** `pages/backoffice/BasicManage/components/CenterAnniModal.jsx`(신규),
`pages/backoffice/BasicManage/CenterListPage.jsx`

### 배경
레거시 `centerDetail.jsp`의 "기념일" 버튼(`center_Anniver()`)은 별도 페이지 이동이 아니라
`window.open()` 팝업으로 동작함. 최초에는 등록 폼만 모달(`CenterAnniFormModal.jsx`)로
분리하고 목록은 여전히 별도 라우트(`CenterAnniListPage.jsx`, `/backoffice/sub/basicManage/cnt/anni`)
로 남겨뒀었는데, 사용자가 매장 목록의 "기념일" 버튼을 직접 지목하며 그 버튼 자체가
페이지 이동 없이 목록+등록을 한 모달로 띄워야 한다고 재요청.

### 변경 내용
- `CenterAnniModal.jsx`(신규) — `CenterAnniListPage.jsx`의 목록 조회/삭제/등록 로직을
  그대로 옮겨 900px 모달 하나로 통합(목록 그리드 + "기념일 등록" 버튼 → 내부에 중첩된
  `CenterAnniFormModal` 재사용)
- `CenterListPage.jsx`의 "기념일" 버튼 `onClick`을 `navigate(...)` → `setAnniCenterId` +
  `setAnniModalOpen(true)`로 변경, `CenterAnniModal` 렌더 블록 추가, 더 이상 안 쓰는
  `useNavigate` import/변수 제거

### 검증
- `npm run build` 정상 확인(에러 없음)

### 이슈 / 개선사항
- 기존 라우트 `CenterAnniListPage.jsx`(`/backoffice/sub/basicManage/cnt/anni`)는 그대로
  남아있음 — 모달로 대체되었으니 라우트를 제거할지, 직접 접근용으로 유지할지 사용자 확인 필요
- 신규로 만든 모달들(`ManagerAdminFormModal`/`CenterFormModal`/`GroupFormModal`/
  `CenterAnniFormModal`)에 `<form>` 태그가 없어 Enter 키로 제출이 안 됨 — 표준화 여부 확인 필요

## DID 단말기 관리 화면 신규 생성
**파일:** `pages/backoffice/equiManage/DidInfoList.jsx`(신규),
`pages/backoffice/equiManage/components/DidFormModal.jsx`(신규), `constants/URL.jsx`, `routes/index.jsx`

### 배경
레거시 `didList.jsp`/`didDetail.jsp`(`emart_cms3.2.1`)를 참고해서 "단말기 관리" 리스트+
등록/수정 모달 신규 작성. 백엔드(`DidInfoManageController`, `/api/backoffice/sub/equiManage/did`)는
이미 JWT/REST로 전면 재작성되어 있어 프론트만 새로 붙이면 되는 상태였음(`list.do`/`formData.do`/
`update.do`/`{didId}.do` DELETE 모두 기존 구현 재사용).

### 변경 내용
- `DidInfoList.jsx`(신규) — 11절 골격 + `useGridInfinite`. 검색조건(단말기명/단말기ID) +
  지점(centerId) 필터 + 목록(부서명/지점명/단말기명·ID/IP/DID스케줄/ON-OFF/OS구분/사용유무/삭제)
- 등록/수정 모달에 필요한 콤보(관리부서/설치지점/그룹정보/단말타입/모델타입/OS/IP타입/해상도)는
  `formData.do?mode=Ins`를 페이지 진입 시 **한 번만** 호출해서 모달에 내려줌(이전 세션에
  ManagerListPage에서 확립한 "콤보는 부모에서 한 번만 조회" 패턴 재사용)
- `DidFormModal.jsx`(신규) — 단말명/단말ID(자동할당, readonly)/단말MAC·IP(자동등록, readonly)/
  관리부서/설치지점/그룹정보(선택, 단말타입이 "음원방송"(`DIDRMODELTYPE03`)이 아니면 필수 —
  레거시 `check_form()` 검증 규칙 그대로 유지)/단말모델타입/단말타입/단말OS/IP타입/해상도/
  가로·세로/운영시작·종료(`<input type="time">`)/사용유무
- 레거시에서 항상 숨김 처리되어 고정값(`Mointer01`/`SERIAL_N`/`DIDSW02`)으로만 저장되던
  시리얼포트/모니터개수/Agent버전 등은 모달에 노출하지 않고, 제출 시 동일 고정값을 자동으로
  채워 보냄(레거시 `check_form()`의 고정 대입과 동일)
- `URL.jsx`에 `DID_LIST`/`DID_INFO`/`DID_UPDATE`/`DID_FORM_DATA` 추가
- `routes/index.jsx` — 장비 관리 그룹에 `/backoffice/sub/equiManage/did` 라우트 추가

### 검증
- `npm run build` 정상 확인(컴파일 에러 없음)

### 이슈 / 개선사항
- 레거시 화면에 있던 "콘텐츠재전송"/"재부팅" 다건 명령 발송 버튼(`restart.do` 등)은 이번
  범위에 포함하지 않음(요청 범위가 "리스트+모달"이라 우선 제외, 그리드 다중선택 UI 필요 —
  후속 요청 시 추가)
- 사이드바 메뉴는 DB(`TB_MENU`)로 관리되는 것으로 보여 별도 정적 메뉴 설정 파일 수정은
  하지 않음 — 메뉴에 노출되려면 관리자 화면(`기초관리 > 메뉴관리`)에서 별도 등록 필요할 수 있음

## DID 단말기 그룹 관리 화면 신규 생성(마스터-서브그리드)
**파일:** `pages/backoffice/equiManage/DIdGroupInfo.jsx`(사용자가 스캐폴딩한 빈 파일을 완성),
`pages/backoffice/equiManage/components/DidGroupFormModal.jsx`(신규),
`pages/backoffice/equiManage/components/DidGroupDetailCellRenderer.jsx`(신규),
`constants/URL.jsx`, `routes/index.jsx`

### 배경
레거시 `did_groupList.jsp`를 참고. `TB_GROUP`(단말기 그룹)은 이전에 다룬 관리부서 개념인
`LETTNAUTHORGROUPINFO`와는 완전히 별개 테이블 — 혼동 주의. 백엔드(`GroupInfoManageController`,
`/api/backoffice/sub/equiManage/group`)는 사용자가 이미 준비해둔 상태였고, 그룹 CRUD +
그룹-단말기 매핑(`TB_GROUPDID`) 엔드포인트가 모두 존재해서 프론트만 신규 작성.
사용자가 미리 만들어둔 `DIdGroupInfo.jsx` 스캐폴드가 `CodeInfo.jsx`(분류코드+상세코드
마스터-서브그리드 화면)를 그대로 복사한 상태여서, 그 패턴과 이미 있는 `MasterDetailGrid.jsx`
공용 컴포넌트를 그대로 재사용함.

### 변경 내용
- `DIdGroupInfo.jsx` — 마스터: 그룹 목록(그룹코드/그룹명/단말기수/사용유무 + 수정/삭제) +
  검색(그룹코드 `GROUP_ID`/그룹명 `GROUP_NM`) + "그룹 등록". 서브그리드는
  `MasterDetailGrid`(`ag-grid-enterprise`의 masterDetail, `CodeInfo.jsx`가 이미 쓰던 방식)로 연결
- `DidGroupFormModal.jsx`(신규) — 그룹코드(Ins 시 입력+중복확인, Edt 시 readonly)/그룹명만
  입력받음. **사용유무 필드는 의도적으로 뺌** — 레거시 등록 폼에도 없었고, 백엔드
  `updateGroupInfoManage` SQL이 `GROUP_NM`만 갱신하고 `GROUP_USEYN`은 애초에 반영하지 않아
  (`insertGroupInfoManage`는 항상 `'Y'` 고정) 넣어봤자 동작하지 않는 컨트롤이 되기 때문
- `DidGroupDetailCellRenderer.jsx`(신규) — 요청하신 대로 **등록/삭제만** 제공(단말 자체 수정
  없음). `GET /didgroupLst/{groupCode}.do`로 그룹 내 단말 목록, `GET /didcomboLst.do`로
  "어느 그룹에도 속하지 않은" 단말 콤보를 가져와 등록용 select에 채움(레거시와 동일하게
  단말명이 "음원방송"으로 시작하는 항목은 콤보에서 제외). 삭제는
  `DELETE /didgroupDel/{groupCode}.do?didId=xxx`가 갱신된 목록을 바로 응답으로 돌려주므로
  별도 재조회 없이 그 응답으로 그리드를 갱신
- `URL.jsx`에 `DID_GROUP_LIST`/`DID_GROUP_ID_CHECK`/`DID_GROUP_UPDATE`/`DID_GROUP_DELETE`/
  `DID_GROUP_MEMBER_LIST`/`DID_GROUP_MEMBER_DELETE`/`DID_GROUP_MEMBER_COMBO`/
  `DID_GROUP_MEMBER_INSERT` 추가
- `routes/index.jsx` — 장비 관리 그룹에 `/backoffice/sub/equiManage/did_group` 라우트 추가

### 검증
- `npm run build` 정상 확인(컴파일 에러 없음)

### 이슈 / 개선사항
- 마스터 목록의 "단말기수"(`didCnt`) — 매퍼(`GroupInfoManagerMapper.xml`)가
  `(SELECT COUNT(*) ...) AS did_CNT`로 **따옴표 없이** 별칭을 줘서(이 프로젝트 다른 쿼리는
  전부 `AS "camelCase"`로 따옴표 처리) PostgreSQL이 소문자로 접어 `did_cnt`가 되고,
  MyBatis가 `mapUnderscoreToCamelCase` 없이는 이를 `didCnt`로 자동 매핑하지 못해 항상
  0/null로 내려올 가능성이 높음(프론트는 `p.value ?? 0`으로만 방어) — 백엔드 매퍼 수정은
  이번 범위 밖이라 손대지 않음, 실제 값이 0으로만 보이면 이 부분 확인 필요
  ⚠️ 사용자 확인 필요
- 그룹 삭제(`DELETE /del/{groupCode}.do`)는 해당 그룹에 속한 단말이 있어도 그대로 삭제되는
  것으로 보임(그룹-단말 매핑 정리 로직이 컨트롤러/매퍼에 안 보임) — 실제 삭제 시 고아
  매핑이 남을 수 있어 확인 필요

## CenterAnniModal 날짜 컬럼 포맷 적용 + AG Grid 날짜 포맷 공통 가이드 작성
**파일:** `lib/formatters.js`, `pages/backoffice/BasicManage/components/CenterAnniModal.jsx`,
`pages/backoffice/BasicManage/CenterAnniListPage.jsx`, `front/docs/grid-date-format-guide.md`(신규)

### 내용
시작일/종료일 컬럼이 백엔드 원본 형식(`YYYYMMDD`, 대시 없음) 그대로 그리드에 표시되던 문제.
찾아보니 이미 `lib/formatters.js`에 `fmtDate`/`fmtDateTime`(YYYYMMDD(HHMMSS) → 대시 포맷
변환)가 있었으나, AG Grid `valueFormatter`는 `(params) => string` 형태를 요구해서 그대로
꽂을 수 없었음 — `gridDateFormatter`/`gridDateTimeFormatter`(각각 `fmtDate`/`fmtDateTime`을
`params.value`에 적용하는 얇은 래퍼) 2개를 추가해서 컬럼 정의에 `valueFormatter:
gridDateFormatter`로 바로 대입할 수 있게 함. `CenterAnniModal.jsx`(요청 대상)와 동일 컬럼
구조를 가진 `CenterAnniListPage.jsx`(원본이 되는 페이지, 같은 문제 있었음) 둘 다 적용.
재사용 가이드는 `front/docs/grid-date-format-guide.md`에 작성 — 새 화면에서 `YYYYMMDD`류
필드를 그리드에 표시할 때 이 문서를 참고해서 같은 함수를 재사용하도록 안내.

### 검증
- `npm run build` 정상 확인(컴파일 에러 없음)

## DID 발송 메시지(자막) 현황 화면 신규 생성 + 그룹→단말 선택 전송 모달
**파일:** `pages/backoffice/equiManage/DidSendMessageList.jsx`(신규),
`pages/backoffice/equiManage/components/DidSendMessageModal.jsx`(신규),
`pages/backoffice/equiManage/components/DidSendMessageFormModal.jsx`(신규), `routes/index.jsx`

### 배경
레거시 `didSendMessagelst.jsp`(현황 리스트) + `didSendMessage.jsp`(그룹→단말 선택 후 등록
팝업)를 참고. 백엔드(`ContentMessageInfoManageController`,
`/api/backoffice/sub/equiManage/message`)는 이미 존재했고, 그룹/단말 선택은 컨트롤러
주석에 "GroupInfoManageService 계약이 불명확해 의도적으로 제외, 기존 DID 관리 화면 재사용
권장"이라 적혀 있었음 — 이번 세션에 이미 만든 `DID_GROUP_LIST`/`DID_GROUP_MEMBER_LIST`
(단말기 그룹 관리 화면에서 쓴 것과 동일)를 그대로 재사용해서 그 공백을 메움.

기존에 `pages/backoffice/ConManage/ContentMessageListPage.jsx`(스캐폴딩 단계에서 만든
러프한 버전 — 골격 미적용, DID ID를 콤마로 직접 입력하는 방식)가 같은 API를 쓰고 있었음.
이번에 만든 `DidSendMessageList.jsx`가 그 기능을 골격 패턴 + 레거시와 동일한 그룹→단말
선택 UX로 제대로 재구현한 것이라, 라우트는 겹치지 않게 `/backoffice/sub/equiManage/message_list`로
새로 등록했고 기존 `ContentMessageListPage`/`/message` 라우트는 그대로 남겨둠(삭제 여부는
사용자 확인 필요).

### 변경 내용
- `DidSendMessageList.jsx` — 골격 패턴 + `useGridInfinite`. 목록(그룹명/단말기명/메시지/
  시작일·종료일(날짜 포맷 가이드의 `gridDateFormatter` 적용)/시작·종료시간/단말기
  전송여부(`sendDidCheckDate`가 null이면 "미전송", 있으면 `gridDateTimeFormatter`)/삭제.
  검색조건은 백엔드 매퍼가 실제 지원하는 두 갈래(`SEND_MESSAGE`=메시지 내용,
  그 외=그룹명)만 노출 — 레거시 JSP엔 "단말기명" 옵션도 있었지만 백엔드 쿼리가 지원하지
  않아 제외
- `DidSendMessageModal.jsx`(신규) — "메시지 등록" 버튼으로 열리는 그룹/단말 선택 모달.
  좌측 그룹 목록(`DID_GROUP_LIST`, 최대 100개 한 번에 로드 — 검색/페이징 없이 단순 클릭
  선택) / 우측 선택 그룹의 단말 목록(`DID_GROUP_MEMBER_LIST`) + 체크박스(전체선택 포함).
  체크 후 "메시지 등록" 클릭 시 하단 `DidSendMessageFormModal`을 중첩으로 오픈(레거시는
  `window.open` 팝업 2단계였지만 이번 세션 내내 써온 "페이지 이동 없이 모달로" 원칙에 맞춰
  중첩 모달로 처리)
- `DidSendMessageFormModal.jsx`(신규) — 메시지 내용/시작·종료일(`<input type="date">`)/
  시작·종료시간(`<input type="time">`, 백엔드가 요구하는 `sendMessageStartHour/Min` 형태로
  분리해서 전송)/글자크기(선택, 프리텍스트 — 백엔드에 별도 콤보가 없어 자유입력으로 둠)/
  사용유무. 제출 시 `POST update.do`에 `mode:'Ins', groupCode, didIds:[...]`로 한 번에
  다건 단말에 동일 메시지를 등록(백엔드가 그렇게 설계됨 — 단말별로 반복 등록 안 함)
- `routes/index.jsx` — 장비 관리 그룹에 `/backoffice/sub/equiManage/message_list` 라우트 추가

### 검증
- `npm run build` 정상 확인(컴파일 에러 없음)

### 이슈 / 개선사항
- 기존 `ContentMessageListPage.jsx`(`/backoffice/sub/equiManage/message`)와 기능이 중복됨 —
  이번에 만든 화면으로 대체할지, 기존 라우트를 제거할지 사용자 확인 필요
  ⚠️ 사용자 확인 필요
- 좌측 그룹 목록은 페이지네이션/검색 없이 최대 100개까지만 로드 — 그룹이 100개를 넘는
  경우 일부가 안 보일 수 있음(단말기 그룹 관리 화면 자체는 정상 페이징 지원)

## 매장 기념일 기간 중복 체크 SQL 버그 수정
**파일:** `backend/src/main/resources/mapper/postgresql/bas/cnt/CenterAnniManagerMapper.xml`
(`selectCenterAnniRetgCheck`)

### 배경
사용자가 "CenterAnniModal에서 날짜가 중복으로 들어간 게 있다"고 보고 → 실제 테스트로 재현.

### 재현 (curl, 로컬 백엔드 7004포트)
1. `C26040801` 매장에 기념일 `20260810~20260820` 등록
2. 그 안에 완전히 포함되는 `20260812~20260815`로 `cntCheck.do` 호출 → `{"result":0}`(중복 없음으로 오판)
3. 반대로 그 구간을 완전히 감싸는 `20260805~20260825`로 호출해도 → `{"result":0}`
4. 실제로 해당 매장에는 이미 겹치는 두 기념일(`20260804~20260804`, `20260804~20260805`)이 등록돼
   있는 상태였음 — 사용자가 말한 "중복 데이터"의 실물 증거

### 원인
`selectCenterAnniRetgCheck` 쿼리가 "기존 행의 시작일 또는 종료일이 새 구간 안에 들어오는지"만
확인했음:
```sql
( CENTER_ANNISTARTDAY BETWEEN #{centerAnniStartDay} AND #{centerAnniEndDay} )
OR
( CENTER_ANNIENDDAY BETWEEN #{centerAnniStartDay} AND #{centerAnniEndDay} )
```
→ 새 구간이 기존 구간을 완전히 포함하거나, 반대로 기존 구간 안에 완전히 중첩되는 경우
(양쪽 끝점이 전부 새 구간 밖에 있는 경우)는 이 조건에 안 걸려서 중복으로 안 잡힘.

### 수정
표준 구간 겹침 조건(existing.start <= new.end AND existing.end >= new.start)으로 교체:
```sql
AND CENTER_ANNISTARTDAY <= #{centerAnniEndDay}
AND CENTER_ANNIENDDAY >= #{centerAnniStartDay}
```

### 검증
- curl로 버그 재현 확인(위 2, 3번 케이스 모두 잘못된 `result:0`) — 수정 후 재검증은 아직
  안 함(매퍼 XML은 핫리로드가 안 되어 재시작이 필요한데, 사용자가 본인 IDE에서 직접
  재시작해서 확인하겠다고 함)
- 테스트 중 만든 임시 기념일(`20260810~20260820`)은 삭제해서 원상복구함
- ⚠️ **사용자 확인 필요**: 백엔드 재시작 후 위 curl 재현 케이스로 재검증 필요. 또한 기존에
  이미 들어가 있던 겹치는 두 기념일(`C26040801` 매장의 `20260804~20260804` /
  `20260804~20260805`)은 실제 운영 데이터인지 테스트 잔여 데이터인지 확인 후 정리 필요

## DID 단말기 관리 OS구분 아이콘 + 그리드 행 높이 조정
**파일:** `pages/backoffice/equiManage/DidInfoList.jsx`

### 내용
레거시 `didList.jsp`의 OS구분 아이콘 분기(안드로이드→`android_icon.png`, 윈도우→
`windows_icon.png`, 그 외(iOS 등)→`ios_icon.png`)를 그대로 재현. 아이콘 실물 파일은
`public/img`가 아니라 `public/resource/img/{android,ios,windows}_icon.png`에 이미 있었음
(다른 정적 리소스도 전부 이 경로 아래 있는 프로젝트 관례) — `/resource/img/...` 경로로 참조.
또한 "단말기명" 컬럼이 이름 아래 작은 글씨로 단말ID를 2줄로 보여주는데 기본 행 높이(약
40px)에서 짤려 보이던 문제 — `AppAgGrid`에 `rowHeight={54}` 지정해서 해결.

### 부수 정리 — 깨진 라우트 수정
`npm run build` 도중 `CenterAnniListPage.jsx`(사용자가 직접 삭제한 것으로 보임 — 이전
로그에서 "CenterAnniModal로 대체되어 라우트 정리 필요"로 플래그했던 파일)를 `routes/index.jsx`가
여전히 참조하고 있어 빌드가 깨져 있는 것을 발견 → import/route 두 줄 제거해서 정리함.

### 검증
- `npm run build` 정상 확인(컴파일 에러 없음)

## 단말기 수정 모달이 안 뜨는 문제 — 매퍼 SQL 구문 오류 수정
**파일:** `backend/src/main/resources/mapper/postgresql/sym/did/DidInfoManagerMapper.xml`
(`selectDidrInfoManageDetail`)

### 배경
"DidFormModal 수정폼 안 뜸" 보고 → 프론트(`DidInfoList.jsx`의 `handleOpenDidModal`)는
`formData.do?mode=Edt&didId=...`의 `regist`가 없으면 조용히 아무 것도 안 하고 리턴하도록
되어 있어서(에러 alert 없음), 실제로는 백엔드가 500을 반환하고 있었는데도 사용자 입장에선
그냥 "버튼 눌러도 반응 없음"으로 보였음.

### 원인
`selectDidrInfoManageDetail` 쿼리 끝에 `WHERE A.DID_ID = #{didId};` 뒤에 이전 버전(상관
서브쿼리 방식)의 잔재로 보이는 `WHERE DID_ID = #{didId}`가 세미콜론 뒤에 그대로 남아 있어서
`... WHERE ...; WHERE ...` 형태의 구문 오류(SQL syntax error at or near "WHERE")가 발생 →
매번 500 에러.

### 검증 (다른 포트로 임시 인스턴스 띄워서 실제 재현/수정 확인)
사용자가 쓰던 7004포트 인스턴스는 건드리지 않고, 별도로 7099포트에 임시 백엔드를 띄워서:
1. 수정 전 코드로 `formData.do?mode=Edt&didId=C20102005001` 호출 → 500 + 콘솔에 정확한
   PSQLException(`syntax error at or near "WHERE"`) 확인
2. 잔재 `WHERE` 절 제거
3. 임시 인스턴스 재시작 후 동일 요청 재호출 → 200 SUCCESS 확인
4. 임시 인스턴스 종료(정리 완료)

### 이슈 / 개선사항
- ⚠️ **사용자 확인 필요**: 사용자가 실제 쓰는 7004포트 백엔드는 이 수정을 반영하려면 재시작
  필요(매퍼 XML은 핫리로드 안 됨)

## DID 단말기 등록/수정 모달 — 해상도 선택 시 가로/세로 자동 입력
**파일:** `pages/backoffice/equiManage/components/DidFormModal.jsx`

### 내용
레거시 `didDetail.jsp`의 해상도 선택(`DIDRES01~04`) 시 가로/세로 자동 대입 + "기타"만 직접
입력 가능하게 하던 로직을 재현. 코드값을 하드코딩하는 대신 콤보의 `codeNm`이
`가로*세로`(예: `1920*1080`) 패턴인지로 판별해서 매칭되면 가로/세로를 자동 채우고 두 입력을
readOnly로 바꿈("기타"처럼 그 패턴이 아닌 코드만 직접 입력 가능) — 코드값 자체에 의존하지
않아 EMT002 코드가 추가/변경돼도 그대로 동작함.

## DID 단말기 등록/수정 모달의 공통코드 콤보를 formData.do 번들 대신 개별 조회로 전환
**파일:** `pages/backoffice/equiManage/DidInfoList.jsx`

### 배경
사용자가 백엔드 `DidInfoManageController.selectDidrInfoManageDetail`(formData.do)의
`resultMap.put("selectType"/"selectResolution"/"selectIpType"/"selectModelType"/"selectOs", ...)`
(EMT001/002/003/004/011) 라인을 백엔드에서 직접 제거할 예정이라며, 프론트에서 이 콤보들을
`DidInfoList.jsx`에 직접 정의해달라고 요청.

### 변경 내용
- 단말타입(EMT001)/해상도(EMT002)/IP타입(EMT003)/모델타입(EMT004)/OS(EMT011) 5개 콤보를
  `formData.do` 응답에서 꺼내 쓰던 방식 → `useCommonCodeData('EMT00N')` 개별 훅 호출로 전환
  (다른 화면(`CenterListPage`의 `EMT022` 등)에서 이미 쓰던 것과 동일한 표준 패턴)
- 관리부서(`selectRole`)/설치지점(`selectCenter`)/그룹정보(`selectGroup`)는 공통코드가 아니라
  각각 다른 서비스가 조합하는 값이라 그대로 `formData.do?mode=Ins` 한 번 호출 유지
- `EMPTY_COMBOS`/`combos` state에서 위 5개 필드 제거

### 검증
- `npm run build` 정상 확인(컴파일 에러 없음)

### 이슈 / 개선사항
- ⚠️ 백엔드에서 해당 5개 `resultMap.put` 라인을 제거해도 프론트는 이제 영향 없음(이미 개별
  훅으로 전환 완료) — 다만 `formData.do?mode=Edt` 응답의 `regist`(단말 상세)는 이 프론트
  변경과 무관하게 계속 필요하니 그 부분은 그대로 둬야 함

## DID 등록/수정 모달 "관리부서" 콤보 빈 목록 버그 수정 + GROUP_COMBO로 전환
**파일:** `backend/.../sym/did/web/DidInfoManageController.java`(`formData.do`, `equipRegistCombo.do`),
`pages/backoffice/equiManage/DidInfoList.jsx`, `pages/backoffice/equiManage/components/DidFormModal.jsx`

### 배경
"관리 부서 combo 안나옴" 보고 → curl로 확인해보니 매장관리가 쓰는 `GROUP_COMBO`
(`/api/basicManage/group/combo.do`)는 정상인데, DID 모달의 관리부서(`formData.do`의
`selectRole`)만 빈 배열이었음.

### 원인
`DidInfoManageController`의 `formData.do`/`equipRegistCombo.do` 두 곳 모두
`groupManagerService.selectGroupManageCombo(groupVo)`를 `groupVo.setGroupId(partId)`만
설정한 채 호출하고 있었음 — 이 계층 제한 쿼리는 `parentGroupId`도 같이 없으면 항상 빈
목록을 반환하는 버그(2026-08-04 앞부분 로그에서 `GroupManagerController`에 대해 이미 확인/
수정했던 것과 동일한 버그)가 이 컨트롤러에는 아직 반영 안 돼 있었음.

### 수정
1. (백엔드, 우선 수정) 두 곳 모두 `groupManagerService.selectUserGroupManageListByPagination(new GroupVo())`
   (계층 제한 없는 전체 목록)로 교체 — `GroupManagerController`와 동일한 해결책
2. (사용자 요청에 따른 추가 정리) 관리부서(`roleCode`)는 결국 부서(LETTNAUTHORGROUPINFO)
   정보이므로, `formData.do`의 `selectRole`에 의존하는 대신 다른 화면(`CenterFormModal`,
   `ManagerAdminFormModal`)과 동일하게 `GROUP_COMBO` 엔드포인트를 `DidInfoList.jsx`에서
   직접 `useCustomReqDataCombo`로 조회하도록 전환. `DidFormModal.jsx`의 옵션 렌더링도
   `o.groupId`/`o.groupNm` → `o.code`/`o.codeNm`(콤보 정규화 형태)로 맞춤

### 검증
- 별도 7099 포트에 임시 백엔드를 띄워 수정 전(`selectRole: []`) → 수정 후(정상 목록 반환)
  재확인, 테스트 인스턴스는 종료해서 정리
- 프론트는 `npm run build` 정상 확인(컴파일 에러 없음)

### 이슈 / 개선사항
- ⚠️ **사용자 확인 필요**: 사용자가 쓰는 7004포트 백엔드는 재시작해야 반영됨
- `equipRegistCombo.do`(통합관리 화면용)는 아직 프론트에서 실사용되지 않지만 동일 버그라 같이 고침

## DID 단말기 관리 — 콘텐츠재전송/재부팅 일괄 명령 버튼 추가
**파일:** `pages/backoffice/equiManage/DidInfoList.jsx`, `constants/URL.jsx`

### 내용
레거시 `didList.jsp`의 `Restart_Did(code)`(체크된 단말에 콘텐츠재전송/재부팅 명령 일괄 발송,
이전 작업 때 "리스트+모달" 범위가 아니라고 제외했던 기능)를 추가.
- 그리드 맨 왼쪽에 체크박스 컬럼 추가(`rowSelection={{mode:'multiRow', checkboxes:true,
  headerCheckbox:true}}` + 빈 헤더의 `pinned:'left'` 컬럼)
- 검색영역에 "콘텐츠재전송"/"재부팅" 버튼 추가 — `gridApiRef.current.getSelectedRows()`로
  체크된 행의 `didId`/`didMac`을 모아 레거시와 동일한 `restartInfo` 형식(선행 구분자 1자 +
  `didId|didMac` 콤마 목록)으로 만들어 기존 백엔드 `POST .../did/restart.do`(`DID_RESTART`
  URL 상수 신규 추가)로 전송
- 이 엔드포인트는 `@RequestParam`(폼 파라미터) 기반이라 JSON 바디로 보내면 안 됨 — 쿼리스트링에
  담아서 전송(그룹 관리 화면의 단말 삭제 때 이미 쓴 것과 동일한 방식)

### 검증
- `npm run build` 정상 확인(컴파일 에러 없음)

### 후속 수정 — 체크박스 컬럼 스타일 어긋남
`pinned: 'left'`를 준 빈 헤더 컬럼이 AG Grid 테마의 "고정 컬럼" 별도 섹션 스타일을 타면서
다른 헤더(진한 남색)와 색이 달라 붙어있는 것처럼 어색하게 보임 — `pinned` 속성 제거(순서상
이미 맨 왼쪽이라 굳이 고정할 필요 없음).

## 로그인 화면이 원래 가려던 경로 대신 무조건 hr/manager로 보내던 문제 수정
**파일:** `pages/Login/LoginPage.jsx`

### 배경
"`/backoffice/sub/equiManage/did_group`로 이동하면 `/backoffice/hr/manager`로 강제로
이동함" 보고. `ProtectedRoute.jsx`는 인증 실패 시 `state: {from: location}`을 실어
`/login`으로 보내는데, `LoginPage.jsx`는 그 `state.from`을 전혀 안 보고 "이미 로그인됨"
이펙트와 로그인 성공 후 둘 다 `/backoffice/hr/manager`로 하드코딩해서 보내고 있었음 — 어떤
이유로든(토큰 일시 만료, 새로고침 시 인증 재확인 등) `/login`을 거치면 원래 있던 페이지를
잃어버리고 무조건 hr/manager로 튕기는 구조였음.

### 수정
`useLocation()`으로 `location.state.from`을 읽어서(있으면 그 경로로, 없으면 기존처럼
`/backoffice/hr/manager`로) 리다이렉트하도록 두 곳(이미 로그인된 경우 스킵 이펙트, 로그인
성공 후 이동) 모두 수정.

### 검증
- `npm run build` 정상 확인(컴파일 에러 없음)
- 실제 인증 만료/재현 테스트는 안 함(재현 조건을 특정하지 못함) — ⚠️ 사용자가 다시
  같은 상황을 재현해서 이제 did_group으로 정상 유지되는지 확인 필요

### 후속 수정 — 체크박스 컬럼 옆에 빈 열 생기던 문제
직전에 `pinned` 제거하며 남겨둔 빈 헤더 스페이서 컬럼(`headerName:'', width:20...`)이 AG
Grid의 신규 `rowSelection` API가 자동으로 삽입하는 선택용 컬럼과 겹쳐서 체크박스 옆에
불필요한 빈 열이 하나 더 생기고 전체선택 헤더 체크박스도 안 뜨는 문제 발생 — 스페이서
컬럼을 완전히 제거하고 AG Grid가 자동 삽입하는 선택 컬럼만 쓰도록 정리.

### 검증
- `npm run build` 정상 확인(컴파일 에러 없음)

## 단말기 그룹 관리 페이지 호출 시 500 에러 — javax → jakarta 패키지 오류 수정
**파일:** `backend/.../sym/grp/web/GroupInfoManageController.java`

### 배경
"DIdGroupInfo 페이지 호출하면 `IllegalStateException: No primary or single unique
constructor found for interface javax.servlet.http.HttpServletRequest` 에러 남" 보고.

### 원인
이 컨트롤러 상단 import가 `javax.annotation.Resource`/`javax.servlet.http.HttpServletRequest`로
남아있었음(Jakarta EE 이관 누락 — CLAUDE.md에 명시된 "javax.servlet.*은 반드시 jakarta.*로"
규칙 위반). Spring 6/Spring Boot 3는 `jakarta.servlet.http.HttpServletRequest`만 특수
프레임워크 타입으로 인식하기 때문에, 컨트롤러 메서드 파라미터가 `javax.*` 버전이면 이걸
그냥 일반 `@ModelAttribute`로 오인해서 데이터 바인딩을 시도하다가(인터페이스라 생성자가
없으니) 예외를 던짐 — 이 컨트롤러가 담당하는 모든 엔드포인트(`didGroupList.do`,
`didgroupLst/{groupCode}.do`, `didgroupUpdate.do` 등, `DIdGroupInfo.jsx`가 쓰는 API 전부)가
호출될 때마다 100% 실패하는 상태였음.
- 참고: 같이 뜬 "Redis unavailable, refresh token not cached" 경고는 **이 문제와 무관**한
  별개 사항(Redis 서버가 로컬에 없어서 JWT 리프레시 토큰 캐싱만 건너뛰는 것 — 로그인 자체나
  이 500 에러의 원인이 아님)

### 수정
```java
import jakarta.annotation.Resource;
import jakarta.servlet.http.HttpServletRequest;
```

### 검증
- `./gradlew compileJava` 정상 확인
- 전체 백엔드에 `javax.servlet.*` import가 남아있는 곳이 더 있는지 검색 → 이 파일이 유일했음

### 이슈 / 개선사항
- ⚠️ **사용자 확인 필요**: 7004포트 백엔드 재시작 후 단말기 그룹 관리 화면 재확인 필요
- 검색 중 `javax.annotation.Resource`를 쓰는 Service 클래스 12개를 추가로 발견함(전부
  `sts/*Service.java`류, 컨트롤러 아님) — 이쪽은 `@Resource` 필드 주입이 컴파일은 되지만
  Spring이 `jakarta.annotation.Resource`만 인식해서 조용히 주입이 안 될 위험이 있는
  별개 문제라 이번 범위에서는 손대지 않음. 필요하면 후속으로 확인 요청드립니다.

## 매퍼 XML의 LETTNGNRLMBER 테이블 참조를 TB_MANAGERINFO로 전환
**파일:** `CenterInfoManagerMapper.xml`, `ContentFileManagerMapper.xml`(2곳),
`ContentInfoManagerMapper.xml`(2곳), `GroupInfoManagerMapper.xml`(3곳) — 전부
`mapper/postgresql` 하위

### 배경
"xml 에서 LETTNGNRLMBER 테이블을 tb_managerinfo 로 변경해줘" 요청. 5개 매퍼 파일에 걸쳐
동일한 패턴(로그인한 관리자의 `mberId`로 권한 스코프용 부서/매장을 조회하는 서브쿼리)이
총 8곳 반복돼 있었음:
```sql
SELECT GROUP_ID FROM LETTNGNRLMBER WHERE MBER_ID = #{mberId}   -- 부서 스코프
SELECT CENTER_ID FROM LETTNGNRLMBER WHERE MBER_ID = #{mberId}  -- 매장 스코프
```

### 컬럼 매핑 확인 후 치환
단순히 테이블명만 바꾸면 컬럼이 안 맞아서(`LETTNGNRLMBER`는 `MBER_ID`/`GROUP_ID`,
`TB_MANAGERINFO`는 `MANAGER_ID`/`PART_ID`) 먼저 `TB_MANAGERINFO`를 실제로 쓰는 다른 매퍼
(`ManagerInfoManagerMapper.xml`, `EgovLoginMapper.xml`)에서 컬럼명을 확인:
- `MBER_ID` → `MANAGER_ID`(PK)
- `CENTER_ID`는 `TB_MANAGERINFO`에 그대로 존재 — 컬럼명 안 바꿔도 됨
- **`GROUP_ID`(부서 스코프 서브쿼리)는 `PART_ID`로 치환** — `TB_MANAGERINFO`엔 `GROUP_ID`
  컬럼이 없고 대신 `PART_ID`(→ `TB_PARTINFO` FK)가 있음. 이전 로그(2026-08-04 앞부분)에
  "`TB_PARTINFO`의 ID 포맷이 `LETTNAUTHORGROUPINFO`의 `GROUP_ID`(EMART_+13자리)와 일치 —
  마이그레이션 출처로 추정"이라고 남긴 그 관계를 그대로 적용해서, `PART_ID` 값을 기존
  `LETTNAUTHORGROUPINFO` 계층 쿼리(`WHERE GROUP_ID = (...)`)에 그대로 넣음

### 검증
- `./gradlew compileJava` 정상 확인(매퍼 XML은 컴파일 시점엔 검증 안 되므로 런타임 재확인 필요)
- 전체 `mapper/` 디렉토리에서 `LETTNGNRLMBER` 재검색 → 0건(전부 치환 완료)

### 이슈 / 개선사항
- 백엔드 재시작 필요(매퍼 XML 핫리로드 안 됨)

### 후속 확인 — PART_ID vs GROUP_ID
사용자가 "PART_ID는 이제 안 쓰고 GROUP_ID로 할 예정"이라고 해서 4곳(`CenterInfoManagerMapper`
외 3개 파일)을 `GROUP_ID` 컬럼명으로 되돌렸다가, 곧바로 "TB_MANAGERINFO의 PART_ID는
LETTNAUTHORGROUPINFO의 GROUP_ID와 매핑함"이라고 정정 — 즉 **현재 시점 컬럼명은 여전히
PART_ID이고(값은 GROUP_ID와 동일 값 공간), 향후 컬럼명 자체를 GROUP_ID로 바꿀 예정**이라는
뜻으로 이해해서 원래대로 `PART_ID`로 다시 되돌림(4곳 전부). 최종 상태는 이번 항목 시작
시점과 동일 — `TB_PARTINFO`/`LETTNAUTHORGROUPINFO` 두 갈래 부서 개념 이슈는 여전히 유효.

⚠️ **사용자 확인 필요**: TB_MANAGERINFO의 컬럼명을 실제로 `PART_ID` → `GROUP_ID`로 바꾸시면
(또는 새 GROUP_ID 컬럼을 추가하시면) 이 4개 파일의 8곳도 같이 컬럼명을 맞춰야 함 — 스키마
변경하실 때 알려주시면 함께 반영하겠습니다.

## didGroupList.do 권한 스코프 필터가 항상 잘못된 분기를 타던 오타 수정
**파일:** `backend/.../sym/grp/web/GroupInfoManageController.java`
(`selectGroupDidInfoManageListByPagination`, `selectGroupListInfo`)

### 배경
"`didGroupList.do`에서 `searchVO.put("roldId", "ROLE_ADMIN")` 했는데도 매퍼의
`<if test='params.roleId != "ROLE_ADMIN" ...'>` 분기를 탐" 보고.

### 원인
컨트롤러가 맵에 넣는 키는 `"roldId"`(오타, e 빠짐)인데, 매퍼는 `params.roleId`(정상 철자)를
검사함 — 키가 안 맞아서 MyBatis OGNL 입장에선 `params.roleId`가 항상 `null`. `null !=
"ROLE_ADMIN"`은 참이라 항상 `<if>` 블록에 들어가고, `null`은 `ROLE_DID_ADMIN`/
`ROLE_MUSIC_ADMIN`도 아니라서 `<otherwise>`(매장 단위로 좁히는 `CENTER_ID` 조건)로 빠짐 —
관리자(`ROLE_ADMIN`)로 로그인해도 전체 목록이 아니라 본인 매장으로만 좁혀진 결과를 받는
상태였음. `selectGroupDidInfoManageListByPagination`은 디버깅 중이셨는지
`loginVO.getRoleId()` 대신 `"ROLE_ADMIN"` 하드코딩 + `log.info` 디버그 라인도 같이 남아있어
정리.

### 수정
`searchVO.put("roldId", ...)` → `searchVO.put("roleId", loginVO.getRoleId())`(2곳,
`selectGroupDidInfoManageListByPagination`/`selectGroupListInfo`), 하드코딩된
`"ROLE_ADMIN"`과 디버그 `log.info` 라인 제거.

### 검증
- `./gradlew compileJava` 정상 확인
- 백엔드 전체(Java 소스 + 매퍼 리소스)에서 `roldId` 재검색 → 0건(이 두 곳이 유일했음)

### 이슈 / 개선사항
- ⚠️ 백엔드 재시작 필요(코드 변경, 핫리로드 안 됨)
