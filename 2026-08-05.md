# 2026-08-05 작업 로그

## DidPicListPage 화면 기본 골격 적용
**파일:** `pages/backoffice/OperManage/DidPicListPage.jsx`

### 내용
DID 모니터링 캡처화면을 11절 골격(`content-header`/`content-search`/`content-table`) 3단
구성으로 재작성. 캡처 이미지 썸네일 갤러리라 표 형태 데이터가 아니어서 `content-table`
영역은 `AppAgGrid` 대신 기존 카드형 그리드(썸네일 그리드)를 그대로 유지 — 골격 문서의
"그리드는 보통 `useGridInfinite`" 원칙의 예외로, `GroupListPage`(계층형이라 클라이언트
그리드 사용)와 같은 케이스로 처리.
- 검색 영역: 시작일/종료일(YYYYMMDD 텍스트 입력) + 검색/검색초기화 버튼, 우측에 기존
  "수동 등록"(DID ID/MAC/파일 업로드) 미니 폼 유지
- `ProtectedRoute`가 이미 인증을 보장하므로, 기존에 있던 `getCookie('accessToken')` 체크 +
  수동 `navigate('/login')` 리다이렉트(다른 골격 페이지들엔 없는 레거시 잔재)는 제거

### 검증
- `npm run build` 정상 확인(컴파일 에러 없음)

## XmlListPage(전문 관리) 화면 기본 골격 적용 + 미리보기/등록 모달화
**파일:** `pages/backoffice/OperManage/XmlListPage.jsx`,
`pages/backoffice/OperManage/components/XmlFormModal.jsx`(신규),
`pages/backoffice/OperManage/components/XmlPreviewModal.jsx`(신규),
`backend/.../mapper/postgresql/sts/xml/XmlInfoManagerMapper.xml`

### 내용
레거시 `xmlList.jsp`를 참고해서 11절 골격(`content-header`/`content-search`/`content-table`
+ `useGridInfinite`)으로 재작성하고, 기존에 화면 우측에 인라인으로 붙어있던 등록/수정
폼과 미리보기(`<pre>`)를 각각 `XmlFormModal`/`XmlPreviewModal`로 분리(레거시는 별도
팝업창(`window.open`)이었지만 이번 세션 내내 써온 "페이지 이동 없이 모달로" 원칙 유지).

### 백엔드 사소 추가 — 목록에 확인(testOk) 컬럼 누락
레거시 목록엔 "확인/미확인" 컬럼이 있는데 `selectXmlInfoManageListByPagination` 쿼리가
`TEST_OK`를 SELECT하지 않고 있었음(상세 조회/등록/수정 쪽엔 이미 있음) — 목록 쿼리에
`A.TEST_OK AS "testOk"` 한 줄만 추가해서 그리드에 확인/미확인 표시 가능하게 함.

### 레거시 대비 의도적으로 다르게 처리한 부분
- **전문구분(workGubun) 검색 필터 제외**: 레거시 화면엔 전문구분 드롭다운이 검색 조건에
  있었지만, 포팅된 `list.do` 쿼리(`selectXmlInfoManageListByPagination`)엔애초에 `workGubun`
  필터링 조건 자체가 없음(검색조건은 전문명/전문설명 두 가지만 지원) — 넣어봤자 동작 안
  하는 컨트롤이라 검색 영역에서 뺌. 검색조건 select는 실제 지원되는 두 값(`XML_PROCESS_NAME`/
  `processRemark`)만 노출
- **"요청전문"/"결과전문" → "JSON 미리보기"/"XML 미리보기"로 표기 변경**: 레거시는 두 미리보기
  버튼이 각각 별도 백엔드 URL(`jsonAuthView.do`/`jsonAuthReq.do`)을 써서 입력(요청)/출력(결과)
  전문을 구분해서 보여줬는데, 새 백엔드의 `preview/json`/`preview/xml` 두 엔드포인트는 **둘 다
  입력 파라미터(`xmlInputParam`+`xmlInputParamSample`) 기준으로 형식(JSON/XML)만 다르게
  생성**함 — 출력 파라미터(`xmlOutputParam`) 기준 미리보기 엔드포인트는 백엔드에 아예 없음.
  "요청전문"/"결과전문"이라고 라벨을 붙이면 실제 동작과 안 맞아서 오해를 줄 수 있어 정직하게
  "JSON 미리보기"/"XML 미리보기"로 이름을 바꿈

### 검증
- `npm run build`, `./gradlew compileJava` 모두 정상 확인

### 이슈 / 개선사항
- ⚠️ **사용자 확인 필요**: "결과전문"(출력 파라미터 기준 미리보기)이 실제로 필요하시면
  백엔드에 새 엔드포인트를 추가해야 함(현재는 입력 파라미터만 미리보기 가능) — 필요하시면
  말씀해 주세요
- 백엔드 재시작 필요(매퍼 XML 컬럼 추가분 반영, 핫리로드 안 됨)

## SendMsgListPage(전문통신현황) 화면 기본 골격 적용 + 레거시 대비 누락 기능 점검
**파일:** `pages/backoffice/OperManage/SendMsgListPage.jsx`

### 내용
레거시 `sendResultList.jsp`와 비교해서 11절 골격(`content-header`/`content-search`/
`content-table` + `useGridInfinite`)으로 재작성. 이 화면은 조회 전용 로그라 "등록" 버튼은
없음(레거시에도 없음).

### 레거시 대비 점검 결과 — 빠져있던 것
백엔드(`SendMsgInfoManagerMapper.xml`)가 실제로 지원하는 필터/필드를 다시 확인해보니,
기존 `SendMsgListPage.jsx`(골격 적용 전)엔 다음이 빠져 있었음 — 이번에 전부 보강:
- **통신 시작일~종료일(schStartDay~schEndDay) 날짜범위 검색** — 백엔드는 이미 지원하는데
  프론트에 필드 자체가 없었음. `<input type="date">` 2개 추가(레거시처럼 "시작일 입력 시
  종료일도 입력해야 함" 검증도 그대로 유지)
- **검색조건(전문명/DID ID) + 검색어** — 백엔드가 `searchCondition`/`searchKeyword`로
  전문명 또는 DID ID 텍스트 검색을 지원하는데, 기존엔 "DID ID 검색"이라는 이름의 프리텍스트
  입력 하나만 있고 실제로는 `searchKeyword`만 보내고 있어서(searchCondition 없이) 항상
  DID_ID 검색으로만 동작했음 — 검색조건 select 추가해서 전문명 검색도 가능하게 함
- **응답시간(didPlayTime) 컬럼** — 백엔드가 이미 내려주는 필드인데 그리드 컬럼 목록에서
  빠져 있었음 — "응답시간" 컬럼 추가(레거시의 "통신시간" 컬럼이 요청시간+응답시간을 한
  칸에 같이 보여주던 것을, 이 프로젝트의 다른 화면들처럼 별도 컬럼으로 분리)

### 레거시에 있지만 이번에도 못 넣은 것
- ⚠️ **삭제 버튼(`del_Msg(msgSeq)`)** — 레거시엔 행별 삭제 버튼이 있었지만, 백엔드
  (`SendMsgInfoManageController`/`SendMsgInfoManagerMapper`/`SendMsgInfoManageService`)에
  `TB_MESSAGEHISTORY` 삭제 엔드포인트가 전혀 없음(select/insert/update만 있고 delete 없음).
  없는 API를 호출하는 버튼을 만들 수 없어서 이번엔 제외 — 필요하시면 백엔드에 삭제
  엔드포인트 추가부터 해야 함

### 검증
- `npm run build` 정상 확인(컴파일 에러 없음)

## ContentFileLibraryPage 화면 기본 골격 적용
**파일:** `pages/backoffice/ConManage/ContentFileLibraryPage.jsx`

### 내용
콘텐츠 파일(이미지/영상/음원) 라이브러리를 11절 골격(`content-header`/`content-search`/
`content-table`) 3단 구성으로 재작성. `DidPicListPage`/`ContentFileLibraryPage` 둘 다
썸네일 갤러리라 `content-table` 영역은 `AppAgGrid` 대신 기존 카드형 그리드를 그대로
유지(골격 문서의 예외 케이스, `DidPicListPage`와 동일 처리).
- 검색 영역: 미디어 타입(전체/이미지/영상/음원) select + 파일명 검색 + 검색/검색초기화 버튼
- `ProtectedRoute`가 이미 인증을 보장하므로 기존 `getCookie('accessToken')` 수동 체크는 제거
- 기능(사용/사용중지 토글, 연결된 콘텐츠 확인 후 삭제)은 그대로 유지, 버튼 스타일만
  프로젝트 공통 클래스(`btn-outline__gray` 등)로 통일

## ContentFileLibraryPage — "미디어 파일 등록" 업로드 화면 신규 추가 (+ 백엔드 버그 2건 수정)
**파일:** `pages/backoffice/ConManage/ContentFileLibraryPage.jsx`,
`pages/backoffice/ConManage/components/ContentFileUploadModal.jsx`(신규),
`backend/.../sts/cnt/web/ContentFileInfoManageController.java`,
`backend/.../sts/cnt/modals/ContentFileInfo.java`,
`backend/.../mapper/postgresql/sts/cnt/ContentFileManagerMapper.xml`, `constants/URL.jsx`

### 배경
레거시 `mediaLst.jsp`의 "미디어 파일 등록"(별도 팝업 `FileUpload.jsp`, 드래그앤드롭 다중
업로드) 버튼이 빠져 있었음 — 확인해보니 백엔드(`ContentFileInfoManageController`)에
업로드 엔드포인트 자체가 아예 없었음(list/삭제/사용여부만 있고 등록 없음).
"미디어 파일정리" 버튼은 레거시 `mediaLst.jsp`에 실제로 없는 기능이라 사용자 확인 결과
**지금은 버튼만 두고 나중에 직접 연결하기로 함**(현재는 "준비 중" 안내만 뜸).

### 변경 내용
- **`POST /api/backoffice/sub/conManage/file/upload.do`(신규 백엔드 엔드포인트)** — 다중
  `MultipartFile` 업로드. 물리 저장은 기존 파일들과 동일한 관례 경로
  `{Common.filePath}/EMART_DID/did/upload/{yyyyMM}/`에 저장(기존 데이터로 실제 확인한 경로
  패턴), `fileMultiService.uploadFile()`로 저장 후 `atchFileId`는 저장된 파일명(확장자 제외)을
  그대로 사용, `conFileService.insertFileManage()`(있었지만 어디서도 호출 안 하던 미연결
  메서드)를 처음으로 연결
- `ContentFileUploadModal.jsx`(신규) — `use-file-upload.jsx`의 `useFileUpload` 훅
  (`multiUse:true` + `renderGalleryDropzone()`)으로 다중 드래그앤드롭 업로드 UI 구성,
  업로드 후 목록 자동 새로고침
- `ContentFileLibraryPage.jsx`에 "미디어 파일 등록"(기능함)/"미디어 파일정리"(자리만,
  클릭 시 "준비 중" 안내) 버튼 추가

### 백엔드 버그 수정 (업로드 연동 중 실제 curl 테스트로 발견)
1. **`ContentFileInfo.java`에 `groupId`/`centerId`/`realFileNm`/`fileAlbumRegdate` 필드
   누락** — `insertFileManage`/`updateFileManage` SQL이 이 필드들을 `#{groupId}` 등으로
   참조하는데 모델에 필드 자체가 없어서 호출하면 MyBatis가 "no getter for property" 오류로
   즉시 실패하는 상태였음(한 번도 호출된 적 없어서 안 드러났던 버그) — 필드 추가
2. **`FILE_SIZE`/`CON_SEQ` 컬럼 타입/누락 문제** — `FILE_SIZE`는 DB가 `numeric`인데 SQL에
   타입 캐스트가 없어 문자열 바인딩 시 `BadSqlGrammarException` 발생 → `::numeric` 캐스트
   추가. `CON_SEQ`는 아예 INSERT 컬럼 목록에서 빠져 있어서(테이블은 NOT NULL 제약) 매번
   삽입이 실패하는 상태였음 → 컬럼 추가(라이브러리 전용 업로드는 `conSeq='0'`으로 저장)

### 검증 (별도 7099 포트 임시 인스턴스로 실제 업로드 → 조회 → 삭제까지 end-to-end 확인)
1. 임시 인스턴스에 실제 파일 업로드 curl 테스트 → 처음엔 위 버그들 순서대로 발견/수정
   (필드 누락 → FILE_SIZE 캐스트 → CON_SEQ 누락, 총 3번 재시도)
2. 최종적으로 업로드 성공 확인, `list.do`로 재조회해서 실제 리스트에 나오는지 확인
   (mediaType이 확장자 기준으로 자동 IMAGE 판별됨), 물리 파일도 정확한 경로에 생성됨을 확인
3. 삭제 API(`DELETE /{atchFileId}.do`, 기존 로직)로 DB+물리파일 함께 삭제되는 것까지 확인
4. 테스트로 만든 파일/레코드 전부 정리 완료, 임시 인스턴스 종료
5. `npm run build` 정상 확인

### 이슈 / 개선사항
- ⚠️ **사용자 확인 필요**: 7004포트 백엔드 재시작 필요(컨트롤러/모델/매퍼 전부 변경됨)
- "미디어 파일정리" 버튼은 자리만 만들어둠 — 사용자가 나중에 직접 기능 연결 예정
- 업로드 시 `groupId`는 로그인 관리자의 `partId`로 채우지만, `centerId`는 비워둠(레거시도
  업로드 시 매장 지정 개념이 없었음) — 필요하면 후속으로 추가 가능
- 동영상(MEDIA) 파일은 썸네일 자동 생성 로직이 did_emart에 아직 없어 `fileThumnail`이 항상
  비어있음(레거시엔 있었을 가능성) — 프론트는 이미 이미지가 아니면 텍스트 플레이스홀더로
  대체하고 있어서 화면이 깨지진 않음

## ContentMutiListPage(화면 구성 관리) 기본 골격 적용 + 등록/수정 모달화
**파일:** `pages/backoffice/ConManage/ContentMutiListPage.jsx`,
`pages/backoffice/ConManage/components/ContentMutiFormModal.jsx`(신규)

### 내용
레거시 `conMutiList.jsp`(목록) + `conMutiDetail.jsp`(등록/수정 폼)를 참고해서 11절 골격 +
`useGridInfinite`로 재작성, 기존에 화면 우측에 인라인으로 붙어있던 등록/수정 폼을
`ContentMutiFormModal`로 분리. "편성"(페이지별 파일 배치, 기존 `ContentDetailEditorPage`로
이동하는 큰 별도 화면)은 요청 범위(등록/수정 모달화) 밖이라 그대로 페이지 이동 유지.

### 레거시 로직 재현
- `conMutiDetail.jsp`의 `ViewPage()` — 화면타입(`conScreen`) 선택 시 가로/세로 코드
  (`conType`)·화면 사이즈·분할사이즈를 자동으로 채워주던 로직(`W11`→가로 풀스크린,
  `H11`→세로 풀스크린, `W21`/`H21`→분할)을 코드값 그대로 재현
- 검색조건은 레거시엔 "콘텐츠명/파일명"으로 돼 있었지만, 실제 백엔드 쿼리를 확인해보니
  "파일명"이 아니라 **순번(conSeq) 텍스트 검색**이었음(파일명 검색 자체가 없음) — 레거시
  라벨이 실제 동작과 달라서 정직하게 "콘텐츠명/순번"으로 라벨을 맞춤

### 발견한 기존 프론트 갭 — 분할사이즈(conMid) 필드 누락
기존(골격 적용 전) 페이지에는 `conMid` 입력 자체가 없었음 — 백엔드 `update.do`는 폼에 없는
필드도 그대로 덮어쓰기 때문에, 수정할 때마다 기존 DB의 `conMid` 값이 조용히 `null`로
날아가고 있었을 가능성이 있음. 이번에 "분할사이즈" 필드를 모달에 추가해서 해결.

### 검증
- `npm run build` 정상 확인(컴파일 에러 없음)

## 14:35 — ContentMutiInfoManagerMapper.xml XML 손상 수정 + roleId 빈값 문제 진단
**파일:** `backend/src/main/resources/mapper/postgresql/sts/cnt/ContentMutiInfoManagerMapper.xml`,
`backend/src/main/java/com/common/backoffice/sym/grp/web/GroupInfoManageController.java`

### 배경
사용자가 `GroupInfoManageController`에서 `searchVO.put("roleId", "ROLE_ADMIN")`을 넣었는데도
mapper에서 roleId가 빈값으로 나온다고 보고 — 진단 중 임시로 `e.printStackTrace()`를 추가해
재기동했더니, **백엔드 전체가 부팅조차 안 되는 훨씬 심각한 문제**를 발견함.

### 원인 (blocking, 전체 부팅 실패)
`ContentMutiInfoManagerMapper.xml`의 `selectContentMutiInfoManageListByPagination` 쿼리가
`<choose>`/`<when>`/`<otherwise>` 태그 구조가 SQL 텍스트와 뒤섞여 깨져 있었음
(`</choose        AND A.CENTER_ID = #{centerId}>` 같은 형태 — 닫는 태그 안에 SQL이 삽입됨).
MyBatis는 부팅 시 모든 mapper XML을 미리 파싱하기 때문에, 이 파일 하나의 XML 오류가
`SAXParseException`을 일으켜 **애플리케이션 컨텍스트 전체가 기동 실패**하고 있었음.
같은 파일 안의 구조가 동일한 정상 쿼리(`selectContentMutiInfoManageListTotCnt_S`)를
템플릿으로 삼아 재구성. 부수적으로 `) TA`(서브쿼리 별칭)가 빠져있던 것과, `#{conType}`에
숫자 캐스트(`::numeric`)가 빠져있던 것도 함께 맞춤(정상 버전과 통일).

### roleId 진단 결론
포트 7099 임시 인스턴스로 재기동 후 로그인 → `didGroupList.do` 호출 결과, 응답의
`regist.roleId`에 `"ROLE_ADMIN"`이 정상적으로 담겨 있음을 확인(200건 조회 성공, 필터
미적용 — ROLE_ADMIN이므로 정상 동작). **roleId 자체는 Map → mapper로 정상 전달되고
있었고**, 이전에 관찰된 문제는 이 XML 손상으로 인한 부팅 실패/이전 인스턴스의 잔여 상태
때문이었을 가능성이 높음. 진단용으로 넣었던 `e.printStackTrace()`는 확인 후 제거함.

### 검증
- 임시 인스턴스(포트 7099) 재기동 → 정상 부팅 확인(`Started BackendApplication`)
- `didGroupList.do` 호출 → `resultCode:200`, `regist.roleId:"ROLE_ADMIN"` 확인
- `./gradlew compileJava` 정상 확인
- 테스트 인스턴스 종료 완료(포트 7099 프로세스 kill)

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- **실사용 중인 7004 백엔드 인스턴스 재기동 필요** — 이번 XML 수정을 포함해 이번 세션에
  변경된 여러 mapper/controller/model 변경사항이 아직 반영 안 됨
- `ResultHelper.setFailResult`가 `e.toString()`만 로깅해서(스택트레이스 없음) 이번처럼
  래핑된 예외(`MyBatisSystemException`)의 근본 원인 파악이 어려웠음 — 추후 로깅 개선을
  검토할 만함(이번엔 범위 밖이라 손대지 않음)

## 14:40 — DID 그룹 상세 단말기 등록 UI 개선 (검색 가능한 selectbox + 버튼 폰트 축소)
**파일:** `front/src/pages/backoffice/equiManage/components/DidGroupDetailCellRenderer.jsx`

### 변경 내용
- "그룹 내 단말기 목록" 하단의 순수 `<select>`를 `react-select` 기반 검색 가능한
  selectbox로 교체(타이핑으로 단말명 필터링 가능, `isClearable`/`isSearchable` 적용).
  기존 값 바인딩(`selectedDidId` 문자열 state)과 등록/삭제 로직은 그대로 유지.
- "단말기 등록" 버튼 글자가 버튼 크기에 비해 커서 잘려 보이던 문제 — 인라인 스타일로
  `fontSize`/`padding`을 줄여서 버튼 안에 온전히 들어가도록 조정. 공통 클래스
  (`btn-default__blue`)는 다른 화면에서도 쓰이므로 건드리지 않고 이 컴포넌트에서만
  인라인으로 오버라이드함.
- 서브그리드(단말기 목록) 하단이 다음 마스터 행과 너무 붙어 있던 문제 — 상세 셀 렌더러
  최하단에 `padding-bottom: 16px` 추가, 그에 맞춰 부모(`DIdGroupInfo.jsx`)의
  `MasterDetailGrid` `detailRowHeight`를 `280`→`320`으로 상향(늘어난 여백만큼 상세 행 높이도
  같이 키워야 내용이 안 잘림).
- 단말기 selectbox의 드롭다운 메뉴가 상세 행(고정 높이·overflow 영역) 아래로 가려져 안
  보이던 문제 — `react-select`의 `menuPortalTarget={document.body}` + `menuPlacement="auto"`
  적용, 메뉴를 `document.body`에 포탈로 렌더링해서 그리드 컨테이너의 overflow/z-index
  제약을 벗어나도록 수정.
- selectbox·버튼 영역을 고정 폭이 아닌 `flex` 기반(반응형)으로 조정 — select는
  `flex: 1 1 200px`(최소 160px~최대 400px)로 가변, 버튼은 `flexShrink: 0`으로 항상 제
  크기 유지, 컨테이너에 `flexWrap: wrap` 추가해서 좁은 화면에서는 버튼이 다음 줄로
  자연스럽게 내려가도록 함.

### 검증
- `react-select`는 이미 프로젝트에 설치돼 있는 라이브러리(`multi-select.jsx`에서 사용 중)라
  별도 패키지 추가 없음
- `npm run build` 정상 확인(컴파일 에러 없음)

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 없음
