# 2026-08-14 작업 로그

## 콘텐츠별 배포 관리 - 배포/해제 버튼을 레거시와 동일한 체크박스 토글로 교체
**파일:** `front/src/pages/backoffice/BrodManage/BrodScheduleStatusPage.jsx`

### 변경 내용
- 레거시 `playShedule.jsp` 원본 확인 결과, "음원콘텐츠배포" 화면은 별도의 등록 버튼이
  아니라 **체크박스 자체가 즉시 토글**(체크 → `checkVal=Y` 배포, 해제 → `checkVal=N`
  배포해제, `onChange`에서 바로 API 호출)이었고, 벌크 등록 버튼(`schUpdate()`)은
  레거시 소스에도 주석 처리된 채 실제로 쓰인 적이 없었음.
- "콘텐츠별 배포 관리" 탭의 매장별 "배포"/"해제" 버튼 2개를 레거시와 동일하게 체크박스
  1개("적용" 컬럼)로 교체. 로직은 기존 `handleToggle(center, connect)`를 그대로
  재사용(체크 시 `BrodDeployTimeModal`로 배포시간 입력 후 배포, 해제 시 확인창 후
  배포해제) — API/동작 자체는 바뀐 게 없고 위젯만 버튼→체크박스로 변경.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 없음

## 방송 배포 관리 - "스케줄 음원 관리" 탭 신설(brodContentPlayList.jsp 이관)
**파일:**
- `front/src/pages/backoffice/BrodManage/BrodScheduleStatusPage.jsx`
- `front/src/pages/backoffice/BrodManage/components/BrodScheduleFileRegisterModal.jsx` (신규)
- `front/src/constants/URL.jsx`

### 변경 내용
- 레거시 `brodContentPlayList.jsp`("스케줄음원관리" 상단탭)를 확인해 세 번째 탭으로 추가.
  좌측 음원 파일 목록(`CON_FILE_LIST`, mediaType=MUSIC) → 파일 선택 시 우측에 그 음원이
  배치된/배치 가능한 전체 매장 편성 현황(`BROD_CONTENT_RIGHT`, 이미 백엔드엔 있었지만
  프론트 미사용이던 API)을 보여주는 2단 레이아웃.
- 매장 체크박스 다중 선택 → "삭제"(`BROD_CONTENT_RIGHT_DELETE`, 기존 편성 있는 매장만
  대상)와 "등록"(신규 모달) 액션.
- "등록" 모달(`BrodScheduleFileRegisterModal.jsx`)은 레거시 팝업 `brodContentCopy.jsp`를
  참고해 콘텐츠등록(일반편성)/특정방송등록 라디오로 폼을 전환하고, 체크된 매장들에
  동시에 등록. 백엔드 `detailCenterUpdate.do`/`annDetailCenterUpdate.do`(둘 다 존재는
  했으나 프론트 미사용)에 연결 — 콤보 데이터는 이미 다른 화면(특정방송 등록)에서 쓰던
  `copyPopupData.do`를 재사용.
- `URL.jsx`에 `BROD_CONTENT_DETAIL_CENTER_UPDATE`/`BROD_CONTENT_ANN_DETAIL_CENTER_UPDATE`
  신규 추가.
- 주의: `rightDelete.do` 바디 키는 camelCase(`insertBrodCode`)인데
  `detailCenterUpdate.do`/`annDetailCenterUpdate.do`는 VO 필드명 그대로인 snake_case
  (`insert_brodCode`) — 레거시 Java 필드명을 그대로 물려받은 값이라 백엔드 코드를
  직접 확인해서 맞춤(둘을 헷갈리면 등록/삭제가 조용히 실패함).
- `right.do`가 지점명 서버 필터(`centerNm`)를 받지 않아(atchFileId만 파라미터) 지점명
  검색은 클라이언트 사이드 필터로 구현.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 실제 데이터로 화면 확인은 못 했음(로컬 서버 미기동) — 특히 "등록/삭제" 두 액션은
  실제 클릭까지 확인 부탁드립니다.
- `right.do`에 `centerNm` 파라미터를 백엔드에서 받아주면 클라이언트 필터 없이 서버
  페이징도 가능해짐(현재는 매장 전체를 한 번에 내려받아 프론트에서 필터링) — 필요하면
  후속으로 백엔드 컨트롤러/매퍼에 파라미터 추가 가능.

## 콘텐츠 등록 - 음원 파일 선택 SELECT BOX를 검색 가능한 SELECT BOX로 교체
**파일:** `front/src/pages/backoffice/BrodManage/components/BrodContentDetailFormModal.jsx`

### 변경 내용
- `BrodContentDetailPage` → "콘텐츠 등록/수정" 모달의 "콘텐츠명(음원 파일)" 항목에서
  결과를 고르던 순수 `<select>`를 `react-select`(프로젝트에 이미 의존성 존재,
  `components/Common/multi-select.jsx`에서 사용 중) 기반 검색 가능한 select로 교체.
- 기존 "파일명 검색" 입력 + 검색 버튼(서버 검색, `fileSearch.do`)은 그대로 유지하고,
  검색된 결과 목록을 보여주는 select만 타이핑으로 재필터링 가능한 컴포넌트로 변경.
- `fileOptions`(atchFileId/orignlFileNm)를 `{ value, label }` 형태로 변환하는
  `fileSelectOptions`, 현재 선택값을 찾는 `selectedFileOption`을 `useMemo`로 추가.
- 높이/라운드를 기존 `form-select`와 맞추기 위한 `FILE_SELECT_STYLES` 정의.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 동일한 음원 파일 select 패턴이 `BrodAnniversaryFormModal.jsx`(특정 방송 등록)에도
  있음(`atchFileId` select). 필요하면 동일하게 검색 가능한 select로 교체 가능 — 이번
  요청 범위(BrodContentDetailPage 콘텐츠 등록)에는 포함하지 않음.

## 특정방송 등록 - 음원 파일 선택 SELECT BOX를 검색 가능한 SELECT BOX로 교체
**파일:** `front/src/pages/backoffice/BrodManage/components/BrodAnniversaryFormModal.jsx`

### 변경 내용
- "특정방송 등록/수정" 모달의 "콘텐츠명(음원 파일)" select도 위 콘텐츠 등록 모달과
  동일한 방식으로 `react-select` 기반 검색 가능한 select로 교체.
- "파일명 검색" 입력 + 검색 버튼(서버 검색)은 그대로 유지, 결과 select만 타이핑
  재필터링 가능하도록 변경. `fileSelectOptions`/`selectedFileOption` useMemo,
  `FILE_SELECT_STYLES` 동일 패턴 적용.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 없음

## 방송 배포 관리 - "배포 현황"(읽기전용) 탭 신설, "콘텐츠별 배포 관리"와 분리
**파일:** `front/src/pages/backoffice/BrodManage/BrodScheduleStatusPage.jsx`

### 변경 내용
- 레거시 확인 결과 `playShedule.do`(음원콘텐츠배포 — 콘텐츠 선택 후 매장별 배포/해제
  토글)와 `playSheduleStatus.do`(콘텐츠배포현황 — 선택 없이 바로 나오는 읽기전용
  리스트)는 서로 다른 화면이었는데, did_emart 이관 과정에서 토글 화면만
  `/backoffice/sub/brodManage/schedule`에 구현되고 "배포 현황" 읽기전용 리스트는
  누락돼 있었음(백엔드 `statusList.do`/`selectplaySheduleStatus`는 이미 구현돼
  있었지만 프론트에서 미사용).
- 사용자 확인(탭으로 분리, 추천안) 반영 — 같은 페이지에 탭 2개 구성:
  - **배포 현황**(기본 탭): brodCode 선택 없이 바로 전체 목록 조회. 콘텐츠명/지점명
    검색 + 배포여부(전체/배포/미배포) 필터. 컬럼: 콘텐츠명·지점명·재생일자·
    기념일여부·지점운영시간·배포여부·배포생성일자·다운로드여부·다운로드일자
    (레거시 jsp 컬럼 그대로).
  - **콘텐츠별 배포 관리**: 기존에 있던 콘텐츠 선택 → 매장별 배포/해제 토글 UI를
    그대로 이 탭으로 이동(로직 변경 없음).
- MhsRoomManagePage.jsx와 동일한 탭 스타일(`tabBtnStyle`/`tabContainerStyle`)을
  재사용해 프로젝트 탭 UI 관례를 맞춤.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 없음

## 콘텐츠 상세 등 화면 하단에 검은 배경이 보이는 문제 수정
**파일:** `front/src/index.css`

### 변경 내용
- 원인: `@media (prefers-color-scheme: dark)` 블록이 앱 자체의 `[data-theme]` 토글과
  무관하게 OS/브라우저의 다크 모드 선호도만 보고 `--bg`(html 배경)를 `#16171d`로
  바꿔버리고 있었음. `body`엔 라이트 모드용 명시적 배경이 없어 html의 `--bg`를 그대로
  물려받는데, 콘텐츠가 짧을 땐 `.wrapper`/`.main-contents`의 흰 배경이 덮어서 안
  보이다가 콘텐츠가 길어져 그 아래로 여백이 생기면 어두운 html 배경이 그대로
  노출됨(사용자가 화면에서 명시적으로 "Light"를 선택한 상태여도 재현).
- 같은 파일 상단 주석(20~23줄)에 이미 "OS 선호도가 아니라 `[data-theme]` 토글을
  따르도록 한다"는 의도가 적혀 있었는데 `color-scheme` 속성만 그렇게 고정해두고
  바로 아래 미디어쿼리 블록은 가드를 빼먹었던 것으로 보임.
- 수정: `@media (prefers-color-scheme: dark)` 내부 셀렉터를
  `:root:not([data-theme="light"])`로 가드 — 사용자가 명시적으로 Light를 선택하면
  이 블록을 건너뛰고 기본 라이트 `--bg`(#fff)를 그대로 사용하도록 함. JS가 아직
  `data-theme`를 붙이기 전(최초 페인트) OS 다크 선호도를 따르는 기존 동작은 유지.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 없음

## 문화센터(MHS) 강의 관리 - 브랜드/매장 미선택 시 전체 목록 조회 + 그리드 폭 채우기
**파일:** `front/src/pages/backoffice/RoomManage/MhsRoomManagePage.jsx`

### 변경 내용
- "강의 관리" 탭이 브랜드/매장을 선택해야만 목록을 조회하도록 프론트에서 막고
  있었는데, 백엔드 매퍼(`MhsClassManageMapper.xml`의 `selectMhsClassList`)는 두
  파라미터가 비어 있으면 필터 없이 전체를 조회하고(`authorCode == 'ROLE_MHS_USER'`
  일 때는 서버가 자동으로 소속 매장 범위로 제한) 이미 지원하고 있어, 프론트 가드를
  제거하고 탭 진입 시 바로 전체 목록이 보이도록 변경.
- 상단 안내 문구("브랜드와 매장을 선택해야 조회됩니다")는 여전히 선택이 필요한
  "편성표" 탭에만 뜨도록 조건 수정.
- `classColumnDefs`에 `flex`가 없어 그리드가 컨테이너 폭을 다 못 채우고 오른쪽에
  빈 공간이 남던 문제 — 다른 그리드(모니터 관리/편성표)와 동일하게 강의명 컬럼에
  `flex: 1`을 주고, 잘리던 시작/종료 컬럼 폭도 넉넉하게 조정.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 기존부터 있던 `react-hooks/set-state-in-effect` ESLint 에러 3건(94, 276, 394줄
  대) — 커밋 HEAD 버전에서도 동일하게 발생하는 것을 확인(pre-existing). 이번 작업
  범위 밖이라 손대지 않음.

## 콘텐츠 등록/특정방송 등록 - 검색 SELECT BOX를 react-select에서 프로젝트 공통 컴포넌트로 재교체
**파일:**
- `front/src/pages/backoffice/BrodManage/components/BrodContentDetailFormModal.jsx`
- `front/src/pages/backoffice/BrodManage/components/BrodAnniversaryFormModal.jsx`

### 변경 내용
- react-select로 교체했던 음원 파일 select에서, 드롭다운 안에 "검색 입력창이 눈에
  보이지 않는다"는 피드백을 받아 프로젝트에 이미 정의돼 있던(미사용 상태였던) 공통
  컴포넌트 `CommonSearchSelect`(`components/Common/Select.jsx`)로 재교체.
- `CommonSearchSelect`는 드롭다운을 열면 상단에 실제 `<input placeholder="검색...">`
  가 보이는 형태라 "검색 가능한 select" 요구사항을 더 명확하게 충족.
- `fileOptions`(atchFileId/orignlFileNm) → `comboData`({ code, codeNm }) 형태로
  변환해서 전달, `onChange(e) => e.target.value`로 기존 select와 동일한 이벤트
  시그니처 유지.
- 두 파일 모두 `react-select` import 및 관련 스타일 상수 제거.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 없음
