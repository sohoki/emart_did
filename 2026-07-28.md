# 2026-07-28 작업 로그

## 14:35 — Redis 서버 없을 때 로그인/캐시 기능이 500으로 죽던 문제 수정
**파일:** `egovframework/com/jwt/EgovJwtTokenUtil.java`, `egovframework/com/util/RedisUtil.java`,
`egovframework/com/config/RedisConfig.java`,
`bas/code/service/EgovCcmCmmnDetailCodeManageService.java`,
`bas/role/service/RoleInfoManageService.java`, `bas/menu/service/MenuCreateManageService.java`

### 변경 내용
- 사용자가 로그인 시도 중 `RedisConnectionFailureException`으로 로그인 자체가 500 실패하는
  로그를 공유. 로컬 개발 환경에 Redis 서버가 없는 상태(어제 20절에서 `localhost:6379`로
  placeholder만 넣어둔 상태)에서, 캐시/리프레시토큰 저장용 Redis 호출들이 예외를 못 잡고
  그대로 전체 요청을 실패시키는 지점을 전수 조사함
- 코드베이스 전체에서 `redisTemplate`/`RedisUtil`/`@Cacheable` 사용처를 grep으로 훑어서
  보호 안 된 지점을 모두 찾아 수정:
  - `EgovJwtTokenUtil.generateRefreshToken()` — 리프레시 토큰을 Redis에 저장하는 호출이
    보호 안 돼 있어 로그인 자체가 실패하던 직접 원인. try-catch로 감싸서 Redis 없어도
    로그인은 계속 성공하게 함(리프레시 토큰만 캐싱 안 됨)
  - `EgovJwtTokenUtil.deleteRefreshToken()`(로그아웃) — 액세스 토큰 블랙리스트 등록 호출도
    동일 문제, catch 체인에 일반 Exception 추가
  - `RedisUtil.isKeyNullCheck()`/`commonCodeRedisRest()` — 애초에 예외 처리가 아예 없거나
    JsonProcessingException만 잡고 있어서 호출부(블랙리스트 체크, 리프레시 토큰 확인,
    공통코드 캐시 갱신) 전부가 영향받던 근본 지점. 여기서 막아서 모든 호출부를 한 번에 보호
  - `EgovCcmCmmnDetailCodeManageService.selectCmmnDetailComboLamp()`/`commonCodeRedisRest()`
    (private, RedisUtil과 별개 구현) — 콤보 조회 캐시 읽기가 try 밖에 있어 보호 안 됐었고,
    캐시 갱신 실패가 상위 등록/수정/삭제 메서드의 catch에 걸려 DB 쓰기가 성공했는데도
    -1(실패)로 잘못 보고되던 버그도 같이 발견/수정
  - `RoleInfoManageService.selectRoleInfoComboList()` — 동일 패턴(콤보 캐시 읽기 미보호)
  - `MenuCreateManageService.insertMenuCreatList()` — 메뉴 등록 후 Redis 캐시 무효화 루프가
    보호 안 돼 있어서, DB 반영은 끝났는데 Redis 캐시 갱신 단계에서 실패하면 메뉴 등록 전체가
    -1(실패)로 보고되던 동일 버그 패턴
  - `RedisConfig`에 `CacheErrorHandler` 빈 신규 추가 — `MenuInfoManageService`의
    `@Cacheable("findLeftMenu")`처럼 Spring Cache 추상화(어노테이션) 방식은 직접 try-catch로
    막을 수 없어서, Spring Boot가 자동 인식하는 전역 에러 핸들러로 처리(캐시 조회/저장/삭제
    실패를 로그만 남기고 무시, 원래 로직은 그대로 진행)
- 공통 원칙: DB 쓰기가 이미 성공한 뒤의 "캐시 갱신" 단계 실패는 절대 상위 CRUD 결과를
  실패로 뒤집지 않도록 각 지점에서 내부적으로 삼키게 처리함

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Logic errors & edge cases (DB 성공인데 캐시 실패로 -1 반환되던 버그 2건 포함)
- [x] Security vulnerabilities
- [ ] Performance concerns
- [ ] Test coverage (컴파일 확인 완료, 사용자 서버 재시작 후 재확인 필요)

### 이슈 / 개선사항
- `./gradlew compileJava` 클린 확인 완료
- 사용자가 직접 띄운 서버 프로세스는 그대로 두고 종료하지 않음 — **재시작 필요**(소스 변경
  반영 위해)
- 실제 Redis 서버가 여전히 없는 상태라 캐싱/블랙리스트/리프레시토큰 저장 기능 자체는 계속
  동작 안 함(의도된 성능 저하일 뿐 — 매 요청 DB 직접 조회로 폴백). 실제 Redis 서버 준비는
  별도 후속 과제

## 14:50 — 로그인 후 메뉴 조회 API의 ClassCastException 수정
**파일:** `uat/hri/web/ManagerInfoManagerController.java`,
`bas/menu/web/MenuInfoManageController.java`, `bas/program/web/ProgrmInfoManageController.java`,
`bas/role/web/RoleInfoManageController.java`, `bas/code/web/EgovCcmCmmnClCodeManageController.java`

### 변경 내용
- 로그인은 성공했으나 관리자 목록(`selectUserManagerList`)에서
  `ClassCastException: Integer cannot be cast to String` 발생 + 로그인 후 메뉴가 아예 안
  보인다는 제보. 원인은 `@RequestBody Map<String, Object>`로 받은 JSON 요청 본문에서
  `pageSize`/`pageUnit` 값을 무조건 `(String) searchVO.get(...)`로 캐스팅하고 있었던 것.
  프론트에서 숫자로 보내면(JSON number) Jackson이 `Integer`로 역직렬화하기 때문에 캐스팅이
  깨짐 — `PaginationHelper.buildInfo()`는 내부에서 `.toString()`으로 안전하게 처리하는데,
  일부 컨트롤러가 그 앞에서 별도로(대부분 실제로는 쓰이지도 않는 죽은 코드로) 위험하게
  미리 캐스팅하고 있었음
- 코드베이스 전체를 grep해서 동일 패턴 5곳을 전부 수정:
  `ManagerInfoManagerController`(관리자 목록), `MenuInfoManageController`(메뉴 목록 2곳 —
  메뉴가 안 보이던 직접 원인으로 추정), `ProgrmInfoManageController`(프로그램 목록),
  `RoleInfoManageController`(권한 목록), `EgovCcmCmmnClCodeManageController`(공통 그룹코드
  목록). `pageSize`/`pageUnit` 값이 실제로 쓰이는 곳은 `UtilInfoService.NVLObj(...)`(내부적으로
  `String.valueOf()` 사용, 타입 안전)로 교체, 안 쓰이는 곳은 죽은 코드 자체를 삭제

### 리뷰 체크리스트 결과
- [x] Logic errors & edge cases
- [ ] Test coverage (컴파일 확인, 사용자 서버 재시작 후 재확인 필요)

### 이슈 / 개선사항
- `./gradlew compileJava` 클린 확인
- 서버 재시작 필요(이번에도 사용자 프로세스는 종료하지 않음)
