# 2026-09-13 작업 로그

## (시간 미상) — MHS 모니터 상세 화면 추가 (roomManage/mhs 단말명 클릭 → 상세)
**파일:**
- `front/src/pages/backoffice/RoomManage/MhsMonitorDetailPage.jsx` (신규)
- `front/src/pages/backoffice/RoomManage/MhsRoomManagePage.jsx`
- `front/src/routes/index.jsx`

### 변경 내용
- `/backoffice/sub/roomManage/mhs` 목록의 "모니터 관리" 탭에서 단말명(`mhsMonitornm`) 셀이 일반
  텍스트였던 것을, 레거시 `roomManage/monitorList.jsp`의 `fn_MoniterView()`와 동일하게 클릭 가능한
  링크로 변경 — 클릭 시 신규 상세 페이지(`/backoffice/sub/roomManage/mhs/monitor/view?mhsMonitorcd=...`)로
  이동.
- 신규 `MhsMonitorDetailPage.jsx`: 레거시 `roomManage/monitorDetail.jsp`를 참고해 구성.
  - 상단: 모니터 정보(조직명/점포명/단말정보/네트워크정보/송출방식/최종통신일자/비고)
  - 하단: 모니터 송출 상세정보(조회일자 선택 + 10:00~22:00 시간대별 편성 강의 블록 테이블,
    행별 삭제 버튼)
  - 상단 액션: 미리보기(`MhsMonitorPreviewModal` 재사용), 모니터 재부팅(레거시와 동일하게 연동된
    커맨드가 없어 "준비 중" 안내만 표시), 정보 수정(목록으로 이동 + `?editMonitorcd=`로 수정모달
    자동 오픈), 목록으로
  - 기존 `DidDetailPage.jsx`(단말기 상세)와 동일한 골격/패턴(정보 테이블 스타일, 쿼리파라미터로
    목록의 수정모달 자동 오픈)을 그대로 재사용.
- `MhsRoomManagePage.jsx`: `useSearchParams` 추가, `?editMonitorcd=` 쿼리로 진입 시 해당 모니터의
  수정 모달을 자동으로 여는 effect 추가 (`DidInfoList.jsx`의 `editDidId` 패턴과 동일).
- `routes/index.jsx`: `/backoffice/sub/roomManage/mhs/monitor/view` 라우트 추가.
- 신규 API 엔드포인트는 필요 없었음 — 기존 `MHS_MONITOR_INFO`, `MHS_VIEWCONN_LIST`,
  `MHS_VIEWCONN_INFO`(백엔드 `CultureDisInfoManageController`)를 그대로 사용.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `npm run build` 정상 완료 확인. `npx eslint`에서 `react-hooks/set-state-in-effect` 경고가
  나오지만, 이는 참고한 기존 패턴(`DidDetailPage.jsx`/`DidInfoList.jsx`의 `editDidId` 처리 등)에도
  동일하게 존재하는 리포지토리 전역 이슈로, 이번 작업 범위에서 별도로 손대지 않음.
- "모니터 재부팅" 버튼은 레거시 JSP에도 실제 연동된 커맨드가 없던 미구현 기능이라 그대로
  "준비 중" 안내만 유지함 — 실제 재부팅 API가 추가되면 연결 필요.

## (시간 미상) — MyBatis SQL 로그가 한 줄로 길게 찍히는 문제 (log4jdbc 연결)
**파일:**
- `backend/src/main/resources/application.yml`
- `backend/src/main/resources/log4jdbc.log4j2.properties` (신규)
- `backend/src/main/java/egovframework/com/config/EgovConfigAppDatasource.java`

### 변경 내용
- 원인: `logging.level.com.common.backoffice: debug`로 켜지는 MyBatis 내장 로거(`==> Preparing` /
  `==> Parameters` / `<== Total`)는 SQL 문자열의 공백(줄바꿈 포함)을 로그로 남기기 전에 한 칸으로
  압축해버려서, 조인이 많은 쿼리가 한 줄로 매우 길게 찍힘. logback 설정만으로는 이미 사라진
  줄바꿈을 복원할 수 없음.
- 해결: 이미 의존성에는 있었지만 실제 연결은 안 돼 있던 `log4jdbc-log4j2`(org.bgee 포크)를
  local 프로파일에서 실제 JDBC 프록시 드라이버로 연결.
  - `application.yml`(local 전용) — `Globals.postgresql.Url`을 `jdbc:log4jdbc:postgresql://...`로,
    `Globals.postgresql.DriverClassName: net.sf.log4jdbc.sql.jdbcapi.DriverSpy` 추가.
    (prod/server 프로파일은 손대지 않음 — 순정 `jdbc:postgresql:` 그대로 유지)
  - `EgovConfigAppDatasource.java` — 하드코딩돼 있던 `"org.postgresql.Driver"`를
    `@Value("${Globals.postgresql.DriverClassName:org.postgresql.Driver}")`로 바꿔 프로파일별로
    다른 드라이버를 쓸 수 있게 함(prod/server는 해당 프로퍼티가 없으므로 기본값 유지 → 영향 없음).
  - `log4jdbc.log4j2.properties`(신규, 클래스패스 루트) — `log4jdbc.dump.sql.maxlinelength=0`으로
    SQL을 줄 길이 제한 없이 여러 줄로 pretty-print, `log4jdbc.spylogdelegator.name`을 slf4j
    델리게이터로 명시(이 프로젝트엔 진짜 Log4j2가 없음).
  - 기존에 이미 `jdbc.sqltiming: debug`로 켜져 있던 로거가 그대로 이 출력을 받음 — 별도 로깅
    레벨 변경 불필요.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `./gradlew compileJava` 정상 완료. `./gradlew bootRun --args='--spring.profiles.active=local'`로
  직접 기동해 확인 — `jdbc.sqltiming` 로거에 `SELECT 1`, `select version()` 등이 번호와 함께
  여러 줄로 정상 출력되는 것을 확인함(원격 DB 접속도 정상). 다만 포트 7004가 사용자의 기존 실행
  중인 인스턴스(PID 35168)로 이미 점유돼 있어 "Web server failed to start" 오류로 부트가
  끝까지 완료되진 않았음 — DataSource/드라이버 연결 자체는 그 이전 로그로 이미 검증됐고, 포트
  충돌은 이번 변경과 무관함. 사용자가 기존 인스턴스를 재기동해야 반영됨.
- `com.common.backoffice: debug`(MyBatis 자체 로거)는 그대로 켜둬서, 이제 같은 쿼리가
  `jdbc.sqltiming`(여러 줄, 파라미터 바인딩됨)과 MyBatis 자체 로그(한 줄, `?` 플레이스홀더)로
  중복 출력될 수 있음. 중복이 거슬리면 `com.common.backoffice`를 `info`로 낮추는 것을 고려
  (다만 SQL 외 다른 debug 로그도 같이 줄어듦 — 이번 작업 범위에서는 임의로 변경하지 않음).

## (시간 미상) — MHS 강의 등록/수정 모달에 조직명/점포명 선택 추가
**파일:**
- `front/src/pages/backoffice/RoomManage/components/MhsClassFormModal.jsx`
- `front/src/pages/backoffice/RoomManage/MhsRoomManagePage.jsx`

### 변경 내용
- 문제: `/backoffice/sub/roomManage/mhs` 목록 상단의 "브랜드 선택/매장 선택"은 원래 검색(필터)
  전용인데, "강의 관리" 탭의 강의 등록/수정 모달(`MhsClassFormModal`)이 그 검색용 선택값
  (`mhsCentercd`)에 의존하고 있었음 — 매장을 먼저 선택해야만 등록 모달이 열렸고, 등록 시에도
  모달 내부가 아니라 상단 검색값을 그대로 붙여서 저장했음. 레거시 `classList.jsp`의 강의
  등록/수정 팝업 및 같은 탭의 `MhsMonitorFormModal`(모니터 등록)은 모두 조직명(부서명)/점포명을
  모달 안에서 직접 선택하는 구조라 이와 불일치했음.
- `MhsClassFormModal.jsx`: `MhsMonitorFormModal`과 동일한 패턴으로 부서명/점포명 select를
  모달 최상단에 추가(부서명 선택 → 점포명 콤보 로드 → 점포 선택 → 강의실(모니터) 콤보 로드).
  더 이상 부모로부터 `mhsCentercd`를 prop으로 받지 않고 `form.mhsBrandcd`/`form.mhsCentercd`를
  직접 관리·검증(미선택 시 각각 경고 후 저장 중단)함.
- `MhsRoomManagePage.jsx`: `EMPTY_CLASS_FORM`에 `mhsBrandcd`/`mhsCentercd` 추가.
  `handleOpenClassModal`은 더 이상 상단 매장 선택을 강제하지 않고 항상 모달을 열되, 상단에서
  이미 필터링해 둔 값이 있으면 등록 폼의 기본값으로만 채워줌(모달 안에서 자유롭게 변경 가능).
  `handleClassSubmit`은 상단 검색값으로 덮어쓰지 않고 `classForm`(모달에서 선택한 값)을 그대로
  전송하도록 수정. 백엔드 `MHS_CLASS_UPDATE`/`MHS_CLASS_INFO` 응답·요청 모두 이미
  `mhsBrandcd`/`mhsCentercd` 필드를 갖고 있어 API 변경은 불필요했음.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `npm run build` 정상 완료. `npx eslint` 결과는 `react-hooks/set-state-in-effect` 관련 경고뿐이며,
  이는 그대로 참고한 `MhsMonitorFormModal.jsx`에도 동일하게 존재하는 기존 패턴(1:1 대응)이라
  이번 범위에서 손대지 않음.
- 레거시 팝업엔 있던 "강의 상태" 필드는 이번 요청 범위(조직명/점포명 선택) 밖이라 추가하지 않음.

## (시간 미상) — 강의 등록 모달 점포명 콤보박스에 검색 필드 추가
**파일:** `front/src/pages/backoffice/RoomManage/components/MhsClassFormModal.jsx`

### 변경 내용
- 점포 수가 많아(수십 개) 일반 `<select>`로는 원하는 매장을 찾기 번거롭다는 피드백 →
  프로젝트에 이미 있는 공통 컴포넌트 `CommonSearchSelect`(`components/Common/Select.jsx`,
  `BrodAnniversaryFormModal.jsx` 등에서 이미 쓰던 패턴)로 점포명 select를 교체.
  드롭다운을 열면 상단에 검색 입력창이 있어 매장명으로 필터링 가능.
- `centerList`(`{mhsCentercd, mhsCenternm}`)를 `CommonSearchSelect`가 기대하는
  `{code, codeNm}` 형태로 매핑하는 `centerSelectOptions`(useMemo) 추가. 선택 로직
  (`handleCenterChange`)과 비활성화 조건(부서 미선택 시 disabled)은 그대로 유지.
- 부서명(브랜드)/강의실 select는 이번 요청 범위 밖이라 그대로 둠(목록이 상대적으로 짧음).

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `npm run build` 정상 완료. 모니터 등록 모달(`MhsMonitorFormModal`)의 점포명 select도 동일하게
  검색이 필요하면 같은 패턴으로 쉽게 확장 가능 — 필요 시 요청.

## (시간 미상) — MhsRoomManagePage 그리드의 강의 시작일/종료일/시간 표시 형식 변경
**파일:** `front/src/pages/backoffice/RoomManage/MhsRoomManagePage.jsx`

### 변경 내용
- "강의 관리" 탭 그리드의 시작일/종료일(`mhsClassstartday`/`mhsClassendday`, 원본 `yyyyMMdd`)과
  시작시간/종료시간(`mhsClassstarttime`/`mhsClassendtime`, 원본 `HHmm`)이 원본 문자열 그대로
  (예: `20260915`, `1540`) 표시되던 것을, `valueFormatter`로 `yyyy-MM-dd`/`HH:mm` 형태로
  보기 좋게 표시하도록 변경.
- "편성표" 탭 그리드의 "시작~종료" 컬럼(`valueGetter`)도 동일하게 `HH:mm ~ HH:mm` 형태로 변경.
- 저장/폼 입력값 자체(원본 `yyyyMMdd`/`HHmm`, `MhsClassFormModal`의 `dashify`/`colonify`가
  이미 처리)는 그대로 — 이번 변경은 그리드 표시 전용이라 API 요청/응답 포맷에는 영향 없음.
- 포맷터(`formatYmd`/`formatHm`)는 파일 상단에 로컬 헬퍼로 추가(`MhsClassFormModal`의
  `dashify`/`colonify`와 동일 규칙, 프로젝트에 공용 날짜 유틸이 없어 기존 관례를 그대로 따름).

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `npm run build` 정상 완료. 기존에 있던 `react-hooks/set-state-in-effect` 경고 4건 외 신규
  경고/에러 없음.

## (시간 미상) — MhsRoomManagePage 상단 "매장 선택" select에 검색 기능 추가
**파일:** `front/src/pages/backoffice/RoomManage/MhsRoomManagePage.jsx`

### 변경 내용
- 목록 상단 검색영역의 "매장 선택" 순수 `<select>`를 `CommonSearchSelect`로 교체(앞서
  `MhsClassFormModal`의 점포명에 적용한 것과 동일 패턴) — 매장이 많아 원하는 매장을 찾기
  번거롭다는 동일한 이유.
- `centerList`를 `{code, codeNm}` 형태로 변환하는 `centerSelectOptions`(useMemo) 추가.
- `CommonSearchSelect`는 기존에 모달(`.input-box`, block 레이아웃) 안에서만 쓰이던 컴포넌트라,
  `content-search__option`의 flex 레이아웃 안에 그대로 두면 폭이 불안정할 수 있어 `width: 200`
  고정 wrapper로 감쌈. CLAUDE.md 11절의 "검색조건 select는 className 없이 순수 태그로 둔다"
  골격 기본값과는 다른 예외지만, 이번 요청 자체가 검색 가능한 select로 바꿔달라는 것이라 의도적
  적용임(브랜드 선택 select는 목록이 짧아 그대로 순수 `<select>` 유지).

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `npm run build` 정상 완료. 기존 lint 경고 4건 외 신규 없음.
- `CommonSearchSelect` 트리거가 Bootstrap `.form-select` 버튼이라 옆 "브랜드 선택"(common.css의
  `.content-search__option select` 커스텀 스타일)과 높이/테두리가 미세하게 다르게 보일 수 있음
  — 실제 화면에서 어색하면 브랜드 select에도 동일 컴포넌트를 적용해 통일하는 방법이 있음.

## (시간 미상) — 모니터 수정 모달 CSS 확인 + 모니터 목록 "연결 상태" 점 표시로 변경
**파일:** `front/src/pages/backoffice/RoomManage/MhsRoomManagePage.jsx`

### 조사 내용 (코드 변경 없음)
- 사용자가 "모니터 수정" 모달의 select/input이 Bootstrap 기본 스타일로 너무 크게 보인다고
  스크린샷 제공 → 코드를 확인해보니 `common.css`에 `.modal-body .form-select`/`.form-control`
  컴팩트 스타일(28px 높이, 12px 글자, 옅은 테두리)이 이미 2026-08-03 최초 커밋 때부터 정의돼
  있고, CSS 명시도 상 Bootstrap 기본 `.form-select`(클래스 1개)보다 이 규칙(클래스 2개)이
  항상 우선함을 확인.
- 실제 common.css/bootstrap.min.css 파일을 그대로 가져와 모달 마크업을 재현한 정적 HTML을
  헤드리스 Chrome으로 렌더링해 검증 — 실제로 28px 높이의 컴팩트한 select/input이 정상 렌더링됨
  (스크린샷과 다름). 즉 **코드 자체는 이미 올바르며, 사용자가 본 화면은 최신 코드가 반영되지
  않은 캐시/이전 빌드일 가능성이 높음** — 브라우저 강력 새로고침 또는 재빌드/재배포를 안내함.

### 변경 내용
- "모니터 관리" 탭 그리드의 "운용상태" 컬럼을 "연결 상태"로 이름 변경하고, 텍스트("정상"/
  "미연결") 대신 파란색(연결:#0d6efd)/회색(미연결:#94a3b8) 원형 점(●)으로 표시하도록 변경.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `npm run build` 정상 완료.
- 접근성: 점 색상 외에 `title` 속성(호버 시 "연결됨"/"연결 안됨")만 있고 텍스트 라벨은 없음 —
  필요하면 텍스트 병기 고려 가능.

## (시간 미상) — 상단 "매장 선택"(CommonSearchSelect) 높이를 "브랜드 선택"과 맞춤
**파일:**
- `front/src/style/RoomManage.css` (신규)
- `front/src/pages/backoffice/RoomManage/MhsRoomManagePage.jsx`

### 변경 내용
- 문제: 앞서 검색 기능 추가를 위해 "매장 선택"을 `CommonSearchSelect`(버튼 기반)로 바꾸면서,
  common.css의 `.content-search__option select`(높이 33px) 규칙이 버튼에는 적용되지 않아
  옆 "브랜드 선택"(순정 `<select>`)보다 눈에 띄게 높게 보이던 문제.
- `RoomManage.css` 신규 추가: `.form-select.mhs-center-search-select`(클래스 2개 결합 —
  `CommonSearchSelect`가 트리거 버튼에 `form-select`와 커스텀 클래스를 함께 붙이는 걸 이용해
  래퍼 div는 건드리지 않고 버튼에만 걸리도록 좁힘)에 `.content-search__option select`와
  동일한 값(높이 33px, 14px 글자, #E0E0E0 테두리, 3px 라운드, min-width 127px) 적용.
- `MhsRoomManagePage.jsx`에 `import '@/style/RoomManage.css'` 추가, 해당
  `CommonSearchSelect`에 `className="mhs-center-search-select"` 부여.
- 실제 common.css/RoomManage.css를 가져와 두 select를 나란히 렌더링한 정적 HTML을 헤드리스
  Chrome으로 스크린샷해 높이가 정확히 일치함을 확인.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `npm run build` 정상 완료. 헤드리스 브라우저 렌더링으로 높이 일치 확인함(스크린샷 첨부).

## (시간 미상) — 모니터 관리 목록: 상단 브랜드/매장 선택이 검색에 실제로 반영되도록 수정
**파일:**
- `backend/src/main/java/com/common/backoffice/sts/mhs/web/CultureDisInfoManageController.java`
- `backend/src/main/resources/mapper/postgresql/sts/mhs/MhsMonitorManageMapper.xml`
- `front/src/pages/backoffice/RoomManage/MhsRoomManagePage.jsx`

### 조사 내용
- 사용자 지적: "문화센터/매장 선택했는데 검색에 반영이 안 되면 그 select가 있을 이유가 없다"
- 원인 2가지 확인:
  1. **컨트롤러**: `selectMhsMonitorListByPagination`이 권한(authorCode)과 무관하게 무조건
     `searchVO.setMhsCentercd(loginVO.getCenterId())`로 클라이언트가 보낸 값을 덮어써서, 관리자
     계정이 상단에서 어떤 매장을 선택하든 항상 로그인 계정 자신의 매장으로 강제됐음.
  2. **매퍼**: `selectMhsMonitorList`/`selectMhsMonitorListCnt`의 검색조건(단말명/ID/IP/MAC
     텍스트 검색)과 매장 필터가 전부 `authorCode == 'ROLE_MHS_USER'`(개별 매장 담당자) 분기
     안에만 있어서, 그 외 권한(통합관리자 등)은 검색창/브랜드/매장 선택 **전부** 목록 조회에
     영향이 없었음(사실상 전체 목록 무조건 반환).
  3. **프론트**: 애초에 `fetchMonitorList`가 상단 `mhsBrandcd`/`mhsCentercd`를 요청에 담아
     보내지도 않았음.

### 변경 내용
- 컨트롤러: `ROLE_MHS_USER`일 때만 로그인 정보로 `groupId`/`mhsCentercd`를 강제 고정(보안 스코프
  유지, 클라이언트 값 무시). 그 외 권한은 클라이언트가 보낸 `mhsBrandcd`/`mhsCentercd`를 그대로
  사용하도록 조건부로 변경.
- 매퍼: `authorCode == 'ROLE_MHS_USER'` 전용이던 브랜드 계층(재귀 CTE) 제한은 그대로 유지하되,
  텍스트 검색조건과 `mhsBrandcd`/`mhsCentercd` 매장 필터는 권한과 무관하게 항상 적용되도록
  바깥으로 이동(두 개의 select 모두 동일하게 수정).
- 프론트: `fetchMonitorList`가 상단 `mhsBrandcd`/`mhsCentercd`를 요청 바디에 포함하도록 수정.
  "강의 관리" 탭과 동일하게, 상단 브랜드/매장 선택이 바뀌면 "모니터 관리" 탭도 즉시
  재조회하는 `useEffect` 추가.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities — ROLE_MHS_USER 강제 스코프는 그대로 보존, 다른 권한만 필터 기능 추가(권한 하향 아님)
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `./gradlew compileJava`, `npm run build` 모두 정상 완료.
- 실제 로그인 계정의 authorCode(ROLE_MHS_USER 여부)에 따라 동작이 달라지므로, 실제 DB로 두
  권한 모두 확인해보는 것을 권장.

## (시간 미상) — MHS 모니터 미리보기 모달을 실제 단말(레거시 preView.jsp) 디자인에 맞춤
**파일:**
- `front/src/pages/backoffice/RoomManage/components/MhsMonitorPreviewModal.jsx`
- `front/public/resource/img/cc_logo.png` (신규 — 레거시 emart_cms3.2.1/img/cc_logo.png 그대로 복사)

### 조사 내용
- 사용자 지적: 내가 처음에 만든 미리보기 모달 디자인이 실제 단말 화면과 달라서, 실제 화면과
  다른 걸 보여준다.
- 확인 결과: 실제 문화센터 룸 단말(안드로이드)은 이 새 `did_emart` 백엔드가 아니라 **레거시
  EMART_CMS(JSP) 시스템의 `did.emart.com/backoffice/sub/roomManage/preView.do`를 인증 없이
  직접 폴링**하는 구조(레거시 `preView.jsp`/`preViewJson.do`, 1분마다 재조회·1시간마다
  새로고침). 두 시스템이 같은 DB를 보므로 강의 등록/편성 데이터 자체는 이미 정상 반영되고
  있고, 이 모달은 어디까지나 관리자용 참고 화면이라 모달 디자인을 바꾼다고 실제 단말 화면이
  바뀌지는 않음 — 다만 관리자가 실제와 다른 걸 보는 건 혼란을 주므로, 레거시 `preView.jsp`의
  실제 색상/타이포/배지를 그대로 옮겨 재현함.
- 레거시 `paragraph_M.css`(색상값), `preView.jsp`(마크업/문구), `img/cc_logo.png`(푸터 로고)를
  직접 확인해서 그대로 이식.

### 변경 내용
- 헤더: 흰 배경 + 하단 회색 경계선, 강의실명(굵게)+매장명(연회색), 우측 날짜(월/일, 요일)+
  큰 시각.
- 현재/다음 강의 카드: 진행중이면 빨간 알약 배지(#c70e0e)+시간, 없으면 회색 배지(#a0a0a0);
  다음 강의는 짙은 회색 박스(#2e2e2e).
- "문화센터 강의 현황" 표: 굵은 상단 테두리(#5f5f5f)+옅은 하단 테두리, 진행중인 행의 강의명만
  빨간 알약 배지(#c20b0b)로 강조, 나머지는 연회색 텍스트 — 레거시와 동일한 강조 규칙.
- 푸터: 짙은 배경(#2e2e2e) + 레거시 "Culture Club" 로고 이미지(신규 복사한 `cc_logo.png`),
  기존에 쓰던 emart 로고 대신 실제와 동일한 로고로 교체.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `npm run build` 정상 완료. 실제 common.css 대신 레거시 색상값을 그대로 인라인 스타일로
  이식했고, 샘플 데이터로 헤드리스 브라우저 렌더링해 사용자가 보여준 실제 단말 스크린샷과
  레이아웃/문구가 일치함을 확인함.
- 이 모달은 여전히 "관리자 참고용 미리보기"일 뿐 실제 단말 렌더러는 아님 — 실제 단말이 legacy
  preView.do가 아닌 다른 URL을 보게 되는 시점(예: did_emart로 완전 전환)이 오면, 그때는 이
  디자인을 실제 단말 렌더링 화면(현재는 백엔드에 인증 필요 API로만 존재)으로 승격하는 별도
  작업이 필요함.

## (시간 미상) — MHS 모니터 미리보기를 실제 단말 렌더링 페이지로 승격
**파일:**
- (신규) `front/src/pages/backoffice/RoomManage/MhsMonitorDevicePage.jsx`
- `front/src/routes/index.jsx` — 인증 불필요 라우트 추가
- `front/src/constants/URL.jsx` — `MHS_DEVICE_PREVIEW` 추가
- `front/src/pages/backoffice/RoomManage/components/MhsMonitorPreviewModal.jsx` — "단말 화면
  원본 열기" 버튼 추가
- `backend/src/main/java/com/common/backoffice/sts/mhs/web/CultureDisInfoManageController.java`
  — 인증 불필요 공개 API `GET /device/preview.do` 신규 추가(기존 `preViewJson.do`와 조회 로직
  `buildPreviewResult()`로 공유)
- `backend/src/main/java/egovframework/com/security/SecurityConfig.java` — 위 API를
  `AUTH_GET_WHITELIST`에 등록

### 변경 내용
- 직전 작업(모달 디자인 이식)을 "실제 단말이 폴링할 수 있는 진짜 페이지"로 승격.
- 신규 `MhsMonitorDevicePage.jsx`: `AppLayout`/`ProtectedRoute`를 거치지 않는 완전 독립 라우트
  (`/backoffice/sub/roomManage/mhs/device?mhsMonitorcd=...`)로 전체화면 렌더링. 레거시
  `preView.jsp`의 실제 픽셀 크기(room_name 63px, meeting_list th 30px/td 26px 등)를 그대로
  가져와 1920x1080 기준으로 재현. 1초마다 시계 갱신, 1분마다 편성 재조회, 1시간마다 전체
  새로고침까지 레거시 `day_view()`/`classInfo()` 동작을 그대로 이식.
- 백엔드: 기존 `preViewJson.do`(관리자, 인증 필요)와 데이터 조회 로직을 `buildPreviewResult()`
  private 메서드로 공통화하고, 인증을 요구하지 않는 `GET /api/backoffice/sub/roomManage/mhs/
  device/preview.do`를 신규 추가 — DID 장비용 `/equiManage/pic/capture.do`와 동일한 방식으로
  `SecurityConfig.AUTH_GET_WHITELIST`에 등록해 로그인 없이 호출 가능하게 함.
- 미리보기 모달에 "단말 화면 원본 열기" 버튼 추가(레거시 `fn_preview()`의 1920x1080 팝업과
  동일 취지) — 관리자가 실제 단말 URL을 바로 확인/테스트할 수 있음.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities — 신규 공개 API는 순수 조회 전용(단말 스케줄 표시 정보만 반환,
      개인정보/인증정보 없음)이라 무인증 노출 허용 가능한 범위로 판단. 등록/수정/삭제 API는
      전혀 건드리지 않음.
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `./gradlew compileJava`, `npm run build` 모두 정상 완료. 헤드리스 브라우저로 1920x1080 기준
  전체화면 레이아웃을 축소 렌더링해 사용자가 보여준 실제 단말 스크린샷과 일치함을 확인함
  (스크린샷 첨부).
- **아직 남은 일**: 실제 물리 단말은 여전히 레거시 `did.emart.com/backoffice/sub/roomManage/
  preView.do`를 보고 있음 — 이 신규 페이지로 실제 단말을 전환하려면 각 단말 키오스크 브라우저의
  홈 URL을 `(did_emart 프론트 도메인)/backoffice/sub/roomManage/mhs/device?mhsMonitorcd=...`로
  바꿔야 함(코드 밖의 실제 기기 설정 작업 — 원격 관리 도구나 직접 방문 필요).
- Swiper 슬라이드 자동 순환은 재현하지 않고 표+카드를 한 화면에 이어붙임(정보량 동일, 순환
  대기 없이 바로 보임) — 순환 애니메이션 자체가 꼭 필요하면 추가 요청 바람.

## (시간 미상) — 방송(음원) 콘텐츠 상세 화면 UI/UX 개선
**파일:** `front/src/pages/backoffice/BrodManage/BrodContentDetailPage.jsx`

### 변경 내용
- 로직/핸들러/상태는 전혀 손대지 않고 화면(JSX/스타일)만 재구성 — 데이터 흐름, API 호출, 모달
  연동은 기존 그대로.
- 기존 문제: 밋밋한 흰 배경에 구분 없는 표 3개가 이어져 있어 어디부터 어디까지가 한 묶음인지
  안 보였고, 상태값(이벤트유무/사용유무 등)도 그냥 텍스트라 스캔하기 어려웠음. 시간대별 음원
  배치 표는 칸마다 텍스트가 빽빽하게 붙어 있었음.
- `DidDetailPage.jsx`/`MhsMonitorDetailPage.jsx`와 동일한 "정보 박스" 관례
  (제목바 `sectionTitleStyle` + 라벨/값 그리드 `labelCellStyle`/`valueCellStyle`)를 그대로
  가져와 "콘텐츠 정보"를 카드형으로 재구성 — 앱 전체의 상세화면 관례와 일관성 확보.
  - 구분(일반/이벤트스케줄), 사용유무를 색상 배지(`Badge` 컴포넌트, 파랑/초록/회색/보라)로 표시.
- 상단 액션 버튼: "목록"을 왼쪽으로 분리하고, "콘텐츠 등록"/"특정 방송 등록"은
  `btn-primary btn-default__blue`(다른 화면의 "등록" 버튼과 동일 톤)로 강조, 나머지(수정/복사/
  배치 적용/편성표생성/방송표보기)는 회색 아웃라인으로 통일 — 어떤 버튼이 "만드는" 동작인지
  한눈에 구분되게 함.
- "시간대별 음원 배치" 표를 표 대신 카드형 컬럼 레이아웃으로 변경 — 슬롯 헤더에 옅은 배경,
  빈 슬롯엔 "배치된 음원 없음" 안내, 항목 간 점선 구분선, 삭제는 눈에 덜 띄는 고스트 "✕"
  버튼(hover 시 빨간색)으로, "총재생시간"은 파란 알약 배지로 변경.
- "특정방송(기념일) 목록" 표에 지브라 스트라이핑, 특정방송여부 배지 추가.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases — 데이터/핸들러 변경 없음(순수 스타일 리팩터)
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `npm run build` 정상 완료. 실제 common.css/bootstrap.min.css로 샘플 데이터 렌더링해 레이아웃
  확인(스크린샷 첨부).
