# 2026-08-23 작업 로그

## 단말기 관리 — "콘텐츠 보기" 열 + 단말기 상세 페이지 신규 작성
**파일:**
- `front/src/pages/backoffice/equiManage/DidDetailPage.jsx` (신규)
- `front/src/pages/backoffice/equiManage/DidInfoList.jsx`
- `front/src/routes/index.jsx`
- `front/src/constants/URL.jsx`

### 변경 내용
질문: `/backoffice/sub/equiManage/did` 목록에서 단말기명을 눌러도 상세 화면이 없고 수정 모달만
뜨는 이유 — 레거시 `equiManage/didView.jsp`에 대응하는 화면이 애초에 이관되지 않았던 것으로 확인.
백엔드(`DidInfoManageController`)에는 이미 `GET /{didId}/view.do`(단말 기본정보)와
`GET /{didId}/schList.do`(연결 스케줄/콘텐츠, 레거시 `didConSchList.do` 대응)가 구현돼 있었지만
프론트에서 쓰는 곳이 없었음 — 이번에 그 두 API를 그대로 재사용.

- 그리드에 "콘텐츠 보기" 열 추가 → `/backoffice/sub/equiManage/did/view?didId=` 페이지로 이동
  (라우트 신규 등록, `BrodContentDetailPage`와 동일하게 페이지 형태로 구현 — 처음엔 모달로
  만들었다가 사용자 요청으로 페이지로 변경).
- `DidDetailPage.jsx`: 레거시 `didView.jsp` 레이아웃(제공 스크린샷) 참고 — "단말기 정보" 3열
  표(단말명/단말ID/단말MAC, 단말IP/IP타입/단말OS, 관리부서/설치지점/그룹정보, 단말타입/단말형태/
  운영시간, 단말해상도/마지막접속일자/사용유무, 재부팅처리/단말화면확인/단말통신정보) +
  "연결 스케줄 정보" 표(순번/송출기간/스케줄/콘텐츠)로 구성.
  - 목록/수정/삭제: 수정은 값 수정 로직을 중복 구현하지 않으려고 목록 화면으로 이동 +
    `?editDidId=`를 붙여 `DidInfoList`가 진입 시 자동으로 기존 수정 모달을 열도록 처리.
    삭제는 기존 `useCommonDelete` 재사용, 성공 시 목록으로 이동.
  - 재부팅 처리("장비 재부팅")/단말화면확인(안드로이드는 "단말 화면캡처")은 이미 있던
    `/reboot.do`, `/capture.do`(둘 다 `@RequestParam` 방식이라 JSON 바디가 아니라 쿼리스트링으로
    전송 — `DidInfoList`의 `restart.do` 호출과 동일한 방식) 명령 발송 API에 연결.
  - 단말화면확인 중 윈도우 OS의 "단말 원격지원", "연결 스케줄 정보"의 "스케줄/콘텐츠
    등록하기"는 대응하는 백엔드 기능이 아직 없어(레거시도 "협의중"/점검중 알림만 띄우던
    부분) 안내(Swal "준비 중인 기능입니다")로만 남겨둠.
  - 단말 통신정보("장비통신 이력조회")는 별도 팝업 화면을 새로 만들지 않고, 이미 있는
    DID 전문통신현황 화면(`/backoffice/sub/operManage/snd`)을 레거시와 동일한 크기의
    팝업창(`window.open`)으로 띄우는 것으로 대체(didId로 검색조건이 자동 필터링되진 않음).

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `vite build` 정상 통과. `DidDetailPage.jsx`/`DidInfoList.jsx`의 데이터 로딩 `useEffect`에
  pre-existing과 동일한 `react-hooks/set-state-in-effect` 린트 에러가 뜨는데, 이 프로젝트
  전반(`BrodContentDetailPage` 등)에 이미 깔린 패턴이라 이번 범위에서 별도로 손대지 않음.
- "장비통신 이력조회" 팝업이 didId로 자동 필터링되지 않는 점은 필요하면 후속 작업으로 붙일 수 있음.

### 후속: "스케줄/콘텐츠 등록하기" 연결, "단말 원격지원"은 원래 미구현으로 확인
**파일:** `front/src/pages/backoffice/equiManage/DidDetailPage.jsx`,
`front/src/pages/backoffice/equiManage/ScheduleListPage.jsx`,
`front/src/pages/backoffice/ConManage/ContentMutiListPage.jsx`

레거시 `aten-common.js`의 `view_connPage()` 정의를 확인한 결과:
- "스케줄 등록하기" → `schDetail.do?mode=Ins&schCode=0` = did_emart의 **발송 스케줄 관리**
  (`ScheduleListPage`, `/backoffice/sub/equiManage/sch`)의 "등록" 모달과 동일 기능. 이미 존재.
- "콘텐츠 등록하기" → `conMutiDetail.do?mode=Ins&conSeq=0` = did_emart의 **화면 구성 관리**
  (`ContentMutiListPage`, `/backoffice/sub/conManage/muti`)의 "화면 등록" 모달과 동일 기능. 이미 존재.
- "단말 원격지원"은 `view_connPage()`에 케이스 자체가 없고, `didView.jsp`에서도 눈에 보이는
  버튼은 `send_break()`(서비스 점검 중 알림)에 연결돼 있었으며, 진짜 커맨드(`send_DID('ReM')`)는
  주석 처리된 채 "협의중입니다" 알림만 있던 **레거시에서도 실제로는 없었던 기능**으로 확인됨.
  did_emart 전체(front/back)에서도 원격지원/VNC/미러링 관련 코드 없음 — 연결할 기존 화면이
  없으므로 "준비 중" 안내를 그대로 유지하고, 판단 근거를 코드 주석으로 남겨둠.

두 등록 화면 모두 원래는 진입 시 자동으로 등록 모달을 여는 기능이 없어서, `DidInfoList`의
`?editDidId=` 자동오픈과 동일한 패턴으로 `?openInsert=1`(스케줄은 `&groupCode=`도) 쿼리 파라미터를
추가해, 단말기 상세 화면에서 넘어오면 목록 진입과 동시에 등록 모달이 열리도록 연결함. 스케줄
등록은 해당 단말이 속한 그룹(`groupCode`)을 미리 선택해 두도록 프리필도 추가함(레거시는 빈 폼만
열었지만, 다른 그룹을 고르면 소용없는 화면이라 컨텍스트를 넘기는 쪽이 더 자연스러움).

**리뷰 체크리스트:** Code quality/Naming/Logic/Security/Performance — 이상 없음. Test coverage: 미흡.

**이슈:** 없음. `vite build` 정상 통과.

---

## 로그아웃 시 인증 쿠키 미삭제 버그 수정
**파일:** `front/src/lib/cookie.jsx`, `front/src/layouts/AppLayout.jsx`

### 변경 내용
- `cookie.jsx`에 `removeCookie(name)` 추가 — `setCookie`와 동일하게 `path=/`로 만료시켜야 실제로 지워짐.
- `AppLayout.jsx`의 `handleLogout`에서 `actionLogoutJWT.do` 호출 성공 후 `accessToken`,
  `refreshToken`, `userId`, `userName`, `roleId`, `partId` 쿠키를 명시적으로 삭제하도록 수정.
  (기존엔 서버가 refreshToken만 무효화하고 클라이언트 쿠키는 그대로 남아 있어, 로그아웃 후에도
  브라우저에 accessToken이 남아 재로그인된 것처럼 보였음. hotelPms 등 다른 프로젝트를 같은
  호스트에서 같이 띄워두면 쿠키가 이름 기준으로 겹쳐 증상이 더 헷갈리게 나타날 수 있었음.)
  `savedManagerId`(아이디 저장) 쿠키는 로그인 편의 기능이라 그대로 유지.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 없음

---

## 방송(음원) 콘텐츠 상세 — "편성표생성"/"방송표보기" 버튼 구현
**파일:**
- `backend/src/main/java/com/common/backoffice/sts/brd/web/BrodContentInfoManageController.java`
- `front/src/constants/URL.jsx`
- `front/src/pages/backoffice/BrodManage/BrodContentDetailPage.jsx`
- `front/src/pages/backoffice/BrodManage/components/BrodContentOrganizationModal.jsx` (신규)

### 변경 내용
레거시 `emart_cms3.2.1`의 `brodContentView.jsp`(방송(음원) 콘텐츠 상세) 참고. 이 화면은 항상
`centerId` 없이 `brodCode` 단독으로 호출하는 "일반 편성표" 케이스만 쓰므로, 그 범위만 포팅함
(지점(`centerId`) 기준 배포 케이스는 원본이 의존하는 `FN_CENTERBRODINFO` DB 함수가 did_emart에
없어 계속 제외 — 백엔드 컨트롤러 상단 주석 참고).

- **방송표보기** (레거시 `ContentBrodReport.do`): `GET /api/backoffice/sub/brodManage/content/{brodCode}/organization.do`
  신규 추가. 기존에 있던(미사용) `BrodOrganizationManagerService.selectBrodOrgnizationPage()`를
  그대로 재사용 — 파라미터 바인딩된 쿼리라 추가 위험 없음. 프론트는 팝업창 대신
  `BrodContentOrganizationModal` 모달로 재생시간/재생콘텐츠/구분 테이블을 보여줌.
  (레거시의 "EXCEL DOWN" 버튼(`ContentBrodExcel.do`)은 did_emart 백엔드에 없어 이번 범위 제외.)
- **편성표생성** (레거시 `ContentBrodConfirm.do`): `POST /api/backoffice/sub/brodManage/content/scheduleGenerate.do?brodCode=`
  신규 추가. 원본이 재생간격(`codeDc`) 조회에 쓰던 `UniSelectInfoManageService`(테이블/컬럼명을
  문자열로 조립하는 동적 SQL이라 SQL 인젝션 위험이 있어 did_emart엔 들이지 않기로 이미 결정했던
  유틸)는, 이미 `selectBrodContentInfo()`가 같은 값을 `codeDc`로 정상 반환하고 있어 그걸 재사용하는
  것으로 대체(로직 자체는 원본과 동일). 원본 private 헬퍼 `brodReport()`(약 246줄, 09:00~24:00을
  10분 슬롯으로 순회하며 특정방송/일반 편성을 `TB_BRODORGANIZATION`에 채워 넣는 로직)를 조건식
  그대로 포팅. 원본이 쓰던 `EgovStringUtil.lenReplace/secToMinTimeChart`는 did_emart 공용 유틸에
  없어 공용 파일을 건드리지 않고 컨트롤러 로컬 `private static` 메서드(`padLeftZero`/`toSlotMinSec`)로
  옮겨둠.
- 프론트 `BrodContentDetailPage.jsx`의 두 버튼을 각각 `handleOpenOrgModal`/`handleGenerateSchedule`로
  연결(기존엔 "준비 중" 안내만 뜨는 `handleNotReady`였음). 편성표생성은 파괴적 작업(기존 편성표
  전체 삭제 후 재생성)이라 Swal 확인창을 거침.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `backend`: `gradlew compileJava` 정상 통과(기존 warning 외 신규 에러 없음).
- `front`: `vite build` 정상 통과. `BrodContentDetailPage.jsx`에 pre-existing 린트 에러
  1건(`react-hooks/set-state-in-effect`, `loadDetail` 이펙트)이 있으나 이번 변경 이전부터 있던
  것으로 확인(git stash 비교) — 이번 작업 범위 밖이라 별도 손대지 않음.
- 포팅한 `brodReport()`는 원본 그대로라 "09시대 일반 콘텐츠는 삽입되지 않는" 것으로 보이는
  조건(`Integer.parseInt(brodTime.substring(0,2)) >= 10`)이 남아있음 — 레거시 원본에도 동일하게
  있던 조건이라 의도된 동작인지 버그인지 불명확. 실제 편성표생성 결과를 방송표보기로 확인해보고,
  09시 슬롯에 일반 콘텐츠가 비어있는 게 의도와 다르면 별도로 알려주시면 수정하겠음.
- 테스트 계정으로 실제 DB against 동작 검증은 하지 못함(로컬 DB 미연결) — 배포 전 브라우저에서
  실제 클릭 테스트 권장.
