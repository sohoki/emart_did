# 2026-08-03 작업 로그

## 레거시 URL(manageList.do) 대응 화면 확인
**파일:** 없음(코드 변경 없음, 확인/문서화만)

### 변경 내용
- 사용자가 레거시 참고 URL(`http://did.emart.com/backoffice/sub/basicManage/manageList.do`)을
  제시하며 새 프론트에 해당 화면이 있는지 문의. 사용자가 알려준 레거시 소스 경로
  (`E:\works\이마트\DID\EMART_CMS\EMART_CMS\emart_cms3.2.1\src\main\webapp\WEB-INF\jsp\
  backoffice\sub\basicManage\centerList.jsp`)를 열어 확인한 결과, "기초관리(basicManage)"
  영역은 상단 공유 탭 6개(관리자관리/기초코드관리/부서관리/지점관리/메뉴관리(지점)/
  메뉴관리(타입))로 구성되어 있고, `manageList.do`는 그중 **"관리자관리"** 탭이었음
  (지점관리 탭인 `centerList.do`와는 다른 화면)
- "관리자관리"는 이미 `ManagerInfoManagerController`(`/api/backoffice/hr/manager/empList.do`)
  + `ManagerListPage.jsx`(`/backoffice/hr/manager`)로 포팅되어 있음을 확인 — 어제(2026-07-28)
  이 컨트롤러의 ClassCastException 버그를 이미 고쳤으므로, 재시작 후 정상 동작할 것으로 예상
- 레거시의 6탭 공유 상단 네비게이션 UI는 새 프론트에 아직 없음(각 페이지가 독립 라우트로만
  존재)을 확인하고 사용자에게 문의 → **지금은 보류, 각 페이지 독립 이동으로 충분**하다고 결정

### 이슈 / 개선사항
- 6탭 공유 네비게이션 UI 신규 구축은 보류 상태로 남겨둠(필요 시 후속 요청)
- 레거시 소스 참고 경로 확인됨: `E:\works\이마트\DID\EMART_CMS\EMART_CMS\emart_cms3.2.1\
  src\main\webapp\WEB-INF\jsp\backoffice\sub\basicManage\` — 향후 "기초관리" 영역 다른 탭
  (기초코드관리/부서관리/메뉴관리 등) 작업 시 이 폴더의 JSP를 참고할 것

## 관리자 공통 레이아웃(사이드바/헤더/즐겨찾기) 실제 적용 + Manager 프로필 수정/비밀번호 변경 CRUD 신규
**파일:** 아래 "변경 파일" 참고

### 배경
사용자가 다른 어드민 시스템("에이텐시스템") 스크린샷을 참고로 제시하며 did_emart 프론트에도
같은 형태(다크 사이드바+헤더+즐겨찾기 탭바)의 레이아웃과 `routes` 폴더 구성을 요청함. 조사 결과
`layouts/AppLayout.jsx`, `components/Layout/{Header,SideBar,Footer,UserBookmarks,AuthHeader}.jsx`,
`contexts/ThemeContext.jsx`가 이미 존재했으나(front_common 공유 템플릿에서 복사된 것으로 추정)
`App.jsx`에 전혀 연결돼 있지 않았고, `AppLayout.jsx`가 참조하는 프로필 수정 모달 4개
(`ManagerFormModal`/`ManagerPwdModal`/`VendorUserFormModal`/`VendorPwdModal`) 중 Vendor용 2개는
did_emart에 없는 도메인, Manager용 2개는 미구현 상태였음. 사용자 확인 후: (1) 사이드바는 DB
메뉴 테이블(`menuNoLeft.do`) 그대로 연동, (2) Vendor 로직은 완전 제거하고 Manager 모달만 신규
제작, (3) Footer 문구는 이마트 DID 프로젝트에 맞게 수정하기로 결정. Manager 모달을 만들려다
보니 `ManagerInfoManagerController`에 리스트 조회 API만 있고 등록/수정/삭제/idCheck/비밀번호
변경 API 자체가 없었음을 발견 — 사용자 확인 후 `basic/backend` 레퍼런스를 참고해 함께 이식함.

### 변경 내용 — 백엔드 (Manager CRUD 신규)
- `uat/hri/models/dto/{ManagerInfoReqDto,ManagerInfoResDto}.java`(신규) — did_emart
  `tb_managerinfo` 스키마 기준(`EgovLoginMapper.xml`/`05_alter_tb_managerinfo.sql`로 실존 확인된
  컬럼만 사용: `role_gubun`/`manager_position`/`manager_pic`/`center_id`/`password_hint`/
  `password_cnsr` 등. `systemcode_usecode`/`update_password`는 실제 컬럼이 아님을 확인하고 제외)
- `uat/hri/mapper/ManagerInfoManagerMapper.java`+`.xml` — 상세조회/idCheck/비밀번호체크/등록/수정/
  비밀번호변경/상태변경/사용유무변경/삭제 쿼리 추가(Postgres 문법)
- `uat/hri/service/ManagerInfoManagerService.java`, `uat/hri/web/ManagerInfoManagerController.java`
  — `GET /{managerId}.do`, `POST /managerUpdate.do`, `DELETE /{managerId}.do`,
  `GET /idCheck/{managerId}.do`, `POST /passChangeCheck.do`, `POST /passChange.do`,
  `GET /StateChange/{managerId}.do`, `GET /useyn/{managerId}.do` 추가
  (기존 `POST /empList.do` 리스트 조회는 유지)
- `mapper/config/postgresql-config.xml` — `ManagerInfoReqDto`/`ManagerInfoResDto` typeAlias 추가
- **비밀번호는 현재 평문으로 저장/비교**(로그인이 아직 평문 비교 중이라 정책상 통일 —
  `EgovLoginService.actionLogin()`의 해시 호출도 동일 이유로 주석 처리돼 있음). 해싱은
  사용자 지시대로 프로젝트 맨 마지막 단계에서 `07_rehash_manager_password.sql`과 함께 일괄 적용 예정

### 변경 내용 — 프론트엔드
- `layouts/AppLayout.jsx` — Vendor 관련 state/핸들러/모달 렌더 전부 제거. 프로필 수정 시 기존
  비밀번호(해시)를 폼에 채워 화면에 노출/재전송하던 부분을 빈 값으로 변경(보안). `sweetalert2`
  직접 import → 프로젝트 표준인 `@/lib/swal.js`로 정리
- `pages/backoffice/HrInfo/components/{ManagerFormModal,ManagerPwdModal}.jsx`(신규) — 헤더
  프로필 드롭다운에서 여는 "내 정보 수정"/"비밀번호 변경" 모달. front_common의 동명 파일을
  참고하되, 이 화면은 항상 `mode: 'Edt'`(본인 정보 수정)만 다루므로 부서/권한/상태/사진업로드
  등 관리자-등록용 필드는 이번 범위에서 제외(다른 관리자를 등록/수정하는 화면은 아직 없음)
- `components/Layout/Footer.jsx` — 다른 프로젝트("에이텐시스템") 실제 상호/주소/전화번호 하드코딩
  → "이마트 DID 통합관리시스템"으로 교체
- `components/Common/IosSwitch.jsx` — 이미 존재했으나 docblock 주석이 인코딩 깨짐 상태였음 → 정리
- `routes/routes.jsx`(신규) — AppLayout 하위에서 렌더링되는 14개 화면의 경로↔컴포넌트 매핑을
  한 곳으로 모음(사이드바 노출 여부는 DB 메뉴 테이블이 별도로 결정)
- `App.jsx` — 플랫 라우트 나열 방식을 버리고 `routes/routes.jsx` 배열을 매핑, `/backoffice/*`
  전체를 `<AppLayout>` 레이아웃 라우트로 감쌈(`/login`은 레이아웃 밖 유지)
- `main.jsx` — `ThemeProvider`(다크모드 컨텍스트)와 `style/DarkMode.css`를 처음으로 전역 연결
  (기존엔 어디서도 import되지 않아 다크모드 토글 버튼이 있어도 시각적 효과가 전혀 없었음)
- `index.css` — Vite 기본 템플릿에서 물려받은 `#root { width:1126px; max-width:100%; margin:0 auto;
  text-align:center; border-inline:1px solid ... }` 제약 제거. 전체화면 사이드바+헤더 레이아웃이
  중앙 1126px 폭으로 잘려 보이던 원인이었음(로그인 페이지는 자체 `.login-center`로 폭 100%를
  쓰고 있어 영향 없음 확인)
- 로고 이미지 참조 5곳(`Header`/`AuthHeader`/`LoginForm`/`PwdFindForm`/`IdFindForm`)을
  `logo_black.svg` → `emartLogo.png`로 교체(요청)

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities (기존 비밀번호 해시 미노출 처리)
- [ ] Performance concerns
- [ ] Test coverage (백엔드 `./gradlew clean compileJava`, 프론트 `npm run build` 정상 확인 완료.
      실서버 기동 후 로그인 → 사이드바/즐겨찾기/프로필 모달 실제 동작 확인은 아직 못 함)

### 이슈 / 개선사항
- 사이드바가 실제로 항목을 보여주려면 DB `COMTNMENUINFO`/`COMTNPROGRMLIST`/`COMTNMENUCREATDTLS`에
  did_emart 14개 화면에 대한 메뉴 행이 등록돼 있어야 함(이번 세션에서 DB 조회 도구가 없어 실제
  등록 여부 미확인) — 비어 있으면 사이드바가 빈 화면으로 보일 수 있음. 메뉴 등록 API
  (`MENU_SAVE` 등)는 이미 있으나 메뉴관리 화면은 아직 프론트에 없어 후속 작업 필요
- `ManagerListPage.jsx`는 여전히 조회 전용(다른 관리자를 등록/수정/삭제하는 관리자용 CRUD 화면은
  이번 범위 밖) — 이번에 만든 Manager 모달은 "본인 정보 수정" 전용
- `AppLayout.jsx`의 `handleLogout`이 `partId === 'VENDOR'`를 비교하는 분기가 남아있음(죽은
  코드지만 에러는 아님, Vendor 도메인 자체가 없으므로 항상 `/login`으로만 이동) — 필요시 후속 정리

## 로그인 직후 "관리자 리스트 ↔ 로그인" 무한 루프 긴급 수정
**파일:** `bas/mark/**`(신규), `bas/menu/mapper/MenuInfoManageMapper.java`

### 증상 및 원인 분석
사용자가 로그인 후 관리자 리스트로 갔다가 곧바로 다시 로그인 화면으로 튕기는 무한 루프를
보고함. 실행 중인 서버에 curl로 실제 JWT를 발급받아 재현 테스트한 결과 원인 확정:

1. **직접 원인**: 방금 연결한 `UserBookmarks.jsx`가 호출하는
   `POST /api/backoffice/bas/books/bookmarksListAjax.do`에 **컨트롤러 자체가 아예 없었음**.
   유효한 토큰으로 호출해도 HTTP 401(`resultCode 403, "인가된 사용자가 아닙니다"`,
   `JwtAuthenticationEntryPoint`가 찍은 응답)이 나옴 — 핸들러 없음 → Spring이 `/error`로
   내부 forward → 그 forward 요청엔 Authorization 헤더가 없어서 Spring Security가
   미인증으로 판단하는 전형적 패턴으로 확인
   (참고: `empList.do`는 유효 토큰으로 정상 200 확인, `menuNoLeft.do`는 후술할 별개 버그로 500)
2. **루프가 되는 이유**: 프론트(`fn-ajax-fetch.jsx`)는 401을 받으면 refreshToken으로 재발급을
   시도하는데, 현재 Redis가 꺼져 있어서 로그인 시점에 refreshToken이 Redis에 저장되지 못했고
   (7/28 세션에서 이미 안전 처리해둔 부분), 리프레시 검증(`JwtVerification.isVerificationRefresh`
   → `EgovJwtTokenUtil.isRefreshToken` → `RedisUtil.isKeyNullCheck`)도 Redis 연결 실패 시
   "리프레시 토큰 없음(=거부)"로 해석되게 되어 있어 재발급이 항상 실패함 → `window.location.href
   = '/login'`로 강제 이동 → `LoginPage`는 `accessToken` 쿠키가 아직 남아있으니 즉시 다시
   관리자 리스트로 이동 → `AppLayout` 재마운트 → 즐겨찾기 재호출 → 다시 401 → 무한 반복
3. **부가 발견(루프의 직접 원인은 아님)**: `menuNoLeft.do`(사이드바 메뉴 조회)도 항상 500 에러가
   나고 있었음. `MenuInfoManageMapper.selectMainMenuLeft(String roleId, String url)`가
   `@Param` 없이 파라미터 2개를 받는데, XML은 `#{roleId}`로만 참조 — `-parameters` 컴파일
   옵션도 없어 MyBatis가 파라미터명을 못 찾고 `BindingException`을 던지던, 이번 세션과 무관한
   기존 버그(사이드바를 실제로 렌더링해본 게 이번이 처음이라 지금까지 드러나지 않았을 뿐)

### 수정 내용
- `bas/mark/models/dto/BookMarksInfoReqDto.java`, `bas/mark/mapper/BookMarksInfoManageMapper.java`
  +`.xml`, `bas/mark/service/BookMarksInfoManageService.java`,
  `bas/mark/web/BookMarksInfoManageController.java`(전부 신규) — basic/backend의 동명 패키지를
  참조해 이식(`POST /bookmarksListAjax.do`, `/updateBookMarksInfo.do`, `/updateBookMarksOrder.do`,
  `/deleteBookMarksInfo.do`). 원본은 Redis 캐싱을 포함하지만, 지금 환경은 Redis가 없고 다른
  서비스들도 전부 DB 직접조회로 폴백 중이라 새 Redis 의존을 추가하지 않고 단순하게 이식함
- `mapper/config/postgresql-config.xml` — `BookMarksInfoReqDto` typeAlias 추가
- `bas/menu/mapper/MenuInfoManageMapper.java` — `selectMainMenuLeft(String roleId, String url)`에
  `@Param("roleId")`/`@Param("url")` 추가

### 리뷰 체크리스트 결과
- [x] Logic errors & edge cases (무한 루프 재현 및 원인 확정 후 수정)
- [x] Security vulnerabilities
- [ ] Test coverage (`./gradlew compileJava` 확인 완료. **사용자가 직접 띄운 서버 재시작 필요**
      — 재시작 후 실제 로그인 재확인 필요)

### 이슈 / 개선사항
- `TB_BOOKMARKS` 테이블이 실제 DB에 존재하는지 이번 세션에서 DB 조회 도구가 없어 미확인.
  없다면 즐겨찾기 조회가 500으로 실패하겠지만(이미 같은 방식으로 동작 중인 `menuNoLeft.do`와
  동일한 안전한 실패 모드), 최소한 이번 루프의 원인이었던 401 무한 리다이렉트는 발생하지 않음
- Redis가 계속 꺼져 있는 한 리프레시 토큰 재발급 기능 자체는 계속 동작 안 함(의도된 성능 저하) —
  실제 Redis 서버 준비는 여전히 별도 후속 과제

## MyBatis SQL 디버그 로그가 안 찍히는 문제 확인
**파일:** `backend/src/main/resources/application.yml`

### 원인
사용자가 IntelliJ Run Configuration의 환경변수로 `SPRING_PROFILES_ACTIVE`를 `prod`(또는
`server`)로 지정해서 로컬에서 실행 중이었음. `logging.level.com.common.backoffice: debug`/
`egovframework.let: debug`(MyBatis SQL 로그용, 7/28에 `local` 프로파일 블록에만 추가했었음)가
`prod`/`server` 프로파일 블록에는 없어서 SQL 로그가 안 보였던 것 — 실행 중인 프로세스
커맨드라인(`Get-CimInstance Win32_Process`)을 직접 확인해 실행 자체는 정상이고 프로파일
설정 차이가 원인임을 확정함

### 수정 내용
- `application.yml`의 `prod`/`server` 프로파일 블록에도 `local`과 동일하게
  `com.common.backoffice: debug`/`egovframework.let: debug` 추가

### 이슈 / 개선사항
- 서버 재시작 필요(설정 파일 변경이라 재시작해야 반영됨)
- `prod`/`server` 프로파일은 `didadmin` 계정으로 DB에 붙고(local은 `root`), 원래 SQL 디버그
  로그가 없었던 건 "운영 환경에서는 조용히" 의도였을 수 있음 — 실제 운영 배포 시에는 이번에
  추가한 두 줄을 다시 빼는 것을 고려할 것(지금은 사용자가 로컬에서 prod 프로파일로 테스트
  중이라 켜둠)

## SideBar 무한 로딩 버그 수정 + routes/ProtectedRoute 아키텍처 전환 + 기초관리 4개 화면 이식
**파일:** 아래 "변경 내용" 참고

### SideBar가 계속 스켈레톤(로딩 상태)에 갇혀 있던 버그
`hooks/use-sidebar.js`가 `didInit` ref로 effect를 "한 번만 실행"하도록 막고 있었는데, 이 패턴이
React StrictMode(개발모드 mount→cleanup→mount 이중 호출)에서 깨짐: 첫 실행의 cleanup이 자신의
`active` 플래그를 `false`로 만들고, `didInit` 가드 때문에 두 번째 실행은 새 fetch를 시작하지
못해 — 첫 fetch 응답이 도착해도 `active===false`라 `setItems`/`setLoading(false)`가 영원히
호출되지 않고 로딩 상태에 고착됐음(화면상 사이드바가 거의 안 보이는 것처럼 보였던 원인).
`didInit` 가드를 제거해서 각 effect 실행이 자기 own `active` 클로저를 갖도록 수정.
덧붙여 `ManagerListPage.jsx`가 AppLayout 연결 전(독립 페이지 시절) 만든 자체 헤더/로그아웃
버튼이 그대로 남아 AppLayout 헤더와 중복 표시되던 것도 함께 정리함.

### routes/ProtectedRoute 아키텍처로 전환
사용자가 `E:\dev\front\common\front_common\src\routes\index.jsx`/`ProtectedRoute.jsx`를
왜 안 쓰냐고 지적 — 지금까지 만든 `routes/routes.jsx`(단순 배열 + App.jsx에서 수동 매핑)
대신, front_common의 실제 라우팅 아키텍처를 그대로 적용함:
- `routes/ProtectedRoute.jsx`(신규, front_common에서 그대로 포팅) — 페이지 진입 전
  `POST /jwtAuthAPI`로 토큰을 서버에서 실제로 검증(단순 쿠키 존재 여부 체크가 아님),
  실패 시 refreshToken으로 재시도 후 그래도 실패하면 `/login`으로 리다이렉트
- `routes/index.jsx`(신규) — `App.jsx`를 대체하는 실제 라우터. `Suspense` + `lazy()`로 전
  페이지 코드 스플리팅(기존엔 번들 하나에 다 들어있었음), `ScrollToTop`, 인증 불필요 라우트는
  `AppTopDownLayout`으로 분리
- `App.jsx` — `<RouterConfig />` 하나만 렌더링하도록 정리, 구 `routes/routes.jsx` 삭제
- **백엔드**: `POST /jwtAuthAPI`가 did_emart에 없었음(front_common엔 `EgovSiteManagerApiController`로
  존재) → `EgovLoginApiController.java`에 최소 구현으로 추가(이 메서드까지 도달했다는 것 자체가
  `JwtAuthenticationFilter`를 통과했다는 뜻이라 별도 로직 없이 성공만 반환)

### 기초관리 4개 화면(코드/메뉴/권한/프로그램 관리) 신규 이식
사용자가 `front_common`의 `pages/backoffice/Basic/{CodeInfo,MenuInfo,RoleInfo,ProgrameInfo}.jsx`를
"기존 것"으로 재사용하고 싶다고 확인 → 조사 결과 이 4개 화면이 호출하는 URL 상수
(`CODEID_LIST`/`MENU_LIST`/`ROLE_LIST`/`PROGRAM_LIST` 등)와 그 하위 hooks/컴포넌트
(`use-common-delete`/`use-form`/`use-menu-tree`/`use-grid-infinite`/`MasterDetailGrid`/
`Select`/`IosSwitch`/`use-id-check`/`use-file-upload`/`use-pwd-validate`/`MarkdownEditor` 등)가
**전부 이미 did_emart front에 존재**했고, 대응하는 **백엔드 컨트롤러도 이미 이번 세션 이전부터
존재**했음(`EgovCcmCmmnCodeManageController` 등) — 페이지 파일 자체만 did_emart로 복사가 안 돼
있던 상태. 코드 수정 없이 그대로 복사:
- `pages/backoffice/Basic/{CodeInfo,MenuInfo,RoleInfo,ProgrameInfo}.jsx`
- `pages/backoffice/Basic/components/{CodeDetailCellRenderer,CodeFormModal,DetailCodeFormModal,
  ProgramChoiceModal,RoleFormModal,MenuSelectForm,ProgramFormModal}.jsx`
- 라우트: `/sub/bas/codeInfo`, `/sub/bas/menuInfo`, `/sub/bas/roleInfo`, `/sub/bas/programeInfo`
  (front_common 원본과 동일 경로 유지 — 공유 인프라인 메뉴/프로그램 DB 시드가 이 경로 규칙을
  전제할 가능성이 높아서 임의로 바꾸지 않음)

### 검증
- `./gradlew compileJava` 정상, `npm run build` 정상(코드 스플리팅 적용되어 청크가 페이지별로
  분리됨 — 이전엔 단일 1.6MB 번들 경고가 있었음)

### 이슈 / 개선사항
- `routes/index.jsx`에 `/404`, `/500`, `/error`, `NotFound`, `ErrorPage`, `BookmarksInfo` 등은
  포함하지 않음(front_common엔 있으나 did_emart엔 해당 페이지 파일이 없음) — 미정의 경로는
  일단 `/login`으로 리다이렉트. 필요시 후속으로 추가
- 서버 재시작 필요(`/jwtAuthAPI` 백엔드 신규 엔드포인트 반영)
