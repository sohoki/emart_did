# 2026-08-11 작업 로그

## 09:20 — 방송 관리: 음원 파일 전용 관리 화면 신규 추가(BrodMusicFileListPage)
**파일:** `front/src/pages/backoffice/BrodManage/BrodMusicFileListPage.jsx`(신규),
`front/src/pages/backoffice/ConManage/components/ContentFileUploadModal.jsx`,
`front/src/routes/index.jsx`

### 배경
레거시 `playContentList.jsp`("음원파일관리" 탭)에 대응하는 화면이 뭔지 질문받아
확인해보니, 해당 기능은 콘텐츠 파일 라이브러리(`/api/backoffice/sub/conManage/file/*`,
`ContentFileLibraryPage.jsx`)로 통합되어 있었음(레거시 4개 조회 API를 `/list.do`
하나로 합친 리팩토링). 다만 음원 담당자는 음원 관련 메뉴만 보므로, 이미지/영상까지
섞인 통합 라이브러리 화면 대신 음원만 다루는 전용 화면이 별도로 필요하다는 요청.

### 변경 내용
- `BrodMusicFileListPage.jsx`(신규): `ContentFileLibraryPage.jsx`와 동일한 API
  (`CON_FILE_LIST/USEYN_BULK/CONN_CHECK/INFO`)를 쓰되 `mediaType`을 `MUSIC`으로 고정,
  미디어 종류 선택 UI 자체를 없애고 음원만 다루는 카드형 갤러리 화면으로 구성
  (썸네일 갤러리라 `content-table` 영역은 `AppAgGrid` 대신 카드 그리드를 쓰는
  `ContentFileLibraryPage`와 동일한 골격 예외 케이스).
- `ContentFileUploadModal.jsx`에 선택 props(`accept`/`title`/`dragText`) 추가해서
  재사용 — 안 넘기면 기존(`ContentFileLibraryPage`) 동작 그대로, 음원 화면에서는
  `accept={{ 'audio/mpeg': ['.mp3'], 'audio/wav': ['.wav'], 'audio/midi': ['.mid'] }}`로
  드롭존 파일 선택을 음원 확장자로 제한(서버는 업로드 시 mediaType을 저장 안 하고
  조회 시 확장자 기준 자동 판별이라 백엔드 변경은 불필요 —
  `ContentFileManagerMapper.xml`에서 mp3/wav/mid만 MUSIC으로 분류하는 걸 확인).
- 라우트 추가: `/backoffice/sub/brodManage/musicFile`.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 사이드바 메뉴 등록은 DB(`COMTNMENUINFO`) 기반이라 이 환경에서 직접 못 함 — 메뉴
  관리 화면에서 수동 등록하거나 별도로 DB 반영 필요.
- 백엔드 API 변경 없음(`CON_FILE_*` 기존 엔드포인트 재사용). 백엔드/DB가 없어 실제
  화면 재확인은 못 함.
- 사용자가 "나머지 이야기"(추가 요구사항)를 이어서 전달 예정 — 후속 커밋에서 반영.

---

## 09:35 — BrodMusicFileListPage에 미리듣기(인라인 재생) 기능 추가
**파일:** `front/src/pages/backoffice/BrodManage/BrodMusicFileListPage.jsx`

### 변경 내용
- 카드 썸네일 영역을 재생/정지 아이콘이 있는 버튼으로 바꿔서 클릭하면 카드 안에
  `<audio controls autoPlay>` 플레이어가 펼쳐지도록 함(재생경로는
  `ContentFileLibraryPage`의 `fileThumbSrc`와 동일한 규칙 —
  `config.REACT_APP_IMG_URL + fileStreCours + streFileNm`).
- 한 번에 하나만 재생되도록 `previewId` 상태로 관리(다른 카드 미리듣기 클릭 시 이전
  것 자동 접힘, 재생 끝나면(`onEnded`) 자동으로 접힘).
- 미리듣기 중인 파일을 삭제하면 `previewId`도 같이 초기화해서 끊긴 플레이어가 안
  남게 처리.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 백엔드/DB가 없어 실제 재생 경로(`config.REACT_APP_IMG_URL` 조합)가 맞는지 실 화면
  에서 재생까지는 확인 못 함 — 다른 이미지 썸네일에서 이미 쓰던 것과 동일한 경로
  조합 규칙이라 크게 다르지 않을 것으로 예상.

---

## 10:40 — 방송(음원) 콘텐츠 상세(편성) 화면 신규 추가(BrodContentDetailPage)
**파일:** `front/src/pages/backoffice/BrodManage/BrodContentDetailPage.jsx`(신규),
`front/src/pages/backoffice/BrodManage/components/BrodContentDetailFormModal.jsx`(신규),
`front/src/pages/backoffice/BrodManage/components/BrodContentCopyModal.jsx`(신규),
`front/src/pages/backoffice/BrodManage/BrodContentListPage.jsx`,
`front/src/constants/URL.jsx`, `front/src/routes/index.jsx`

### 배경
`/backoffice/sub/brodManage/content` 목록에서 방송명을 눌러도 레거시
`brodContentView.jsp`(콘텐츠 정보 + 시간대별 편성 + 특정방송 목록 + 목록/수정/
콘텐츠등록/특정방송등록/음원콘텐츠복사/배치적용/편성표생성/방송표보기 액션)에
대응하는 상세 화면이 없어서, 그 화면을 참고해 새로 만들어 달라는 요청.

### 사전 조사
- 백엔드는 이미 `BrodContentInfoManageController`(`/api/backoffice/sub/brodManage/content/*`)
  와 `BrodContentDetailManageController`(`/api/backoffice/sub/brodManage/contentDetail/*`)로
  상세조회/시간대별 편성 조회/편성 등록·수정·삭제/편성 복사/배치 적용까지 대부분
  이식되어 있었음.
- 단, 두 컨트롤러의 자바 주석에 **"편성표생성/방송표보기"(`ContentBrodConfirm.do`/
  `ContentBrodReport.do`/`ContentBrodExcel.do`) 클러스터는 이번 백엔드 이관 범위에서
  의도적으로 제외**되어 있음을 확인함 — 존재하지 않는 유틸(`UniSelectInfoManageService`,
  동적 SQL 조립 방식이라 SQL 인젝션 위험)과 없는 DB 함수(`FN_CENTERBRODINFO`)에 의존해서,
  당시 사용자 확인 하에 범위 제외로 결정된 사항. 이번 작업에서도 그대로 존중해서
  이 두 버튼은 "준비 중" 안내로만 남겨둠(`ContentFileLibraryPage`의 "미디어 파일정리"
  버튼과 동일한 기존 처리 방식).

### 변경 내용
- **BrodContentDetailPage(신규)**: `/backoffice/sub/brodManage/content/detail?brodCode=`
  - 콘텐츠 정보(콘텐츠코드/명/이벤트유무/사용유무/반복재생간격/기본음원 "리스트
    미리보기" — `BASIC_BROD_FILE_LIST` 재사용)
  - 시간대별(10분 단위, `codeDc`/10칸) 편성 그리드 — 슬롯별 `BROD_CONTENT_TIME_LIST`
    조회, 트랙별 삭제, 슬롯 총재생시간(mm:ss) 표시
  - 특정방송(기념일) 목록 — 상세조회 응답에 포함된 `brodAnniver`를 그대로 사용,
    등록/수정은 기존 `BrodAnniversaryFormModal` 재사용, 삭제는 기존 `BROD_ANNIVER_DELETE`
  - 콘텐츠(방송) 정보 수정은 기존 `BrodContentFormModal` 재사용
  - "콘텐츠 등록"(시간대별 음원 편성 등록/수정) — 신규 `BrodContentDetailFormModal`.
    레거시의 "N개를 M분 간격으로 자동 배치"(`timeCheck.do`) 기능은 이식하지 않고,
    시간대 1개를 직접 골라 등록/수정하는 단순한 형태로 축소함(명시적 축소, 필요하면
    후속 작업)
  - "음원 콘텐츠 복사" — 신규 `BrodContentCopyModal`. 다른 방송의 편성을 통째로
    복사(기존 편성은 삭제 후 대체)하기 전에 명시적 경고 확인창을 거치도록 함
  - "배치 적용" → `BROD_CONTENT_SCHEDULE_CONFIRM`(POST, `@RequestParam`이라 body
    대신 쿼리스트링으로 호출)
  - "편성표생성"/"방송표보기" → 백엔드 미구현이라 "준비 중" 안내만
  - "목록"/"수정" 버튼, 목록 화면과 동일한 골격(`content-header`) 사용
- **BrodContentListPage.jsx**: 그리드의 방송명 링크가 기존엔 바로 수정 모달을
  열었는데, 레거시(목록→상세뷰→수정) 흐름에 맞춰 상세 페이지로 이동하도록 변경.
  목록의 등록 모달은 "신규 등록" 전용으로 축소(기존 방송 수정은 상세 페이지의
  "수정" 버튼에서).
- `URL.jsx`에 `BROD_CONTENT_DETAIL_FORM/UPDATE/INFO/COPY_COMBO/COPY_INSERT` 5개
  상수 추가(`contentDetail/*` 네임스페이스, 기존 백엔드 엔드포인트에 매핑만 함 —
  백엔드 신규 작업 없음).
- 라우트 추가: `/backoffice/sub/brodManage/content/detail`.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- **범위 축소 2건 명시**: (1) 시간대 자동 다건 배치(`contentDetail/timeCheck.do`)
  미이식 — 수동으로 시간대 1개씩 등록/수정만 가능. (2) 동일 시간대 중복 편성 검사
  (`contentDetail/checkCnt.do`)·음원 파일 실시간 검색(`contentDetail/fileSearch.do`)
  미이식 — 파일은 콤보 목록에서 고르는 방식만 지원.
  둘 다 백엔드 API 자체는 이미 있으니 필요하면 다음에 이어서 붙일 수 있음.
- "편성표생성"/"방송표보기"는 애초에 백엔드가 없어서(이전 작업에서 SQL 인젝션
  위험으로 의도적 제외) 준비 중 상태 유지 — 필요하면 별도로 그 두 기능부터
  안전하게 다시 설계해야 함.
- 백엔드/DB가 없는 환경이라 실제 화면 동작(특히 응답 JSON 중첩 구조 — `Map` 오버로드
  는 단일 계층 `result.xxx`, `(data,msg)` 오버로드는 `result.result`/`result.resultList`
  로 서로 다름을 컨트롤러 코드 기준으로 추정해서 맞춤)은 재확인 못 함. 실 화면에서
  상세 조회/편성 등록/삭제/복사/배치적용을 한 번씩 확인 부탁드립니다.

---

## 11:05 — BrodContentListPage 그리드에 "수정" 버튼 복원
**파일:** `front/src/pages/backoffice/BrodManage/BrodContentListPage.jsx`

### 배경
방송명 클릭이 상세 페이지로 이동하도록 바뀌면서(직전 작업), 목록에서 방송 기본
정보(방송명/재생간격/기초방송/사용유무)만 빠르게 고치는 경로가 없어졌다는 요청.

### 변경 내용
- 그리드에 "수정" 컬럼 버튼 추가 — 클릭 시 `BrodContentFormModal`을 Edt 모드로 바로
  염(상세 페이지로 안 가도 됨). `handleOpenBrodModal`이 다시 `row` 인자를 받아
  등록/수정 겸용으로 동작하도록 원복(`BROD_CONTENT_FORM_DATA`를 mode에 맞게 호출).
- 방송명 링크는 그대로 상세 페이지(`BrodContentDetailPage`) 이동 유지 — 편성/특정방송
  관리는 계속 상세 페이지에서.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 백엔드 API 변경 없음. 백엔드/DB가 없어 실제 화면 재확인은 못 함.

---

## 11:30 — 콘텐츠 등록 모달에 운영스케줄(자동 시간대 배정)·정렬순서 추가
**파일:** `front/src/pages/backoffice/BrodManage/components/BrodContentDetailFormModal.jsx`,
`front/src/pages/backoffice/BrodManage/BrodContentDetailPage.jsx`, `front/src/constants/URL.jsx`

### 배경
직전 작업에서 시간대 1개를 직접 고르는 형태로 축소했던 "콘텐츠 등록" 모달에 대해,
실제 레거시 화면(`ContentReg.do` 팝업, 로컬 JSP 소스는 없고 스크린샷만 확보)을
보여주며 빠진 두 부분을 채워달라는 요청:
1. 콘텐츠명 — 텍스트 검색 + 검색 버튼 + 결과 select (레거시 스크린샷과 동일)
2. 운영스케줄 — "OOO분 부터 OOO 간격으로 N번 입력" (자동 시간대 배정)
3. 정렬순서 select

### 사전 조사
- 백엔드에 이미 `contentDetail/fileSearch.do`(GET, `orgFileNm` 키워드로 MUSIC 파일
  검색)와 `contentDetail/timeCheck.do`(POST, `timeInterval`/`contentInsertInterval`/
  `timeIntervalInsertCnt`로 겹치지 않는 시간대를 자동 계산해 `timeIntervalResult`
  반환)가 이미 구현돼 있어서 그대로 연결만 하면 됐음(백엔드 변경 없음).
- `BrodContentDetail.java`/insert 매퍼에 `contentOrder` 필드가 있고 서버가 기본값을
  안 채워주는 걸 확인 — "정렬순서"는 프론트에서 직접 받아 보내야 하는 값이었음.

### 변경 내용
- `BrodContentDetailFormModal.jsx`: 콘텐츠명 입력을 검색어+검색버튼+select 조합으로
  교체(`onFileSearch` prop으로 부모의 `contentDetail/fileSearch.do` 호출을 위임 —
  `ScheduleFormModal`의 `onGroupSearch`/`onContentSearch`와 동일한 패턴). 등록(Ins)
  모드에는 "OOO분 부터 OOO 간격으로 N번 입력" 3단 select(운영스케줄)를 추가하고,
  수정(Edt) 모드는 기존처럼 단일 시간대 select 유지(자동배정 재실행 없이 확정된
  1건만 직접 고침). 정렬순서 select(1~10) 추가.
- `BrodContentDetailPage.jsx`: `handleFileSearch`(파일 검색) 추가. `handleDetailSubmit`을
  Ins/Edt로 분기 — Ins는 `contentDetail/timeCheck.do`로 자동 시간대를 먼저 확정하고
  (빈 시간대 없으면 경고 후 중단), 그 결과(`timeIntervalResult`)로
  `contentDetail/update.do`를 호출. Edt는 기존처럼 단일 `intervalSection`으로 바로
  업데이트. 두 경로 다 `contentOrder`를 함께 전송.
- `URL.jsx`에 `BROD_CONTENT_DETAIL_FILE_SEARCH`/`BROD_CONTENT_DETAIL_TIME_CHECK`
  2개 상수 추가(기존 백엔드 엔드포인트 매핑만, 신규 백엔드 작업 없음).

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- "정렬순서" select의 정확한 의미/범위(레거시 JSP 소스가 로컬에 없어 스크린샷만
  보고 추정 — 같은 시간대에 여러 곡이 겹칠 때 재생 순서로 추정)는 실 화면에서
  값 범위(1~10 고정)가 충분한지 확인 필요.
- 백엔드/DB가 없어 `timeCheck.do` 응답 파싱(`result.result` 문자열)과 실제 자동
  배정 동작은 재확인 못 함.

---

## 12:10 — 특정방송 모달 미표시 버그 수정 + 필드 구성 전면 수정
**파일:** `front/src/pages/backoffice/BrodManage/components/BrodAnniversaryFormModal.jsx`,
`front/src/pages/backoffice/BrodManage/BrodContentDetailPage.jsx`,
`front/src/pages/backoffice/BrodManage/BrodAnniversaryListPage.jsx`, `front/src/constants/URL.jsx`

### 배경
상세 화면 하단 특정방송 목록에서 이름(예: "안전수칙 수정1")을 눌러도 모달이 전혀
안 뜬다는 제보. 이어서 레거시 `ContentSpReg.do`(특정방송 등록/수정 팝업) 스크린샷을
보여주며 모달 필드 구성 자체도 틀렸다는 지적.

### 원인 1 — 모달이 안 뜸(진짜 버그)
`handleOpenAnniverModal`이 상세 조회 응답이 비어 있으면(`if (!obj) return;`) 아무
에러 메시지도 없이 조용히 함수만 종료하고 있었음 — 실패해도 사용자는 "클릭했는데
아무 일도 안 일어남"으로만 보임. 게다가 폼 상태(`EMPTY_ANNIVER_FORM`)가
`anniverStartday`/`anniverEndday`(소문자 d)를 쓰고 있었는데, 실제 백엔드
모델(`BrodAnniversary.java`)의 필드명은 `anniverStartDay`/`anniverEndDay`(대문자
D)였음 — 대소문자가 달라 날짜 값이 항상 비어서 저장되는 별개의 버그도 같이 있었음.
`BrodAnniversaryListPage.jsx`의 그리드 컬럼(`field: 'anniverStartday'` 등)도 동일하게
틀려 있어서 시작일/종료일 컬럼이 항상 빈 칸으로 보였을 것.

### 원인 2 — 필드 구성 자체가 실제 화면과 다름
최초에 만들 때는 "방송 기념일 관리" 단순 화면(`BrodAnniversaryListPage`)을 기준으로
만들어서 스케줄명/시작일/종료일/시작시간(HHmm)/재생시간(초) 정도만 있었는데, 실제
`ContentSpReg.do` 팝업은 콘텐츠명(음원파일) 검색, 특정방송여부, 방송시간(시/분),
정렬순서까지 있는 훨씬 자세한 폼이었음. 특히 "재생시간(초)"이라고 잘못 라벨링해서
쓰던 `anniversaryTime` 필드가 실제로는 "방송시간"(HHmm, 시/분을 합친 4자리 문자열)
이었다는 것도 이번에 스크린샷 비교로 확인함(레거시 브로드 상세 화면의 "반복재생"
컬럼 값 1120/1420/1720/2020이 곧 11:20/14:20/17:20/20:20 방송시각과 일치).

### 변경 내용
- `BrodAnniversaryFormModal.jsx` 전면 재작성: 스케줄명 / 콘텐츠명(검색+select,
  `BrodContentDetailFormModal`과 동일한 검색 패턴) / 콘텐츠운영날짜(시작~종료) /
  특정방송여부(select) / 방송시간(시·분 select 2개 → `anniversaryTime` "HHmm"로
  합쳐서 저장) / 정렬순서(select, `anniverOrder` 필드) 로 재구성.
- `BrodContentDetailPage.jsx`/`BrodAnniversaryListPage.jsx` 둘 다: `EMPTY_ANNIVER_FORM`
  필드명을 `anniverStartDay`/`anniverEndDay`로 수정, `atchFileId`/`anniverOrder` 추가.
  `handleOpenAnniverModal`에서 상세 조회 실패 시 에러 Swal을 띄우도록 수정(더는 조용히
  실패 안 함). 특정방송여부 콤보(EMT020)는 `content/copyPopupData.do`로 함께 조회.
  콘텐츠명 검색은 기존 `contentDetail/fileSearch.do`(콘텐츠 등록 모달과 동일 API,
  `BrodAnniversaryListPage`에는 새로 연결)를 재사용.
- `BrodAnniversaryListPage.jsx` 그리드 컬럼 `anniverStartday`/`anniverEndday` →
  `anniverStartDay`/`anniverEndDay`로 수정(빈 칸으로 보이던 시작일/종료일 컬럼도
  같이 고쳐짐).
- `URL.jsx`에 `BROD_CONTENT_REG_POPUP_DATA`(`content/copyPopupData.do`) 추가.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- "특정방송여부"가 간격재생(ANNGUBUN02로 추정)일 때 레거시는 방송시간 UI가 시/분
  단일 select가 아니라 시간(060/120/180/240)+분(000~050) 콤보 조합으로 바뀌는
  것으로 보이는데, 그 상태의 스크린샷이 없어 이번엔 구현하지 않음 — 특정시간재생
  기준(시/분 단일 select, `anniversaryTime`)으로만 구현함. 간격재생을 실제 쓰면
  추가로 봐야 함.
- 백엔드/DB가 없어 실제 화면에서 모달이 뜨는지, 필드 값이 정확히 채워지는지는
  재확인 못 함 — 이번엔 특히 실 화면 확인이 중요합니다(원래 버그가 "조용히
  실패"였던 만큼).
