# 2026-08-10 작업 로그

## 09:40 — 발송 스케줄 관리: 콘텐츠명 그리드 링크 → 콘텐츠 편성 화면 새창 연결
**파일:** `front/src/pages/backoffice/equiManage/ScheduleListPage.jsx`,
`front/src/pages/backoffice/equiManage/ScheduleStatusPage.jsx`(신규, 현재 미사용),
`front/src/routes/index.jsx`

### 변경 내용
- `ScheduleListPage` 그리드의 `콘텐츠명` 컬럼을 링크 버튼으로 변경. 처음엔 스케줄
  읽기전용 상세 페이지(`ScheduleStatusPage`, `/backoffice/sub/equiManage/sch/status`)로
  라우팅했으나, 이후 요청으로 기존 `ContentDetailEditorPage`(콘텐츠 편성 화면)를
  `window.open(..., '_blank')`로 새창 여는 방식으로 최종 변경.
- `ScheduleStatusPage`와 그 라우트는 사용자가 "남겨두기"를 선택해 코드는 남아있지만
  현재 어디서도 링크되지 않는 상태(추후 다른 진입점에서 재사용 가능).
- 백엔드는 기존 `GET /api/backoffice/sub/equiManage/sch/{schCode}/view.do`(이미 구현돼
  있던 엔드포인트)를 그대로 사용, 백엔드 변경 없음.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `ScheduleStatusPage`는 현재 미사용 상태 — 필요 없어지면 정리 대상.

---

## 10:10 — ProgrameInfo 그리드 하단 검은 화면 수정
**파일:** `front/src/pages/backoffice/Basic/ProgrameInfo.jsx`

### 배경
행이 10줄 넘게 늘어나면 그리드 하단에 배경색 없는 검은 영역이 노출된다는 제보.

### 원인
`rowModelType="infinite"`(비동기 블록 단위 로딩)와 `domLayout="autoHeight"`를 함께 써서
그리드 높이 계산이 실제 데이터보다 큰 블록 크기(`cacheBlockSize=20`) 기준으로 잡혀,
남는 영역이 배경 없이 노출됨. 같은 골격을 쓰는 `RoleInfo.jsx`/`ScheduleListPage.jsx`
등은 `domLayout` 없이 고정 `height: 760`을 써서 문제가 없었음.

### 변경 내용
- `domLayout='autoHeight'` 제거, wrapper를 고정 `height: 760`으로 통일.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 이 환경엔 백엔드/DB가 없어 실제 화면 재확인은 못 함(`vite build`만 검증).

---

## 10:35 — 메뉴 관리 등록/수정 폼 input 폰트 크기 수정
**파일:** `front/src/style/MenuInfo.css`(신규), `front/src/pages/backoffice/Basic/MenuInfo.jsx`

### 원인
`index.css`의 전역 `:root { font: 18px/145% ... }`를 Bootstrap `.form-control`/
`.form-select`가 rem 기준으로 그대로 물려받아, 별도 스타일이 없는 `th` 라벨보다
입력창 글자가 훨씬 크게 보임.

### 변경 내용
- `.main_table th`, `.main_table .form-control/.form-select`를 앱 전역 검색폼
  컨벤션인 14px로 통일하는 페이지 전용 CSS 추가.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

---

## 11:00 — CodeInfo/DIdGroupInfo 공용 MasterDetailGrid 하단 검은 화면 수정
**파일:** `front/src/components/Common/MasterDetailGrid.jsx`

### 배경
`/sub/bas/codeInfo`에서 행이 20건 정도 되면 그리드 하단에 검은 화면이 나온다는 제보.
ProgrameInfo와 증상은 같지만 원인 컴포넌트는 다름(이쪽은 `MasterDetailGrid` 공용
컴포넌트, `rowModelType`은 client-side).

### 원인
`domLayout="autoHeight"` + `minHeight:200`만 있고 고정 높이가 없어, 행 수가 많아질 때
높이 계산이 어긋나 하단에 빈 영역이 검게 노출됨.

### 변경 내용
- `domLayout="autoHeight"` 제거, wrapper를 고정 `height: 760`으로 통일(다른 그리드
  화면들과 동일 컨벤션).

### 영향 범위
- `/sub/bas/codeInfo`(코드 관리), `/backoffice/sub/equiManage/did_group`(DID Group
  관리) 둘 다 이 컴포넌트를 공유하므로 함께 수정됨.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 별개로 CodeInfo 화면의 "한글명" 컬럼 값이 자모 분리되어 깨져 보이는 문제 발견 —
  이번 요청 범위 밖이라 미처리, 추후 확인 필요.

---

## 11:25 — 상세 코드 등록 모달 버튼 폰트 크기 수정
**파일:** `front/src/pages/backoffice/Basic/components/DetailCodeFormModal.jsx`

### 원인
이 모달의 `닫기`/`저장` 버튼만 공용 `.modal-footer`(common.css의 `font-size:14px`
규칙 포함) 컨벤션을 안 쓰고 순수 Bootstrap `.btn`(`btn-secondary`/`btn-primary`)만
써서 전역 루트 폰트(18px)를 그대로 물려받아 라벨보다 크게 보임. 같은 폴더의
`CodeFormModal.jsx`는 이미 `.modal-footer` 구조라 정상이었음.

### 변경 내용
- 하단 버튼 영역을 `CodeFormModal.jsx`와 동일한 `.modal-footer`/`.modal-footer__right`
  + `btn-cancel`/`btn btn-primary btn-action__blue` 구조로 통일.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

---

## 11:50 — ContentFileLibraryPage 하단 검은 화면 + 버튼 폰트 크기 수정
**파일:** `front/src/pages/backoffice/ConManage/ContentFileLibraryPage.jsx`

### 배경
파일 라이브러리 카드 목록이 길어지면 마지막 줄 아래가 검게 보이고, 카드의
`사용중지`/`삭제` 버튼 글자가 파일명 라벨보다 크다는 제보.

### 변경 내용
- 카드 그리드 래퍼와 개별 카드에 명시적 배경색(`var(--bs-body-bg, #fff)`) 추가 —
  배경이 없으면(투명) 목록 하단 빈 영역이 브라우저/OS 다크모드 강제 렌더링으로
  검게 보이는 문제 방지(AG Grid 쪽에서도 동일 원리로 이미 `DarkMode.css`에
  `.ag-theme-material { background-color:#fff }` 처리가 돼 있던 것과 같은 케이스).
- 카드 액션 버튼(`사용중지`/`사용`/`삭제`) 폰트를 파일명 라벨과 같은 12px로 축소.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

---

## 12:15 — 메뉴 관리: 메뉴 이동(드래그&드롭) 시 PostgreSQL 타입 에러 수정
**파일:** `backend/src/main/resources/mapper/postgresql/bas/menu/MenuInfoManageMapper.xml`

### 원인
`updateMenuOrder` 쿼리만 `MENU_NO`/`MENU_ORDR`/`UPPER_MENU_NO`에 `::numeric` 캐스팅이
빠져 있었음. 같은 파일의 다른 쿼리(`insertMenuManage_S`/`updateMenuManage_S`/
`deleteMenuManage_S` 등)는 전부 이 세 컬럼을 `::numeric`으로 캐스팅하고 있어 컬럼
타입이 NUMERIC임이 확인됨. `CASE MENU_NO WHEN ? THEN ?` / `MENU_NO IN (?, ...)`에서
`jdbcType=INTEGER`로 바인딩된 파라미터와 numeric 컬럼을 캐스팅 없이 비교/대입해
PostgreSQL이 타입을 못 맞춰 에러가 나는 상황으로 추정.

### 변경 내용
- `updateMenuOrder`의 `MENU_NO`/`MENU_ORDR`/`UPPER_MENU_NO` 파라미터 바인딩에
  `::numeric` 캐스팅 추가(파일 내 다른 쿼리와 동일 컨벤션으로 통일).

### 검증
- XML 문법 유효성만 확인(PowerShell `[xml]` 파싱 OK). PostgreSQL DB가 이 환경에 없어
  실제 쿼리 실행/에러 재현은 못 함 — 배포 후 실제 메뉴 드래그&드롭 이동 테스트 필요.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 실 DB에서 재현/재확인 필요. 여전히 에러가 나면 정확한 예외 메시지로 재확인 예정.

---

## 13:05 — BasicBrodListPage를 리스트 관리 화면 기본 골격으로 전환 + 입력창 Modal화
**파일:** `front/src/pages/backoffice/BrodManage/BasicBrodListPage.jsx`,
`front/src/pages/backoffice/BrodManage/components/BasicBrodFormModal.jsx`(신규)

### 배경
CLAUDE.md 11번 "프론트엔드 리스트(관리) 화면 기본 골격"을 전혀 안 따르던 초기
프로토타입 형태 페이지(순수 inline style, `Swal.fire({input:'text'})`로 방송명
등록/수정)를 표준 골격 + 모달로 정리해 달라는 요청.

### 변경 내용
- `content-header`/`content-search`/`content-table content-table__main` 3단 골격 +
  `useGridInfinite`(무한스크롤) 적용. 그리드는 client 배열 대신 `AppAgGrid`
  `rowModelType="infinite"`로 교체(다른 목록 화면과 동일 컨벤션).
- "신규 등록"/"이름 수정"에 쓰이던 `Swal.fire({input:'text'})` 프롬프트를
  `BasicBrodFormModal`(신규, 방송명 단일 필드)로 교체 — `ScheduleFormModal`/
  `CodeFormModal`과 동일한 `.modal-header`/`.modal-body`/`.modal-footer` 구조.
- 방송 선택 시 우측에 있던 "배포 매장 관리" 패널은 그리드 하단 상세 카드 섹션으로
  이동(기능은 그대로: 이름수정/복사/삭제, 매장별 배포/해제 — 이 부분은 확인창(Swal)만
  쓰고 텍스트 입력이 없어서 모달화 대상이 아님).
- 페이지 자체 `getCookie('accessToken')` 체크는 제거 — 이 라우트는 이미
  `ProtectedRoute`(서버 검증 기반)로 감싸여 있어 중복이었음(다른 전환된 목록
  화면들도 이런 체크는 안 함).

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 백엔드 API는 변경 없음(`BASIC_BROD_LIST/UPDATE/DELETE/CENTER_LIST/CENTER_UPDATE`
  기존 엔드포인트 그대로 사용). 백엔드/DB가 없어 실제 화면 재확인은 못 함.

---

## 13:25 — BrodContentListPage를 리스트 관리 화면 기본 골격으로 전환 + 입력창 Modal화
**파일:** `front/src/pages/backoffice/BrodManage/BrodContentListPage.jsx`,
`front/src/pages/backoffice/BrodManage/components/BrodContentFormModal.jsx`(신규)

### 배경
`BasicBrodListPage`와 같은 폴더의 자매 페이지로, 역시 inline style 프로토타입 +
우측 인라인 등록/수정 폼 형태였음. 동일하게 골격 적용 + 모달화 요청.

### 변경 내용
- `content-header`/`content-search`/`content-table content-table__main` 골격 +
  `useGridInfinite` 적용(client 배열 → `rowModelType="infinite"`).
- 그리드 체크박스 다중선택(`rowSelection multiRow`)으로 "선택 삭제" 유지, 방송명은
  링크 버튼으로 눌러서 수정 모달 진입(스케줄 관리 화면과 동일 컨벤션).
- 우측 인라인 등록/수정 폼(방송명/재생간격/기초방송/사용유무)을 `BrodContentFormModal`
  (신규)로 교체. 등록/수정 진입 시 `BROD_CONTENT_FORM_DATA`로 콤보(재생간격/기초방송)
  + 상세를 불러오는 로직은 그대로 유지.
- 상단 안내문구("파일 편성은 콘텐츠 편성 화면에서 진행")는 `content-header` 아래
  얇은 배너로 유지.
- 페이지 자체 `getCookie('accessToken')` 체크 제거(`ProtectedRoute`로 이미 커버,
  `BasicBrodListPage` 때와 동일한 이유).

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 백엔드 API 변경 없음(`BROD_CONTENT_LIST/FORM_DATA/UPDATE/DELETE_BULK` 기존
  엔드포인트 그대로 사용). 백엔드/DB가 없어 실제 화면 재확인은 못 함.

---

## 13:50 — BrodAnniversaryListPage / BrodScheduleStatusPage 리스트 골격 전환 + 입력창 Modal화
**파일:** `front/src/pages/backoffice/BrodManage/BrodAnniversaryListPage.jsx`,
`front/src/pages/backoffice/BrodManage/BrodScheduleStatusPage.jsx`,
`front/src/pages/backoffice/BrodManage/components/BrodAnniversaryFormModal.jsx`(신규),
`front/src/pages/backoffice/BrodManage/components/BrodDeployTimeModal.jsx`(신규)

### 배경
같은 방송 관리 그룹의 나머지 두 프로토타입 페이지도 동일하게 골격 적용 + 입력창
모달화 요청(`BasicBrodListPage`/`BrodContentListPage`에 이어서).

### 변경 내용
- **BrodAnniversaryListPage**: `content-header`/`content-search`/`content-table` 골격
  적용 + `useGridInfinite`. 이 화면은 "방송 코드"가 있어야 조회되는 특성상 표준
  select+keyword 검색 대신 방송코드(필수)/기준일(선택) 두 입력을 그대로 유지.
  우측 인라인 등록/수정 폼을 `BrodAnniversaryFormModal`(신규)로 교체, 그리드에
  삭제 버튼 컬럼 추가.
- **BrodScheduleStatusPage**: 좌측 방송 목록에 골격 + `useGridInfinite` 적용.
  우측 "배포 매장 관리" 패널은 `BasicBrodListPage`와 동일하게 그리드 하단 상세
  카드 섹션으로 이동. 매장 배포 시 시작/종료 시간을 입력받던
  `Swal.fire({html:'<input>...'})` 프롬프트를 `BrodDeployTimeModal`(신규)로 교체.
- 두 화면 다 `getCookie('accessToken')` 수동 체크 제거(`ProtectedRoute`로 이미 커버,
  앞선 두 페이지와 동일 이유).

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `BrodAnniversaryFormModal`은 원본 인라인 폼에 없던 "구분(anniversaryGubun)" 입력
  필드가 폼 자체에 없던 기존 갭을 그대로 유지함(이번 요청 범위는 골격 전환/모달화라
  필드 추가는 별도 확인 후 진행하는 게 맞다고 판단). 필요하면 별도로 다뤄야 함.
- 백엔드 API 변경 없음(`BROD_ANNIVER_*`, `BROD_SCHEDULE_*` 기존 엔드포인트 그대로
  사용). 백엔드/DB가 없어 실제 화면 재확인은 못 함.
