# 2026-08-06 작업 로그

## 00:15 — MhsRoomManagePage "강의 관리" 등록/수정 모달화 + "편성표" 그리드 추가 + CSS 점검
**파일:** `front/src/pages/backoffice/RoomManage/MhsRoomManagePage.jsx`,
`front/src/pages/backoffice/RoomManage/components/MhsClassFormModal.jsx`(신규),
`backend/.../mapper/postgresql/sts/mhs/MhsClassManageMapper.xml`

### 배경
전날 밤 늦게 "모니터 관리" 탭만 골격+모달화했었는데, 이어서 "강의 관리" 탭의 인라인
등록 폼을 모달로 바꾸고, "편성표" 탭의 순수 HTML 테이블을 AG Grid로 바꿔달라는 요청 +
전반적인 CSS 재확인 요청.

### 변경 내용
- **강의 관리**: 인라인 우측 폼을 `MhsClassFormModal`(신규)로 분리. 강의실 필드를 자유
  텍스트 입력에서 `MHS_MONITOR_COMBO` 기반 select로 바꿈(레거시도 강의실=모니터코드였는데
  기존 코드는 코드값을 직접 타이핑하게 되어 있었음). 요일은 토글 버튼(일~토), 시작일/종료일은
  `type="date"`, 시작시간/종료시간은 `type="time"`으로 바꾸고 내부적으로 YYYYMMDD/HHmm
  문자열로 변환해서 저장(다른 화면들의 기존 관례와 통일). 목록도 `content-search`/
  `content-table` 골격 + 검색조건(강의명/강사명/강의실) 검색 추가, 강의실 컬럼도 그리드에
  추가(`mhsMonitornm`, 기존엔 아예 안 보이던 정보).
- **편성표**: 모니터/강의 select + "편성 등록" 버튼을 `content-search`로, 순수 `<table>`을
  `AppAgGrid`(client-side)로 교체.
- **CSS 점검**: 두 탭 모두 브랜드/매장 select와 마찬가지로 `content-search__option` 밖에
  있던 select/input들이 원인이었음 — 새로 짤 때부터 `content-search`/`content-search__option`
  구조를 지켜서 작성.

### 백엔드 버그 발견 및 수정 — 강의 검색조건 값 불일치
`MhsClassManageMapper.xml`의 `selectMhsClassListByPagination`(목록)은
`searchCondition == 'MHS_CLASSNM'`(접두사 없음)을 기대하는데, `selectMhsClassListCnt`(총건수)는
`searchCondition == 'a.MHS_CLASSNM'`(소문자 `a.` 접두사)을 기대하고 있어서 — 프론트가 어느
쪽 형식을 보내든 목록과 총건수 중 하나는 검색조건이 안 먹는 상태였음. 목록 쿼리 쪽 형식
(접두사 없음)에 맞춰 총건수 쿼리를 통일.

### 검증
- `npm run build`, `./gradlew compileJava` 모두 정상 확인

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 백엔드 재시작 필요(`MhsClassManageMapper.xml` 변경분 반영)

## 00:40 — MHS 모니터 "미리보기" 기능을 레거시 preView.jsp 디자인으로 재구현
**파일:** `front/src/pages/backoffice/RoomManage/MhsRoomManagePage.jsx`,
`front/src/pages/backoffice/RoomManage/components/MhsMonitorPreviewModal.jsx`(신규)

### 배경
기존엔 "미리보기" 버튼이 단순 텍스트 Swal 팝업이었는데, 레거시 `preView.jsp`(회의실알림이
DID 실제 송출 화면 — 룸명/센터명/날짜시각 헤더, 진행중인 강의 카드, 오늘 강의 시간표,
이마트 로고 푸터로 구성된 스와이프 슬라이더 화면) 디자인을 참고해서 다시 만들어달라는 요청.

### 변경 내용
- `MhsMonitorPreviewModal`(신규) — 레거시의 슬라이드 4장(강의현황표×2 + 현재강의카드×2)을
  모달 하나에 세로로 이어붙여 재현: 어두운 헤더(룸명/센터명 + 날짜·요일·실시간 시계) →
  현재 강의 카드(제목/시간/강사명/비고 + 다음 강의 안내) → 오늘 강의 시간표(진행중인 행
  강조) → 이마트 로고 푸터
- 백엔드는 이미 있는 `viewConn/preview.do`(`monitorInfo`+`resultList`+`pageInfo`)를 그대로
  사용 — `pageInfo`에 서버가 이미 계산해 둔 `mhsNowgubun`(NOW/NEXT)이 있어서, 레거시 JS가
  매초 직접 하던 "지금이 이 강의 시간대 안에 있는가" 비교 로직을 거의 그대로 재사용(진행중
  강조 표시는 오늘 시간표 쪽만 프론트에서 다시 계산— 목록 전체에 하이라이트를 매겨야 해서)
- 장비 전용 동작(스와이프 자동 순환, 1분마다 재조회, 1시간마다 페이지 새로고침, 그리고
  레거시 코드에 남아있던 `alert(currentTime)` 디버그 잔재)은 재현하지 않음 — 관리자용
  미리보기 모달이라 실시간 시계만 유지하고 나머지는 불필요

### 검증
- `npm run build` 정상 확인

## 01:10 — ContentDetailEditorPage: 배치된 파일 드래그앤드롭 정렬 + 미디어 리스트 자동 조회
**파일:** `front/src/pages/backoffice/ConManage/ContentDetailEditorPage.jsx`

### 변경 내용
- "배치된 파일" 순서 변경을 ▲▼ 버튼에서 행 자체를 끄는 네이티브 HTML5 드래그앤드롭으로
  교체(별도 라이브러리 설치 없이 `draggable`/`onDragStart`/`onDragOver`/`onDrop`만 사용,
  레거시도 `dragHandle` 요소가 있던 자리). 드롭 시 이동 구간에 포함된 파일들의 `fileOrder`를
  0부터 다시 순서대로 매겨서 저장.
- "미디어 리스트"가 "조회" 버튼을 눌러야만 나오던 문제 — 레거시 `contentLst()`처럼 페이지
  진입 시 자동으로 한 번 조회하도록 수정(검색 로직을 `loadMediaSearch`로 분리해서 최초
  진입/버튼 클릭 모두 재사용).

### 검증
- `npm run build` 정상 확인

## 01:25 — ContentDetailEditorPage: 미디어 리스트 검색영역 한 줄 정렬 + 페이징 추가
**파일:** `front/src/pages/backoffice/ConManage/ContentDetailEditorPage.jsx`

### 변경 내용
- 검색영역(콘텐츠타입 select + 콘텐츠명 input + 조회 버튼)이 줄바꿈되던 문제 —
  `content-search__option`/`content-search__action` 클래스로 감싸고 각 입력 요소에 명시적
  너비를 줘서 한 줄로 고정
- 미디어 리스트에 페이징 추가 — 20건 단위, 이전/다음 버튼 + "N / 총페이지 (총 M건)" 표시.
  검색 버튼/최초 진입 시 1페이지로 리셋

### 검증
- `npm run build` 정상 확인

## 02:00 — 인증 만료 시 로그인 페이지 이중 리다이렉트/화면 깜박임 수정
**파일:** `front/src/routes/ProtectedRoute.jsx`, `front/src/service/api/fn-refresh.jsx`

### 배경
토큰 만료 시 로그인 페이지로 두 번 연속 이동하거나 화면이 깜박이는 현상 — 리프레시 토큰이
없는 개발 환경(Redis 미사용)에서는 매번 발생. 원인 조사 결과, 로그인 페이지로의 리다이렉트
경로가 최대 3곳에서 중복 실행되고 있었음:
1. `ProtectedRoute`의 인증확인(AUTH_CHECK) 호출 — 그 호출 자체가 `fnAjaxFetch`를 거치는데,
   `fnAjaxFetch` 내부에 자체 401 처리(Swal + `window.location.href='/login'` 강제 이동)가
   있어서 이게 먼저 실행됨
2. `fn_Refresh()` 자체도 백엔드 리프레시 호출이 실패하면 별도로 Swal + 강제 이동을 했음
3. `ProtectedRoute`의 정상적인 SPA 리다이렉트(`<Navigate to="/login">`)

세 경로가 동시에 실행되면서 하드 리다이렉트(전체 새로고침)와 SPA 리다이렉트가 겹쳐 로그인
페이지가 두 번 걸리거나 화면이 깜박였음.

### 변경 내용
- `ProtectedRoute.jsx` — AUTH_CHECK 호출에 `suppressErrorHandling: true` 추가. 이 호출의
  401/리프레시 실패 처리는 `ProtectedRoute`의 `<Navigate>` 하나로만 처리되도록 함(fnAjaxFetch
  자체 리다이렉트 비활성화).
- `fn-refresh.jsx` — `fn_Refresh()`에서 자체 Swal + `window.location.href` 로직을 완전히
  제거(성공/실패 boolean만 반환). 실패 시 사용자 안내/이동은 항상 호출부
  (`fn-ajax-fetch.jsx`의 `handleUnauthorized`, 또는 `ProtectedRoute`)에서만 하도록 일원화 —
  이건 ProtectedRoute뿐 아니라 다른 일반 API 호출에서 리프레시가 실패하는 경우에도 똑같이
  중복 리다이렉트를 유발하던 문제라 같이 고침.

### 검증
- `npm run build` 정상 확인

## 02:15 — ProgrameInfo(프로그램 관리) 그리드 하단 검은 영역 수정
**파일:** `front/src/pages/backoffice/Basic/ProgrameInfo.jsx`

### 원인
AG Grid에 `domLayout='autoHeight'`(데이터 양만큼만 그리드 높이 자동 조절)를 쓰면서, 동시에
감싸는 `<div className="ag-theme-material">`에 `height: 760`(고정 높이)을 줘서 — 실제 그리드는
데이터 15건만큼만(짧게) 렌더링되는데 바깥 div는 760px를 그대로 차지해서, 그리드 아래
빈 공간에 ag-theme-material의 어두운 배경색이 그대로 노출됐음.

### 수정
- 고정 `height: 760` 제거(`autoHeight`와 상충) — 그리드가 실제 데이터 양만큼만 높이를
  차지하도록 정리
- 같은 패턴(`domLayout='autoHeight'` + 고정 높이 wrapper)을 쓰는 다른 화면이 있는지 전체
  검색해서 확인 — `MasterDetailGrid.jsx`도 `autoHeight`를 쓰지만 그걸 쓰는 화면들
  (`DIdGroupInfo.jsx` 등)은 고정 높이 wrapper가 없어서 문제없음

### 검증
- `npm run build` 정상 확인

## 02:30 — AG Grid 하단 검은 영역 전역 수정 (여러 화면 공통)
**파일:** `front/src/style/DarkMode.css`

### 원인
`.ag-theme-material` wrapper div(우리가 직접 `height:760` 같은 고정 높이를 주는 곳들)에
그리드 콘텐츠(행+페이징)가 그 고정 높이보다 짧게 렌더링되면, 남는 빈 공간에는 wrapper
자체 배경(지정 안 하면 투명)이 아니라 페이지 배경이 그대로 노출됨. 라이트 모드는 페이지
배경이 흰색이라 안 보였지만, 다크 모드는 페이지 배경이 어두운 색이라 그대로 드러남 —
ProgrameInfo(프로그램 관리)뿐 아니라 ManagerListPage(관리자 관리) 등 여러 화면에서 동일하게
발생.

### 수정
- `.ag-theme-material`에 명시적 배경색 추가(라이트: `#fff`, 다크: 기존에 선언은 돼 있었지만
  실제로 안 쓰이던 `#25262b`를 `background-color`로 실제 적용) — 개별 화면마다 고치는 대신
  전역 CSS 한 곳에서 처리해서 다른 화면에도 동일하게 적용됨

### 검증
- `npm run build` 정상 확인

## 02:50 — ContentDetailEditorPage: 이미지 추가 시 기본 송출시간 10초 + 콘텐츠 삭제 버그 2건 발견/수정
**파일:** `front/src/pages/backoffice/ConManage/ContentDetailEditorPage.jsx`,
`backend/.../mapper/postgresql/sts/cnt/ContentFileManagerMapper.xml`

### 요청 배경
1. "미디어 리스트에서 추가할 때 송출시간을 기본 10초로 채워달라" — (동영상은 재생시간을
   수동 입력할 필요 없다는 점은 사용자가 스스로 정정)
2. "ContentDetailEditorPage에서 콘텐츠 삭제하니 로그인 만료라고 하면서 로그인 페이지로
   갔다가 다시 기본 페이지로 이동한다, 토큰 확인 좀 해달라"

### 1) 이미지 기본 송출시간
`handleAddFile`에서 이미지(`mediaType==='IMAGE'`)일 때만 등록 요청에 `timeInterval:'10'`을
같이 보내도록 수정(백엔드 `insertContentDetailFileManage`가 `timeInterval` 파라미터를 그대로
저장하는 걸 확인). 동영상/음원은 여전히 값을 안 보내서(백엔드가 실제 재생시간으로 자동
보정하는 기존 로직 그대로 유지), 사용자가 직접 정정한 대로 손대지 않음.

### 2) 삭제 버그 조사 결과 — "로그인 만료"가 아니라 실제 삭제 실패였음
임시 인스턴스(7099)에 테스트용 콘텐츠를 등록 후 실제로 삭제해보니, **인증과 무관하게
resultCode 500(진짜 서버 오류)이 나는 걸 확인**. `e.printStackTrace()`로 원인 확인:

```
DELETE FROM LETTNFILEDETAIL WHERE CON_SEQ = ?
Cause: PSQLException: operator does not exist: numeric = character varying
```

`ContentFileManagerMapper.xml`의 `deleteFileConSeq`가 `CON_SEQ`(실제 컬럼 타입 numeric)에
타입 캐스트 없이 문자열 파라미터를 그대로 바인딩하고 있었음 — 이번 세션에서 반복적으로
나온 numeric/varchar 캐스트 누락 패턴과 동일. `#{conSeq,jdbcType=VARCHAR}::numeric`으로 수정.

**추가로 발견한 프론트 버그**: `handleDeleteContent`가 삭제 API 응답을 전혀 확인하지 않고
무조건 목록 페이지로 `navigate()`하고 있었음 — 그래서 삭제가 (이번처럼) 500으로 실패해도
사용자는 마치 성공한 것처럼 조용히 목록으로 넘어가버리는 상태였음. 응답의
`resultCodeInfo`를 확인해서 실패 시 에러 Swal을 띄우고 목록 이동은 하지 않도록 수정.

**"로그인 만료" 메시지 자체의 원인**: 위 두 버그와는 별개로, 삭제 후
`navigate('/backoffice/sub/conManage/muti')`로 이동하면 `ProtectedRoute`가 그 새 페이지에서
다시 인증확인을 하는데, 그 시점에 실제로 토큰이 만료돼 있었다면(리프레시 토큰도 없는
환경이라) 로그인 페이지로 가는 것 자체는 정상 동작 — 다만 이전에 있던 이중 리다이렉트
버그(오늘 앞서 수정함) 때문에 그 과정이 지저분하게 보였을 가능성이 큼. 이번 삭제 500 버그가
"매번" 재현되는 문제였다면 그게 원인이었을 가능성이 더 높음.

### 검증
- 7099 인스턴스에서 테스트 콘텐츠 등록 → 삭제 재현(500 확인) → 수정 → 재기동 → 재현 콘텐츠로
  삭제 성공 확인(`resultCode:200`)
- `npm run build`, `./gradlew compileJava` 모두 정상 확인
- 진단용 `printStackTrace()`는 확인 후 제거 완료

### 이슈 / 개선사항
- 백엔드(7004) 재시작 필요(`ContentFileManagerMapper.xml` 변경분 반영)

## 03:15 — ContentDetailEditorPage: "제외" 삭제 400 에러 수정 + 레거시 참고 UI 보강
**파일:** `front/src/pages/backoffice/ConManage/ContentDetailEditorPage.jsx`

### 1) "제외"(페이지에서 파일 삭제) 400 에러 — 실사용 로그로 확인
사용자가 붙여준 실제 서버 로그:
```
MissingServletRequestParameterException: Required request parameter 'detailSeq' ... is not present
```
`handleRemoveFile`이 `fnAjaxFetch({ method:'DELETE', param:{detailSeq} })` 형태로 호출했는데,
`fnAjaxFetch`는 GET이 아닌 메서드에 `param`을 주면 쿼리스트링이 아니라 **요청 바디**로 보냄 —
백엔드(`ContentDetailFileInfoManageController.deleteContentReg`)는 `detailSeq`를
`@RequestParam`(쿼리스트링)으로 받기 때문에 항상 400이 났음. URL에 직접
`?detailSeq=...`를 붙이는 방식으로 수정(같은 파일의 다른 DELETE 호출들, 다른 화면의
`DidGroupDetailCellRenderer.jsx` 등과 동일한 패턴으로 통일).

### 2) 레거시(conMutiView_back.jsp) 실제 화면 스크린샷 참고 UI 보강
- "배치된 파일" 테이블에 "상세정보"(파일 가로x세로, 예: `1080x1920`) 컬럼 추가 —
  `CON_DETAIL_FILE_LIST`가 이미 `fileWidth`/`fileHeight`를 내려주고 있었는데 안 쓰고 있었음
- "송출시간 적용" 버튼을 이미지일 때만 보이게 했던 걸 레거시처럼 모든 파일 타입에 항상
  보이도록 수정(입력창 자체의 readOnly는 동영상/음원에 대해 유지 — 재생시간 자동 보정
  로직과 일치)
- "미디어 리스트"를 그리드(카드형 여러 열) → 레거시처럼 세로로 한 줄씩 쌓이는 리스트로
  변경(썸네일 + 파일명 + 추가 버튼이 한 행에 나란히)

### 검증
- `npm run build` 정상 확인

## 04:00 — 화면생성/스케줄전송 백엔드 연결 + 신규 코드 버그 2건 + 저장 위치 수정
**파일:** `backend/.../web/ContentDetailFileInfoManageController.java`,
`front/src/pages/backoffice/ConManage/ContentDetailEditorPage.jsx`, `front/src/constants/URL.jsx`

### 배경
사용자가 `ContentDetailFileInfoManageController.java`에 레거시 `preView()`/`sendSchedule()`에
대응하는 백엔드(`preViewCheck.do`/`contentPreview.do`/`contentScheduleSend.do`, 콘텐츠
미리보기 HTML 생성 로직 `conPageDetail`/`ContentFileCreate`/`ContentFileCreateLocal` 포함)를
직접 포팅 — 검토 후 프론트("화면생성"/"스케줄전송" 버튼)에 연결해달라는 요청.

### 검토 중 발견한 버그 2건 (연결 전 수정)
1. **`preViewCheck.do`(`selectPreViewCheck`) — resultCode가 항상 500**: `status`/`message`는
   `ret < 1`(미입력 없음 → 성공) 기준으로 올바르게 세팅했는데, 실제 `resultCode`만
   `ret < 0`(카운트라 절대 음수가 될 수 없어 항상 거짓)로 잘못 비교 — 성공 케이스에서도
   `resultCode`만 500이 나가는 상태였음. `ret < 1`로 통일.
2. **`ContentFileCreateLocal`의 `vo.setConLocalfile(vo.getConLocalfile())`**: 방금 만든
   `vo`(필드 전부 null)에서 자기 자신을 읽어 자기 자신에 다시 쓰는 자기참조 — 항상 null을
   저장하는 것과 같음(주석에 "기존 vo.setConLocalfile(fileName); 과 동일"이라고 되어 있어
   원래 의도가 fileName이었던 게 리팩터링 중 유실된 것으로 보임) → `fileName`으로 수정.

### 연결 중 실사용 테스트로 발견한 문제 — 저장 위치 + DB 컬럼 길이
실제로 호출해보니 생성된 HTML이 `upload/` 루트에 바로 만들어졌음(사용자 지적) — 일반
업로드 미디어와 같은 관례(`EMART_DID/did/upload/{yyyyMM}/`)로 옮기려고 처음엔 DB
컬럼(`CON_FILE`/`CON_LOCALFILE`)에 전체 상대경로를 저장하도록 고쳤는데, 실제 삽입 시
`PSQLException: value too long for type character varying(30)` 발생(두 컬럼 다 30자 제한).
DB 스키마는 안 건드리고(승인 없이 변경 금지 원칙), 대신:
- 파일 자체는 `EMART_DID/did/upload/{yyyyMM}/`에 생성
- DB에는 기존처럼 파일명만 저장(30자 이내로 유지)
- `contentPreview.do` 응답에 `result.fileUrl`(전체 상대경로)을 새로 내려주고, 프론트는 콘텐츠
  상세를 다시 조회하는 대신 이 값을 그대로 써서 `/upload/` 미리보기 창을 연다

### jQuery 참조 경로 수정
사용자가 `webapp/upload/jquery-4.0.0.min.js`를 직접 배치 — `conPageDetail`의 스크립트 참조를
기존 `jquery-2.2.4.min.js`(파일명 불일치)에서 `jquery-4.0.0.min.js`로, 서버 재생("S") 참조
경로도 존재하지 않는 `/js/`에서 실제 위치인 `/upload/`로 수정.

### 프론트 연결
- `URL.jsx`에 `CON_DETAIL_PREVIEW_CHECK`/`CON_DETAIL_CONTENT_PREVIEW`/`CON_DETAIL_SCHEDULE_SEND`
  추가
- "화면생성" → `preViewCheck.do` 통과 시 `contentPreview.do` 호출 → 응답의 `fileUrl`로 새 창
  미리보기(레거시 `preView()`와 동일 플로우)
- "스케줄전송" → `preViewCheck.do` 통과 시 `contentScheduleSend.do` 호출(레거시
  `sendSchedule()`과 동일 플로우, 메시지 문구도 레거시 그대로 구분: 미리보기는 "송출시간을
  입력하지 않은 콘텐츠가 있습니다", 스케줄전송은 "이미지 중 타임값을 넣지 않은 콘텐츠가
  있습니다")

### 검증 (7099 임시 인스턴스, 실제 콘텐츠 conSeq=4522로 반복 테스트)
- 위 버그 2건 수정 → 재현 → 재수정까지 총 4~5회 재기동하며 확인
- 최종적으로 `preViewCheck.do`/`contentPreview.do` 둘 다 `resultCode:200` 확인,
  생성된 파일이 `EMART_DID/did/upload/202608/`에 만들어지고 `/upload/` 경로로 정상 접근되는
  것까지 확인
- 테스트 중 잘못된 위치(구 `upload/` 루트)에 생겼던 임시 파일 2개는 정리 완료
- `npm run build`, `./gradlew compileJava` 모두 정상 확인

### 이슈 / 개선사항
- 백엔드(7004) 재시작 필요
- `contentScheduleSend.do`(실제 단말 전송 이력 등록)는 이번엔 라이브 인스턴스에서 실행까지는
  안 해봄(실데이터에 스케줄 전송 이력이 실제로 쌓이는 부작용이 있어서) — 실사용 전에 한 번
  실제 스케줄 있는 콘텐츠로 확인 권장

## 04:30 — 동영상 추가 시 송출시간 자동 입력 + "파일 추가" 기능 전체가 깨져있던 버그 3건 발견/수정
**파일:** `front/src/pages/backoffice/ConManage/ContentDetailEditorPage.jsx`,
`backend/.../mapper/postgresql/sts/cnt/ContentDetailFileInfoManagerMapper.xml`,
`backend/.../mapper/postgresql/sts/cnt/ContentDetailManagerMapper.xml`

### 요청
"동영상 추가할 때는 동영상 파일 정보의 재생시간 정보를 송출시간에 입력해줘"

### 조사 중 발견 — insert.do(파일 추가)가 사실상 전부 깨져 있었음
라이브 테스트로 실제 추가를 재현해보니, **이미지든 동영상이든 상관없이 "추가" 자체가 항상
500으로 실패**하고 있었음(사용자가 요청한 동영상 케이스뿐 아니라 이미지도 마찬가지).
연쇄적으로 발견된 원인 3가지, 모두 수정:

1. **`selectContentDetailFileInfoFileSeq` 쿼리 문법 오류** —
   `CEIL(B.PLAY_TIME::numeric) AS playTime AS playTime` 처럼 `AS playTime`이 중복되어 있어
   SQL 문법 에러(`syntax error at or near "AS"`). 중복 제거.
2. **같은 쿼리의 `PLAY_TIME` 캐스트가 빈 문자열에서 깨짐** — 이미지 파일은 `PLAY_TIME`이
   빈 문자열이라 `''::numeric` 캐스트가 실패. `NULLIF(B.PLAY_TIME, '')`로 감쌈.
3. **`updateContentDetailTimeManage`(페이지 총 재생시간 재계산)의 서브쿼리에 캐스트 누락** —
   `WHERE DETAIL_SEQ = ?`(캐스트 없음)이 `WHERE DETAIL_SEQ = ?::numeric`(바깥 쿼리, 캐스트 있음)과
   짝이 안 맞아서 `numeric = character varying` 에러. 캐스트 추가.
4. **`insertContentDetailFileManage`에 `FILE_SEQ` 채번값을 되돌려주는 selectKey가 없었음** —
   `NEXTVAL('contentfile_seq')`로 DB에서만 채번하고 Java `vo` 객체에는 반영이 안 돼서,
   `insertContentReg`가 삽입 직후 "동영상/음원이면 실제 재생시간으로 보정"하려고
   `vo.getFileSeq()`로 재조회하는 로직이 **항상 `fileSeq=null`이라 아무 결과도 못 찾고
   조용히 스킵**되고 있었음(있으나 마나 한 죽은 코드였음). `<selectKey order="AFTER">
   SELECT CURRVAL('contentfile_seq')`로 채번값을 `vo.fileSeq`에 채워 넣도록 수정 — 이제
   기존에 있던 자동 보정 로직이 실제로 동작함.
5. **위 결과로 TIME_INTERVAL이 빈 문자열(`''`)로 계속 남아있던 것**이 `TB_CONDETAIL`
   총합 재계산 쿼리(`CAST(TIME_INTERVAL AS INTEGER)`)에서 매번 크래시 —
   `CAST(NULLIF(TIME_INTERVAL, '') AS INTEGER)`로 방어.

### 요청사항 자체 구현
- `handleAddFile`을 파일 객체 전체를 받도록 바꾸고, 이미지는 기존처럼 기본 10초, 동영상/음원은
  `file.playTime`(실제 파일 재생시간)을 그대로 `timeInterval`로 보내 등록하도록 함 — 백엔드
  자동 보정(위 4번 수정으로 이제 정상 동작)과 이중으로 안전하게 겹치게 함.

### 검증 (7099 임시 인스턴스, 실제 콘텐츠 conSeq=4522/detailSeq=4743로 반복 재현·수정 확인)
- 버그 1~5를 순서대로 재현 → 수정 → 재기동하며 최종적으로 이미지/동영상 추가 모두
  `resultCode:200` 확인, 동영상은 `timeInterval`이 실제 재생시간(24초)으로 정확히
  자동 채워지는 것까지 확인
- **테스트 도중 이 페이지에 원래 있던 파일 2개(나린 mp4, 무빙워크미디어 mp4)가 DB에서
  사라져 있는 것을 발견**(정확한 원인은 특정 못함 — 이번 세션 내 제 작업으로 인한 것인지
  다른 동시 작업 때문인지 불확실). 제가 만든 테스트용 중복 행 9개는 모두 삭제했고, 없어진
  원본 2개 파일은 같은 atchFileId로 다시 추가해서 페이지를 원래와 동일한 구성(파일
  2개·순서 동일)으로 복원함 — 다만 FILE_SEQ 값 자체는 새로 채번되어 바뀌었고, 나린
  mp4의 송출시간은 원래 비어있었는데 이제 보정 로직이 정상 동작해서 24초로 채워진 상태.
- `npm run build`, `./gradlew compileJava` 모두 정상 확인, 진단용 `printStackTrace()` 제거 완료

### 이슈 / 개선사항
- 백엔드(7004) 재시작 필요
- ⚠️ conSeq=4522 페이지의 파일 2개가 왜 없어져 있었는지는 확실하지 않음 — 혹시 최근에
  이 콘텐츠를 직접 만지신 적이 있다면 확인 부탁드립니다(복원은 해뒀지만 FILE_SEQ가 바뀜)
